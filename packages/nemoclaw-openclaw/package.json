{
  "$comment": "SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.\nSPDX-License-Identifier: Apache-2.0",
  "name": "@nvidia/nemoclaw-openclaw",
  "version": "0.1.0",
  "description": "OpenClaw agent runtime package for NemoClaw",
  "license": "Apache-2.0",
  "scripts": {
    "build:adapters": "nemoclaw-build-adapters .",
    "check:adapters": "tsc -p tsconfig.adapters.json && npm run build:adapters -- --check",
    "test:package": "npm run check:adapters && vitest run --config vitest.config.ts",
    "test:fabric": "bash tests/fabric/run-tests.sh",
    "test:fabric:composed": "bash tests/fabric/run-tests.sh composed",
    "test:nemoclaw": "vitest run --config vitest.nemoclaw.ts && npm --prefix plugin test && npm run test:fabric:composed",
    "test:coverage": "vitest run --config vitest.coverage.mts --coverage && node --experimental-strip-types --no-warnings tests/helpers/coverage-ratchet.mts",
    "test:spec": "vitest run --config vitest.config.ts --reporter=tree && vitest run --config vitest.nemoclaw.ts --reporter=tree && npm --prefix plugin test -- --reporter=tree",
    "test:watch": "vitest watch --config vitest.config.ts",
    "test": "npm run test:package && npm run test:nemoclaw",
    "typecheck": "tsc -p tsconfig.test.json && tsc -p tsconfig.adapters.json && tsc -p jsconfig.json && npm --prefix plugin run typecheck"
  },
  "files": [
    "**/*",
    "!.e2e/**",
    "!coverage/**",
    "!coverage-threshold.json",
    "!jsconfig.json",
    "!tests/**",
    "!package-lock.json",
    "!host/source/**",
    "!tsconfig.adapters.json",
    "!tsconfig.test.json",
    "!vitest.config.ts",
    "!vitest.coverage.mts",
    "!vitest.nemoclaw.ts",
    "!plugin/src/**/*.test.ts",
    "!plugin/tsconfig.test.json",
    "!plugin/vitest.config.ts",
    "!plugin/vitest.project.ts",
    "!plugin/node_modules/**",
    "!**/__pycache__/**",
    "!**/*.pyc"
  ],
  "nemoclaw": {
    "harnessManifest": "manifest.yaml"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "devDependencies": {
    "@nvidia/nemoclaw-harness-contract": "^0.1.0",
    "@vitest/coverage-v8": "^4.1.0",
    "@types/node": "^25.5.2",
    "typescript": "6.0.3",
    "vitest": "^4.1.9"
  }
}
