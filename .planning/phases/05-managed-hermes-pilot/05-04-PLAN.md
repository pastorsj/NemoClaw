---
phase: 05-managed-hermes-pilot
plan: 04
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - packages/nemoclaw-hermes/agent-runtime-package.yaml
  - packages/nemoclaw-hermes/tests/service-declarations.test.ts
  - src/lib/agent/definition-types.ts
  - src/lib/agent/defs.ts
  - src/lib/onboard/hermes-api-port.ts
  - src/lib/onboard/hermes-api-port.test.ts
  - src/lib/hermes-dashboard.ts
  - src/lib/hermes-dashboard.test.ts
  - src/lib/onboard/hermes-dashboard.ts
  - src/lib/onboard/hermes-dashboard.test.ts
  - src/lib/onboard/managed-workload/hermes-state-volume.ts
  - src/lib/onboard/managed-workload/hermes-state-volume.test.ts
autonomous: true
requirements: [HER-01]
must_haves:
  truths:
    - "Hermes dashboard, API port, and health use existing AgentDefinition fields and current narrow owners."
    - "Hermes state volume behavior remains in the current Hermes-specific volume owner."
    - "No named-services, generic resource, receipt, journal, or migration API is introduced."
  artifacts:
    - path: packages/nemoclaw-hermes/agent-runtime-package.yaml
      provides: Current `forward_ports`, `dashboard`, and `health_probe` data.
    - path: src/lib/onboard/hermes-api-port.ts
      provides: Existing Hermes API port allocation.
    - path: src/lib/onboard/managed-workload/hermes-state-volume.ts
      provides: Existing `/sandbox/.hermes` volume behavior.
  key_links:
    - from: packages/nemoclaw-hermes/agent-runtime-package.yaml
      to: src/lib/agent/definition-types.ts
      via: Current AgentDefinition fields.
    - from: src/lib/onboard/hermes-dashboard.ts
      to: src/lib/hermes-dashboard.ts
      via: Current dashboard configuration and forward setup.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Preserve Hermes dashboard, API port, health, and state volume through their current narrow owners.

Purpose: Complete package extraction without generalizing a one-runtime resource shape.
Output: Descriptor parity, current owner adapters, and focused port/dashboard/volume tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-managed-hermes-pilot/05-CONTEXT.md
@.planning/phases/05-managed-hermes-pilot/05-02-SUMMARY.md
@agents/hermes/manifest.yaml
@src/lib/agent/definition-types.ts
@src/lib/onboard/hermes-api-port.ts
@src/lib/hermes-dashboard.ts
@src/lib/onboard/hermes-dashboard.ts
@src/lib/onboard/managed-workload/hermes-state-volume.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve current service declarations and owners</name>
  <files>packages/nemoclaw-hermes/agent-runtime-package.yaml, packages/nemoclaw-hermes/tests/service-declarations.test.ts, src/lib/agent/definition-types.ts, src/lib/agent/defs.ts, src/lib/onboard/hermes-api-port.ts, src/lib/onboard/hermes-api-port.test.ts, src/lib/hermes-dashboard.ts, src/lib/hermes-dashboard.test.ts, src/lib/onboard/hermes-dashboard.ts, src/lib/onboard/hermes-dashboard.test.ts</files>
  <read_first>agents/hermes/manifest.yaml, src/lib/agent/definition-types.ts, src/lib/agent/defs.ts, src/lib/onboard/hermes-api-port.ts, src/lib/hermes-dashboard.ts, src/lib/onboard/hermes-dashboard.ts</read_first>
  <action>Carry the existing `forward_ports`, `dashboard`, and `health_probe` values into the package descriptor and keep their current AgentDefinition-derived behavior. Preserve API port allocation/rebind in `hermes-api-port.ts` and dashboard config/forward behavior in the two current dashboard modules. The package may supply data but cannot allocate a host port, create a route, execute a probe, interpret product status, or restart Hermes. Do not create `named-services.ts`, a generic service catalogue, a new service receipt, or compatibility migration.</action>
  <verify><automated>npm --prefix packages/nemoclaw-hermes test -- service-declarations &amp;&amp; npx vitest run --project cli src/lib/onboard/hermes-api-port.test.ts src/lib/hermes-dashboard.test.ts src/lib/onboard/hermes-dashboard.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - Existing dashboard label, path, port, health, and forward behavior are unchanged.
    - Host port allocation and status interpretation stay in core.
    - No new general service or receipt abstraction exists.
    - Package declarations contain no host command or callback.
  </acceptance_criteria>
  <done>Hermes services remain data-driven through existing narrow core owners.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve current Hermes state-volume behavior</name>
  <files>src/lib/onboard/managed-workload/hermes-state-volume.ts, src/lib/onboard/managed-workload/hermes-state-volume.test.ts, packages/nemoclaw-hermes/agent-runtime-package.yaml, packages/nemoclaw-hermes/tests/service-declarations.test.ts</files>
  <read_first>src/lib/onboard/managed-workload/hermes-state-volume.ts, src/lib/onboard/managed-workload/hermes-state-volume.test.ts, src/lib/onboard/__test-helpers__/hermes-state-volume.ts, agents/hermes/manifest.yaml</read_first>
  <action>Keep Docker volume identity, labels, create/reuse/remove behavior, and `/sandbox/.hermes` mounting in the current Hermes-specific module. Let the descriptor carry the existing state path only. Preserve current cleanup and rollback tests. Do not replace the module with a generic volume manager, add a second journal or receipt, or introduce a state migration API. Generalize only after OpenClaw proves a second current consumer and only by replacing a named old path in a separately accepted change.</action>
  <verify><automated>npx vitest run --project cli src/lib/onboard/managed-workload/hermes-state-volume.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - Existing volume name, labels, mount path, reuse, and cleanup behavior remain.
    - The package cannot create or delete the host Docker volume.
    - No generic volume, journal, receipt, or migration API is added.
  </acceptance_criteria>
  <done>Hermes state storage stays with its tested current owner and behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Untrusted input | Control |
|---|---|---|
| Descriptor service data | Package YAML | Existing AgentDefinition validation and core allocation |
| Health declaration | Package data | Current core/OpenShell probe execution and interpretation |
| State path | Package declaration | Hermes-specific volume module and fixed mount target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| HER-T10 | Elevation of privilege | Host port | Mitigate | Allocation remains in `hermes-api-port.ts` |
| HER-T11 | Spoofing | Health | Mitigate | Core executes and interprets the declared probe |
| HER-T12 | Tampering | State volume | Mitigate | Fixed Hermes labels, path, and cleanup tests |
</threat_model>

<verification>
- [ ] Service declaration, dashboard, port, and volume tests pass.
- [ ] No `named-services.ts` or generic resource owner is introduced.
- [ ] No package code performs a host route, volume, probe, or lifecycle effect.
</verification>

<success_criteria>
- Dashboard/API behavior remains unchanged.
- State-volume behavior remains unchanged.
- The design stays Hermes-specific until a proven second consumer exists.
</success_criteria>

<output>
After completion, create `.planning/phases/05-managed-hermes-pilot/05-04-SUMMARY.md`.
</output>
