---
phase: 05-managed-hermes-pilot
plan: 01
type: execute
wave: 1
depends_on: ["04-04"]
files_modified:
  - packages/nemoclaw-hermes/move-manifest.json
  - packages/nemoclaw-hermes/build-inputs.manifest.json
  - packages/nemoclaw-hermes/package.json
  - packages/nemoclaw-hermes/agent-runtime-package.yaml
  - packages/nemoclaw-hermes/Dockerfile
  - packages/nemoclaw-hermes/Dockerfile.base
  - packages/nemoclaw-hermes/runtime/config/generate.ts
  - packages/nemoclaw-hermes/runtime/config/build-env.ts
  - packages/nemoclaw-hermes/tests/package-boundary.test.ts
  - src/lib/agent/defs.ts
  - src/lib/agent/base-image.ts
  - .github/workflows/base-image.yaml
  - .github/workflows/managed-images.yaml
  - test/package-contract/managed-hermes-package-boundary.test.ts
  - test/package-contract/managed-hermes-path-cutover.test.ts
  - package.json
  - package-lock.json
autonomous: true
requirements: [HER-01, HER-02]
must_haves:
  truths:
    - "Managed Hermes has one package source root and one exact generated move manifest."
    - "Portable Hermes and the host tool-gateway broker retain their current owners."
    - "The copied managed package builds only from declared packed inputs."
    - "Hermes follows the exact Phase 4 runtime-support absent/present decision without creating or expanding that package."
  artifacts:
    - path: packages/nemoclaw-hermes/move-manifest.json
      provides: Exact managed, portable, broker, consumer, and test dispositions.
    - path: packages/nemoclaw-hermes/build-inputs.manifest.json
      provides: Exact source owner, package destination, mode, and digest for every immutable Docker build input.
    - path: packages/nemoclaw-hermes/agent-runtime-package.yaml
      provides: Managed Hermes descriptor.
    - path: test/package-contract/managed-hermes-package-boundary.test.ts
      provides: Copied-package and exclusion proof.
  key_links:
    - from: packages/nemoclaw-hermes/move-manifest.json
      to: proposals/agent-runtime-packages/HERMES-CANDIDATES.tsv
      via: Generated dispositions plus a fresh consumer scan.
    - from: packages/nemoclaw-hermes/Dockerfile
      to: packages/nemoclaw-deepagents-code/move-manifest.json
      via: The Phase 4 decision keeps support absent or selects the exact shared image-build payload.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Create the independently buildable managed Hermes package without moving portable or broker authority.

Purpose: Establish the gateway pilot on the same narrow package boundary proven by Deep Agents Code.
Output: Exact move manifest, managed package source, atomic consumer cutover, and copied-package tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-managed-hermes-pilot/05-CONTEXT.md
@.planning/phases/04-deep-agents-code-pilot/04-04-SUMMARY.md
@proposals/agent-runtime-packages/HERMES-CANDIDATES.tsv
@agents/hermes/manifest.yaml
@agents/hermes/Dockerfile
@agents/hermes/Dockerfile.base
@agents/hermes/config/build-env.ts
@agents/hermes/host/tool-gateway-broker.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the managed-only move manifest</name>
  <files>packages/nemoclaw-hermes/move-manifest.json, packages/nemoclaw-hermes/build-inputs.manifest.json, packages/nemoclaw-hermes/build-inputs/</files>
  <read_first>proposals/agent-runtime-packages/HERMES-CANDIDATES.tsv, agents/hermes/manifest.yaml, agents/hermes/Dockerfile, agents/hermes/Dockerfile.base, agents/hermes/config/build-env.ts, agents/hermes/host/tool-gateway-broker.ts, src/lib/onboard/experimental/hermes-portable-contract.ts</read_first>
  <action>Generate one exact manifest from the audited ledger and a fresh import, Docker COPY, script, workflow, and test scan. Mark current managed-image runtime rows `move` or `split`. Mark portable and host broker rows `keep` with exact owners and consumers. Classify every Docker COPY source as a package-owned moved path, an exact Phase 4 runtime-support path when present, or an immutable package build-input snapshot. Record each snapshot's authoritative core source, package destination under `build-inputs/`, mode, digest, owner, and tests in `build-inputs.manifest.json`; copy no credential, effective policy, executable host hook, or mutable state, and reject an unclassified Docker input. Record any in-image process-wrapper or supervision behavior as compatibility debt with the current caller and an OpenShell 0.0.106 parity removal gate. Do not hide excluded work behind a package directory entry or broad glob.</action>
  <verify><automated>node -e "const m=require('./packages/nemoclaw-hermes/move-manifest.json');const b=require('./packages/nemoclaw-hermes/build-inputs.manifest.json');if(!m.entries?.length)throw new Error('empty Hermes move manifest');for(const e of m.entries)if(!e.source||!e.disposition||!e.owner)throw new Error('incomplete row');if(!Array.isArray(b.entries))throw new Error('build-input entries missing');for(const e of b.entries)for(const k of ['source','destination','mode','digest','owner'])if(!e[k])throw new Error('incomplete build input')"</automated></verify>
  <acceptance_criteria>
    - Every managed candidate has an exact disposition and destination when moved.
    - Every portable and broker row remains assigned to its current owner.
    - Current consumers and test owners are listed.
    - Compatibility supervision debt has a named removal gate.
    - Every Docker COPY resolves inside the package or an exact declared packed dependency; each copied core input retains its authoritative core source and digest.
  </acceptance_criteria>
  <done>The managed Hermes extraction boundary is complete and reviewable before files move.</done>
</task>

<task type="auto">
  <name>Task 2: Move managed source and cut over exact consumers</name>
  <files>packages/nemoclaw-hermes/package.json, packages/nemoclaw-hermes/agent-runtime-package.yaml, packages/nemoclaw-hermes/Dockerfile, packages/nemoclaw-hermes/Dockerfile.base, packages/nemoclaw-hermes/runtime/config/generate.ts, packages/nemoclaw-hermes/runtime/config/build-env.ts, src/lib/agent/defs.ts, src/lib/agent/base-image.ts, .github/workflows/base-image.yaml, .github/workflows/managed-images.yaml, test/package-contract/managed-hermes-path-cutover.test.ts, package.json, package-lock.json</files>
  <read_first>packages/nemoclaw-hermes/move-manifest.json, agents/hermes/config/generate.ts, agents/hermes/config/build-env.ts, src/lib/agent/defs.ts, src/lib/agent/base-image.ts, .github/workflows/base-image.yaml, .github/workflows/managed-images.yaml</read_first>
  <action>Move exact managed rows and update only their manifest-listed consumers. Keep portable Hermes, broker selection/lifecycle, `bin/nemohermes.js`, core provider/credential handling, and OpenShell authority in place. Read the Phase 4 `runtimeSupport` decision. When it records `required: true`, make the package Dockerfiles consume the existing exact payload without adding Hermes-only content. When it records `required: false`, require the support workspace and every Docker dependency on it to remain absent. Resolve managed Hermes through the Phase 3 catalogue and preserve public image names, menu text, alias behavior, dependency pins, and managed-image cohort. Add an old-path scanner for moved rows.</action>
  <verify><automated>npx vitest run --project package-contract test/package-contract/managed-hermes-path-cutover.test.ts --coverage=false &amp;&amp; npm run typecheck:cli</automated></verify>
  <acceptance_criteria>
    - Managed Hermes builds from the package root.
    - Portable and broker behavior still use their existing owners.
    - No active consumer reads an old path for a moved row.
    - Public selection and exact image identity are unchanged.
  </acceptance_criteria>
  <done>Managed Hermes has one active source root without broadening the package scope.</done>
</task>

<task type="auto">
  <name>Task 3: Prove package independence and exclusions</name>
  <files>packages/nemoclaw-hermes/tests/package-boundary.test.ts, test/package-contract/managed-hermes-package-boundary.test.ts, package.json, package-lock.json</files>
  <read_first>test/package-contract/helpers/package-fixture.ts, packages/nemoclaw-hermes/package.json, packages/nemoclaw-hermes/move-manifest.json, packages/nemoclaw-deepagents-code/move-manifest.json</read_first>
  <action>Create a test-owned workspace envelope in a temporary directory containing the root workspace manifest and lock, the managed Hermes and contract sources, the package manifests needed to preserve exact workspace links, and no sibling runtime, portable Hermes, or broker source. Run the package build and detailed tests there. Separately pack declared dependencies, including runtime-support only when the Phase 4 move manifest records `required: true`, and install those exact archives into an empty consumer outside the checkout. Reject checkout absolute paths, parent traversal, sibling source reads, undeclared files, nested workspace locks, portable or broker imports, mutable dependency ranges, build-input digest drift, an unrecorded support package, a missing required support package, and shared-payload drift. The root lock belongs to the test-owned build envelope and is not part of the canonical package tree. Installation records exact identity only and cannot author checked-in release status. Add the package suite to the root aggregate.</action>
  <verify><automated>npx vitest run --project package-contract test/package-contract/managed-hermes-package-boundary.test.ts test/package-contract/managed-hermes-path-cutover.test.ts --coverage=false &amp;&amp; npm test</automated></verify>
  <acceptance_criteria>
    - The copied package builds without the source checkout.
    - No portable or broker implementation crosses the package boundary.
    - Runtime-support is absent for both pilots or its exact identity matches the Deep Agents Code consumer.
    - Package data cannot set release status.
  </acceptance_criteria>
  <done>The managed package is independently buildable and its exclusions are enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Untrusted input | Control |
|---|---|---|
| Managed move | Mixed Hermes source tree | Exact managed/portable/broker dispositions |
| Package archive | Runtime-owned code | Copied-root build and exclusion scanner |
| Optional shared payload | Image-build files | Exact Phase 4 absent/present decision and, when present, manifest and digest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| HER-T01 | Elevation of privilege | Broker move | Avoid | Keep host broker and lifecycle with current core owner |
| HER-T02 | Tampering | Package contents | Mitigate | Exact move manifest and copied-package test |
| HER-T03 | Spoofing | Release status | Avoid | Core checked-in release set remains sole authority |
</threat_model>

<verification>
- [ ] Move-manifest, path-cutover, and copied-package checks pass.
- [ ] Portable and broker tests remain with current owners.
- [ ] Deterministic aggregate and CLI typecheck pass.
- [ ] Compatibility debt is explicit where OpenShell parity is not proven.
</verification>

<success_criteria>
- Managed Hermes has one package root.
- Portable and broker authority do not move.
- The package builds outside the checkout from exact inputs.
</success_criteria>

<output>
After completion, create `.planning/phases/05-managed-hermes-pilot/05-01-SUMMARY.md`.
</output>
