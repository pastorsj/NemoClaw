---
phase: 05-managed-hermes-pilot
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - packages/nemoclaw-hermes/controller/runtime-control.ts
  - packages/nemoclaw-hermes/controller/native-config.ts
  - packages/nemoclaw-hermes/controller/native-claim.ts
  - packages/nemoclaw-hermes/tests/controller-contract.test.ts
  - packages/nemoclaw-hermes/Dockerfile
  - packages/nemoclaw-hermes/agent-runtime-package.yaml
  - src/lib/onboard/managed-startup/image-runtime.ts
  - src/lib/onboard/managed-startup-image-runtime.test.ts
  - src/lib/actions/sandbox/rebuild-hermes-post-restore.ts
  - src/lib/actions/sandbox/rebuild-hermes-post-restore.test.ts
autonomous: true
requirements: [HER-01]
must_haves:
  truths:
    - "Hermes-native configuration is package-owned and package-local."
    - "Every helper invocation is mutation-capable and returns only an executor claim."
    - "Health observations use descriptor-declared probes executed by core/OpenShell without package code."
  artifacts:
    - path: packages/nemoclaw-hermes/controller/runtime-control.ts
      provides: Fixed helper entrypoint over Phase 3 framing.
    - path: packages/nemoclaw-hermes/tests/controller-contract.test.ts
      provides: Closed request, claim, and authority tests.
  key_links:
    - from: src/lib/agent-runtime-packages/controller-client.ts
      to: packages/nemoclaw-hermes/controller/runtime-control.ts
      via: Phase 3 framing followed by a package-local request.
    - from: packages/nemoclaw-hermes/agent-runtime-package.yaml
      to: src/lib/agent/definition-types.ts
      via: Existing `health_probe` declaration.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Put Hermes-native configuration and reconciliation behind the fixed package helper.

Purpose: Prove gateway-native translation without transferring lifecycle or process authority.
Output: Package-local handlers, image installation, reduced core dispatch, and current rebuild integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-managed-hermes-pilot/05-CONTEXT.md
@.planning/phases/05-managed-hermes-pilot/05-01-SUMMARY.md
@packages/nemoclaw-hermes/runtime/config/generate.ts
@packages/nemoclaw-hermes/runtime/hermes-wrapper.py
@src/lib/onboard/managed-startup/image-runtime.ts
@src/lib/actions/sandbox/rebuild-hermes-post-restore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement bounded Hermes-native requests</name>
  <files>packages/nemoclaw-hermes/controller/runtime-control.ts, packages/nemoclaw-hermes/controller/native-config.ts, packages/nemoclaw-hermes/controller/native-claim.ts, packages/nemoclaw-hermes/tests/controller-contract.test.ts, packages/nemoclaw-hermes/agent-runtime-package.yaml</files>
  <read_first>packages/agent-runtime-contract/schemas/runtime-control-v1.schema.json, packages/nemoclaw-hermes/runtime/config/generate.ts, packages/nemoclaw-hermes/runtime/hermes-wrapper.py, packages/nemoclaw-hermes/agent-runtime-package.yaml, src/lib/agent/definition-types.ts</read_first>
  <action>After Phase 3 negotiation, accept closed package-local requests for current managed Hermes configuration and reconciliation. Invoke existing native modules with structured data and return a bounded executor claim. Treat every helper invocation, including inspection or diagnostics, as mutation-capable; helper output cannot commit product or registry state. Use the existing descriptor `health_probe` through core/OpenShell for a product health observation without executing package code. Reject command fields, unknown properties, credential values, identity mismatch, and stale request identity. Do not add lifecycle, supervisor, host callback, or OpenShell operations.</action>
  <verify><automated>npm --prefix packages/nemoclaw-hermes test -- controller-contract</automated></verify>
  <acceptance_criteria>
    - Package-local requests are closed, bounded, and not exported as V1 operations yet.
    - Every response is an executor claim.
    - Product health uses the existing descriptor probe.
    - Helper code cannot call OpenShell or supervise Hermes.
  </acceptance_criteria>
  <done>Hermes-native translation is package-owned without broadening the shared contract.</done>
</task>

<task type="auto">
  <name>Task 2: Install the helper and preserve current lifecycle callers</name>
  <files>packages/nemoclaw-hermes/Dockerfile, packages/nemoclaw-hermes/agent-runtime-package.yaml, src/lib/onboard/managed-startup/image-runtime.ts, src/lib/onboard/managed-startup-image-runtime.test.ts, src/lib/actions/sandbox/rebuild-hermes-post-restore.ts, src/lib/actions/sandbox/rebuild-hermes-post-restore.test.ts</files>
  <read_first>packages/nemoclaw-hermes/Dockerfile, src/lib/onboard/managed-startup/image-runtime.ts, src/lib/actions/sandbox/rebuild-pipeline.ts, src/lib/actions/sandbox/rebuild-post-restore-phase.ts, src/lib/actions/sandbox/rebuild-hermes-post-restore.ts</read_first>
  <action>Install the fixed helper under the existing non-root runtime identity and replace only Hermes-native configuration dispatch in core. Keep process start/restart and sandbox operations on the current OpenShell path. Keep Hermes-specific post-restore orchestration inside the existing core rebuild phase while calling the package only for native reconciliation. Retain any current in-image wrapper or supervision compatibility behavior until an explicit OpenShell 0.0.106 parity test proves it removable; record the debt in the move manifest.</action>
  <verify><automated>npm --prefix packages/nemoclaw-hermes test &amp;&amp; npx vitest run --project cli src/lib/onboard/managed-startup-image-runtime.test.ts src/lib/actions/sandbox/rebuild-hermes-post-restore.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - The image contains the fixed helper with current non-root ownership.
    - Core no longer renders Hermes-native configuration.
    - Current rebuild and OpenShell process boundaries remain.
    - Any in-image supervision debt has an exact caller and removal test.
  </acceptance_criteria>
  <done>Core invokes package-native translation while current OpenShell lifecycle ownership remains intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Untrusted input | Control |
|---|---|---|
| Helper request | Normalized Hermes intent | Closed parser, bounds, and exact identity |
| Helper result | Package-produced claim | Cannot commit product state |
| Product health | Sandbox service state | Descriptor probe executed by core/OpenShell |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| HER-T04 | Spoofing | Helper health | Mitigate | Product health comes from descriptor probe |
| HER-T05 | Elevation of privilege | Helper | Avoid | No OpenShell, lifecycle, or supervisor operation |
| HER-T06 | Denial of service | Premature wrapper removal | Mitigate | Retain until named OpenShell parity gate passes |
</threat_model>

<verification>
- [ ] Package controller and managed-startup tests pass.
- [ ] Hermes post-restore still uses the current rebuild pipeline.
- [ ] Product health does not trust helper diagnostics.
- [ ] Compatibility debt is explicit if parity is not proven.
</verification>

<success_criteria>
- Hermes-native configuration is package-owned.
- Helper output remains an executor claim.
- OpenShell retains lifecycle and canonical process authority.
</success_criteria>

<output>
After completion, create `.planning/phases/05-managed-hermes-pilot/05-02-SUMMARY.md`.
</output>
