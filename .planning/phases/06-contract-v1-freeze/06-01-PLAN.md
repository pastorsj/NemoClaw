---
phase: 06-contract-v1-freeze
plan: 01
type: execute
wave: 1
depends_on: ["05-05"]
files_modified:
  - packages/agent-runtime-contract/PILOT-CONTRACT-MATRIX.md
  - packages/agent-runtime-contract/tests/pilot-contract-matrix.test.ts
  - packages/nemoclaw-deepagents-code/controller/native-request.ts
  - packages/nemoclaw-deepagents-code/controller/native-state.ts
  - packages/nemoclaw-hermes/controller/native-config.ts
  - packages/nemoclaw-hermes/controller/native-mcp.ts
  - packages/nemoclaw-hermes/controller/runtime-control.ts
  - src/lib/agent-runtime-packages/controller-client.ts
  - src/lib/agent-runtime-packages/controller-client.test.ts
autonomous: true
requirements: [CTL-02, CTL-04, CORE-03]
must_haves:
  truths:
    - "Every shared V1 operation and field has positive deterministic evidence from both pilots."
    - "The only candidate shared operations are `reconcile-native` and `prepare-native-state`, each invoked inside an existing core transaction."
    - "Single-runtime payloads, health/status observation, messaging render data, and lifecycle behavior remain outside V1."
  artifacts:
    - path: packages/agent-runtime-contract/PILOT-CONTRACT-MATRIX.md
      provides: Exact two-consumer field, operation, caller, and test evidence.
    - path: packages/agent-runtime-contract/tests/pilot-contract-matrix.test.ts
      provides: Executable proof that each candidate row has both consumers.
  key_links:
    - from: packages/nemoclaw-deepagents-code/controller/native-request.ts
      to: packages/nemoclaw-hermes/controller/native-config.ts
      via: Positive `reconcile-native` consumers.
    - from: packages/nemoclaw-deepagents-code/controller/native-state.ts
      to: packages/nemoclaw-hermes/controller/runtime-control.ts
      via: Positive `prepare-native-state` consumers.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Reduce the pilot protocols to their exact two-consumer intersection.

Purpose: Prevent pilot-specific behavior from becoming a general extension surface.
Output: Reviewed executable matrix, aligned pilot request shapes, and an exact V1 candidate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-contract-v1-freeze/06-CONTEXT.md
@.planning/phases/05-managed-hermes-pilot/05-05-SUMMARY.md
@.planning/phases/04-deep-agents-code-pilot/04-04-SUMMARY.md
@packages/nemoclaw-deepagents-code/controller/native-request.ts
@packages/nemoclaw-deepagents-code/controller/native-state.ts
@packages/nemoclaw-hermes/controller/runtime-control.ts
@src/lib/agent-runtime-packages/controller-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the executable two-consumer matrix</name>
  <files>packages/agent-runtime-contract/PILOT-CONTRACT-MATRIX.md, packages/agent-runtime-contract/tests/pilot-contract-matrix.test.ts</files>
  <read_first>packages/nemoclaw-deepagents-code/controller/native-request.ts, packages/nemoclaw-deepagents-code/controller/native-state.ts, packages/nemoclaw-hermes/controller/native-config.ts, packages/nemoclaw-hermes/controller/native-mcp.ts, packages/nemoclaw-hermes/controller/runtime-control.ts, src/lib/agent-runtime-packages/controller-client.ts</read_first>
  <action>Apply CTL-02 and CTL-04. Inventory every pilot package-local operation and field. For each row name both concrete handlers, both current core transaction callers, and both deterministic tests, or mark it package-internal/core-owned/remove. Admit only `reconcile-native` and `prepare-native-state` as operation candidates. Treat every helper invocation as mutation-capable and every helper result as an executor claim. Classify descriptor health/readiness probes, trusted probe execution and status interpretation, messaging render payloads, terminal/gateway specifics, MCP dialects, dashboard, cron, state layout, process behavior, and sandbox lifecycle outside V1. A Deep Agents Code messaging rejection is not a positive messaging consumer.</action>
  <verify><automated>npm --prefix packages/agent-runtime-contract test -- pilot-contract-matrix</automated></verify>
  <acceptance_criteria>
    - Every admitted row names two positive handlers, callers, and tests.
    - No rejection path is counted as a positive consumer.
    - No helper observation, health, status, lifecycle, or process operation is admitted.
    - Every excluded row has a package-internal, core-owned, or remove disposition.
  </acceptance_criteria>
  <done>The V1 candidate has an exact, executable two-consumer evidence basis.</done>
</task>

<task type="auto">
  <name>Task 2: Align both pilots to the reduced candidate</name>
  <files>packages/nemoclaw-deepagents-code/controller/native-request.ts, packages/nemoclaw-deepagents-code/controller/native-state.ts, packages/nemoclaw-hermes/controller/native-config.ts, packages/nemoclaw-hermes/controller/native-mcp.ts, packages/nemoclaw-hermes/controller/runtime-control.ts, src/lib/agent-runtime-packages/controller-client.ts, src/lib/agent-runtime-packages/controller-client.test.ts</files>
  <read_first>packages/agent-runtime-contract/PILOT-CONTRACT-MATRIX.md, src/lib/agent-runtime-packages/controller-client.ts, packages/nemoclaw-deepagents-code/controller/runtime-control.ts, packages/nemoclaw-hermes/controller/runtime-control.ts</read_first>
  <action>Rename and reduce shared package entrypoints to the two admitted operations and exact shared fields. Keep Deep Agents Code-only and Hermes-only dispatch behind package-local types. Invoke `reconcile-native` only inside the current configure/MCP/messaging transactions that already own the effect. Invoke `prepare-native-state` only inside the current rebuild preparation transaction; it may prepare runtime-native files but cannot copy or mount durable state, publish registry authority, or control a process. Keep all helper results as claims. Do not add a lifecycle sequence, state machine, journal, generic messaging operation, fallback transport, or OpenShell call.</action>
  <verify><automated>npm --prefix packages/nemoclaw-deepagents-code test &amp;&amp; npm --prefix packages/nemoclaw-hermes test &amp;&amp; npx vitest run --project cli src/lib/agent-runtime-packages/controller-client.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - Both packages implement the same two operation names and shared field shapes.
    - Single-runtime payloads remain package-internal.
    - Each operation is called only within an existing core transaction.
    - No package helper can execute OpenShell, lifecycle, supervision, or product observation.
  </acceptance_criteria>
  <done>Both pilots implement the smallest common mutation-capable helper surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Untrusted input | Control |
|---|---|---|
| Contract candidate | Pilot-specific convenience | Two positive consumers, callers, and tests per row |
| Helper operation | Package code | Mutation-capable default inside an existing transaction |
| Package result | Executor claim | Cannot become a product observation or commit state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| CTL-T01 | Elevation of privilege | Candidate operation | Avoid | Only reconcile/prepare inside current transactions |
| CTL-T02 | Spoofing | Helper observation | Avoid | No helper observation operation or authoritative health field |
| CTL-T03 | Tampering | Single-runtime field | Mitigate | Executable two-consumer matrix rejects it from V1 |
</threat_model>

<verification>
- [ ] Two-consumer matrix test passes.
- [ ] Both package suites and controller client tests pass.
- [ ] V1 candidate contains exactly two operations and no helper observation.
- [ ] No new lifecycle, registry, journal, migration, or E2E registry exists.
</verification>

<success_criteria>
- Only shared pilot behavior remains in the V1 candidate.
- Package-internal behavior stays package-internal.
- Both admitted operations remain inside existing transactions.
</success_criteria>

<output>
After completion, create `.planning/phases/06-contract-v1-freeze/06-01-SUMMARY.md`.
</output>
