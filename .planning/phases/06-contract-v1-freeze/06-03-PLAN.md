---
phase: 06-contract-v1-freeze
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - packages/agent-runtime-contract/fixtures/hostile/manifest.json
  - packages/agent-runtime-contract/tests/hostile-controller.test.ts
  - packages/agent-runtime-contract/tests/transaction-boundary.test.ts
  - scripts/checks/agent-runtime-package-boundary.mts
  - test/package-contract/agent-runtime-core-boundary.test.ts
  - src/lib/agent-runtime-packages/controller-client.test.ts
  - src/lib/actions/sandbox/rebuild-dcode-orchestrator.test.ts
  - src/lib/actions/sandbox/rebuild-hermes-post-restore.test.ts
  - src/lib/actions/sandbox/rebuild-recreate-journal.test.ts
  - src/lib/onboard/sandbox-recreate-transaction.test.ts
  - package.json
autonomous: true
requirements: [TEST-03, CORE-01, CORE-03]
must_haves:
  truths:
    - "Hostile packages and controller output fail closed with bounded redacted errors."
    - "Runtime-neutral core has no Deep Agents Code or Hermes name-based executable dispatch."
    - "Fault coverage runs through current product transactions, not a conformance-owned lifecycle."
  artifacts:
    - path: packages/agent-runtime-contract/fixtures/hostile/manifest.json
      provides: Exact hostile fixture inventory and expected failure code.
    - path: scripts/checks/agent-runtime-package-boundary.mts
      provides: Runtime-neutral core boundary check with exact compatibility-debt exceptions.
    - path: packages/agent-runtime-contract/tests/transaction-boundary.test.ts
      provides: Shared operation failure tests over current transaction adapters.
  key_links:
    - from: src/lib/actions/sandbox/rebuild-recreate-journal.ts
      to: src/lib/onboard/sandbox-recreate-transaction.ts
      via: Current production recreate transaction.
    - from: packages/agent-runtime-contract/tests/transaction-boundary.test.ts
      to: src/lib/actions/sandbox/rebuild-dcode-orchestrator.test.ts
      via: Deep Agents Code current-caller fault fixtures.
    - from: packages/agent-runtime-contract/tests/transaction-boundary.test.ts
      to: src/lib/actions/sandbox/rebuild-hermes-post-restore.test.ts
      via: Hermes current-caller fault fixtures.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Enforce Contract V1 with hostile fixtures, core-boundary checks, and current-transaction fault tests.

Purpose: Make the freeze mechanically enforceable before OpenClaw extraction.
Output: Hostile conformance, runtime-neutral core guard, and two-pilot failure evidence over existing callers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-contract-v1-freeze/06-CONTEXT.md
@.planning/phases/06-contract-v1-freeze/06-02-SUMMARY.md
@packages/agent-runtime-contract/schemas/runtime-control-v1.schema.json
@packages/agent-runtime-contract/schemas/operation-receipt-v1.schema.json
@src/lib/actions/sandbox/rebuild-pipeline.ts
@src/lib/actions/sandbox/rebuild-recreate-journal.ts
@src/lib/onboard/sandbox-recreate-transaction.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hostile contract and output fixtures</name>
  <files>packages/agent-runtime-contract/fixtures/hostile/manifest.json, packages/agent-runtime-contract/tests/hostile-controller.test.ts, src/lib/agent-runtime-packages/controller-client.test.ts</files>
  <read_first>packages/agent-runtime-contract/schemas/runtime-control-v1.schema.json, packages/agent-runtime-contract/schemas/operation-receipt-v1.schema.json, packages/agent-runtime-contract/src/conformance.ts, src/lib/agent-runtime-packages/controller-client.ts, src/lib/agent-runtime-packages/controller-output.ts</read_first>
  <action>Create exact manifest rows and fixture files for hostile archive/descriptor paths, unknown operation, extra field, command or callback, lifecycle/process field, read-only helper claim, helper-authored observation, spoofed producer, identity mismatch, replay, extra frame, oversize/binary output, timeout, credential-shaped field, raw diagnostic, and package-authored release status. Each row names its exact expected stable error. Verify bounded redacted errors and no retry through another executable or transport. Do not add a simulated runtime or lifecycle.</action>
  <verify><automated>npm --prefix packages/agent-runtime-contract test -- hostile-controller &amp;&amp; npx vitest run --project cli src/lib/agent-runtime-packages/controller-client.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - Every hostile fixture has one manifest row and stable failure code.
    - Credential values, stack traces, and raw helper diagnostics are absent from output.
    - Helper-authored observation and read-only classifications fail closed.
    - No fallback transport, executable, package, or runtime is attempted.
  </acceptance_criteria>
  <done>Hostile package and controller behavior fails closed with bounded evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce the runtime-neutral core boundary</name>
  <files>scripts/checks/agent-runtime-package-boundary.mts, test/package-contract/agent-runtime-core-boundary.test.ts, package.json</files>
  <read_first>packages/nemoclaw-deepagents-code/move-manifest.json, packages/nemoclaw-hermes/move-manifest.json, src/lib/agent-runtime-packages/controller-client.ts, src/lib/agent-runtime-packages/release-set.ts, package.json</read_first>
  <action>Add a repository check that rejects Deep Agents Code/Hermes package imports, native config construction, command dispatch, helper lifecycle/process operations, a second OpenShell client, a second package catalogue, a mutable release-status mechanism, migration API, E2E registry, or lifecycle state machine in runtime-neutral core. Allow names only in checked-in release data, compatibility presentation, tests, and exact current descendant-wrapper compatibility debt recorded by the move manifests. A shared abstraction must name both current consumers and the exact old path it replaces.</action>
  <verify><automated>npm run checks:repository &amp;&amp; npx vitest run --project package-contract test/package-contract/agent-runtime-core-boundary.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - Runtime-neutral core contains no Deep Agents Code/Hermes executable dispatch.
    - Exact compatibility debt is narrow, tested, and cannot grow.
    - One package catalogue, OpenShell client, lifecycle, and E2E registry remain.
    - No mutable status mechanism or migration API exists.
  </acceptance_criteria>
  <done>The package/core authority boundary is enforced by repository checks.</done>
</task>

<task type="auto">
  <name>Task 3: Exercise failures through current transaction owners</name>
  <files>packages/agent-runtime-contract/tests/transaction-boundary.test.ts, src/lib/actions/sandbox/rebuild-dcode-orchestrator.test.ts, src/lib/actions/sandbox/rebuild-hermes-post-restore.test.ts, src/lib/actions/sandbox/rebuild-recreate-journal.test.ts, src/lib/onboard/sandbox-recreate-transaction.test.ts</files>
  <read_first>src/lib/actions/sandbox/rebuild-pipeline.ts, src/lib/actions/sandbox/rebuild-recreate-journal.ts, src/lib/onboard/sandbox-recreate-transaction.ts, src/lib/actions/sandbox/rebuild-dcode-orchestrator.ts, src/lib/actions/sandbox/rebuild-hermes-post-restore.ts</read_first>
  <action>Exercise `reconcile-native` and `prepare-native-state` through the existing Deep Agents Code and Hermes callers. Inject helper timeout, false claim, identity mismatch, interruption before destructive mutation, failed descriptor probe, failed OpenShell readiness, failed restore, and rollback failure at current seams. Assert claims never satisfy observations, registry publication waits for trusted current facts, the existing recreate journal/transaction remains authoritative, and release status does not change. Use test doubles only at the existing OpenShell adapter; do not implement another lifecycle sequence.</action>
  <verify><automated>npx vitest run --project cli src/lib/actions/sandbox/rebuild-dcode-orchestrator.test.ts src/lib/actions/sandbox/rebuild-hermes-post-restore.test.ts src/lib/actions/sandbox/rebuild-recreate-journal.test.ts src/lib/onboard/sandbox-recreate-transaction.test.ts --coverage=false &amp;&amp; npm test</automated></verify>
  <acceptance_criteria>
    - Both operations have failure evidence from both pilot callers.
    - Existing transaction and rollback owners remain unchanged.
    - Package claims cannot publish registry or product authority.
    - Static release status and immutable qualification evidence remain unchanged on failure.
  </acceptance_criteria>
  <done>Both pilots pass the shared hostile matrix over current production transactions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Untrusted input | Control |
|---|---|---|
| Conformance input | Archive, descriptor, and helper output | Exact fixture manifest and closed parsers |
| Runtime-neutral core | Package-specific convenience | Repository check and exact debt exceptions |
| Transaction failure | Package claim and interrupted sandbox work | Current recreate journal, transaction, and OpenShell adapter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| CTL-T08 | Information disclosure | Hostile output | Mitigate | Bounds and shared redaction |
| CTL-T09 | Spoofing | Product observation | Mitigate | Named trusted producer; helper claim rejected |
| CTL-T10 | Elevation of privilege | Parallel lifecycle | Avoid | Test only current pipeline/journal/transaction |
| CTL-T11 | Tampering | Static release status | Avoid | Runtime failure cannot edit checked-in release data |
</threat_model>

<verification>
- [ ] Hostile conformance and controller tests pass.
- [ ] Runtime-neutral core boundary and repository checks pass.
- [ ] Current Deep Agents Code, Hermes, recreate-journal, and recreate-transaction fault tests pass.
- [ ] Complete deterministic aggregate passes.
</verification>

<success_criteria>
- V1 rejects hostile packages and controller output.
- Runtime-neutral core contains no pilot executable dispatch.
- Both pilots prove failures and rollback through current owners only.
</success_criteria>

<output>
After completion, create `.planning/phases/06-contract-v1-freeze/06-03-SUMMARY.md`.
</output>
