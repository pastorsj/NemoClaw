---
phase: 06-contract-v1-freeze
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - packages/agent-runtime-contract/schemas/runtime-control-v1.schema.json
  - packages/agent-runtime-contract/schemas/operation-receipt-v1.schema.json
  - packages/agent-runtime-contract/src/types.ts
  - packages/agent-runtime-contract/src/validate.ts
  - packages/agent-runtime-contract/compatibility.json
  - packages/agent-runtime-contract/tests/schema-compatibility.test.ts
  - packages/agent-runtime-contract/tests/effect-and-evidence.test.ts
  - src/lib/agent-runtime-packages/controller-output.ts
  - src/lib/agent-runtime-packages/controller-output.test.ts
autonomous: true
requirements: [CTL-04, SEC-04, CTL-05]
must_haves:
  truths:
    - "Contract V1 contains only `reconcile-native` and `prepare-native-state` plus their two-consumer fields."
    - "SEC-04 freezes every helper invocation as mutation-capable; V1 contains no read-only helper classification."
    - "CTL-05 freezes separate package claims and trusted core/OpenShell observations with producer and limitation fields."
  artifacts:
    - path: packages/agent-runtime-contract/schemas/runtime-control-v1.schema.json
      provides: Closed two-operation helper contract.
    - path: packages/agent-runtime-contract/schemas/operation-receipt-v1.schema.json
      provides: Core-owned mutation claim and trusted observation receipt.
    - path: packages/agent-runtime-contract/compatibility.json
      provides: Exact schema and protocol compatibility range.
  key_links:
    - from: packages/agent-runtime-contract/PILOT-CONTRACT-MATRIX.md
      to: packages/agent-runtime-contract/schemas/runtime-control-v1.schema.json
      via: Every operation and field traces to two positive consumers.
    - from: src/lib/agent-runtime-packages/controller-output.ts
      to: packages/agent-runtime-contract/schemas/operation-receipt-v1.schema.json
      via: Package can populate only the executor-claim portion.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Freeze the reduced operation, mutation, evidence, error, and compatibility contract as V1.

Purpose: Give OpenClaw a stable internal contract without adding generic lifecycle or observation APIs.
Output: Closed schemas, generated readers, compatibility metadata, and SEC-04/CTL-05 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-contract-v1-freeze/06-CONTEXT.md
@.planning/phases/06-contract-v1-freeze/06-01-SUMMARY.md
@packages/agent-runtime-contract/PILOT-CONTRACT-MATRIX.md
@packages/agent-runtime-contract/schemas/runtime-control-v1.schema.json
@src/lib/agent-runtime-packages/controller-output.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the exact shared operations and compatibility rules</name>
  <files>packages/agent-runtime-contract/schemas/runtime-control-v1.schema.json, packages/agent-runtime-contract/src/types.ts, packages/agent-runtime-contract/src/validate.ts, packages/agent-runtime-contract/compatibility.json, packages/agent-runtime-contract/tests/schema-compatibility.test.ts</files>
  <read_first>packages/agent-runtime-contract/PILOT-CONTRACT-MATRIX.md, packages/agent-runtime-contract/schemas/runtime-control-v1.schema.json, packages/agent-runtime-contract/src/types.ts, packages/agent-runtime-contract/src/validate.ts</read_first>
  <action>Freeze `reconcile-native` and `prepare-native-state` with only the fields marked shared in the matrix. Preserve Phase 3 framing, identity binding, size bounds, deadlines, cancellation, and negotiation. Add closed generic error codes only where both pilots use them. Generate readers from the schema and publish exact compatible schema/protocol ranges. Reject unknown operations, extra properties, native runtime objects, commands, callbacks, lifecycle/process fields, helper observation/health/status fields, credential values, package-authored release status, and unknown major versions. Do not add a portable template or example lifecycle.</action>
  <verify><automated>npm --prefix packages/agent-runtime-contract test -- schema-compatibility</automated></verify>
  <acceptance_criteria>
    - V1 contains exactly the two matrix-backed operations.
    - Every request/response field traces to two positive pilot uses.
    - Generated readers reject native, lifecycle, observation, credential, and release-status fields.
    - Compatibility metadata has explicit supported ranges and no mutable service.
  </acceptance_criteria>
  <done>The shared helper operation surface is frozen, bounded, and fail-closed.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze SEC-04 and CTL-05 receipt semantics</name>
  <files>packages/agent-runtime-contract/schemas/operation-receipt-v1.schema.json, packages/agent-runtime-contract/src/types.ts, packages/agent-runtime-contract/src/validate.ts, packages/agent-runtime-contract/tests/effect-and-evidence.test.ts, src/lib/agent-runtime-packages/controller-output.ts, src/lib/agent-runtime-packages/controller-output.test.ts</files>
  <read_first>packages/agent-runtime-contract/PILOT-CONTRACT-MATRIX.md, src/lib/agent-runtime-packages/controller-output.ts, src/lib/agent-runtime-packages/receipt.ts, src/lib/actions/sandbox/rebuild-recreate-journal.ts, src/lib/onboard/sandbox-recreate-transaction.ts</read_first>
  <action>Freeze one core-authored operation receipt used by both pilots. Its effect is the fixed literal `mutation-capable`; V1 has no helper read-only option. The package supplies only a bounded executor claim. Core may attach a separately produced observation from the exact descriptor probe or trusted OpenShell operation named by the pilot matrix, including producer, collection boundary, freshness, and explicit limitation. The helper cannot author or satisfy that observation, and a claim alone cannot commit product state. A detected unexpected effect fails the current transaction and records bounded incident evidence; any later `revoked` status requires an ordinary reviewed release-set change and never rewrites qualification evidence. Keep immutable installation/runtime identity receipts separate.</action>
  <verify><automated>npm --prefix packages/agent-runtime-contract test -- effect-and-evidence &amp;&amp; npx vitest run --project cli src/lib/agent-runtime-packages/controller-output.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - SEC-04: every helper receipt is unconditionally mutation-capable.
    - SEC-04: no V1 read-only helper operation or prevention/detection choice exists.
    - CTL-05: claims and trusted observations are distinct, and observations name producer and limitation.
    - Package output cannot author observations or change static release status.
  </acceptance_criteria>
  <done>Phase 6 is the roadmap-owned freeze point for shared effect and evidence semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Untrusted input | Control |
|---|---|---|
| V1 request/response | Package-controlled payload | Closed schemas and generated readers |
| Executor claim | Helper output | Fixed mutation-capable effect; cannot commit state |
| Product observation | Core/OpenShell evidence | Named producer, boundary, freshness, and limitation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| CTL-T04 | Downgrade | Version negotiation | Mitigate | Exact supported ranges and fail-closed major mismatch |
| CTL-T05 | Spoofing | Independent observation | Mitigate | Package cannot populate the core observation fields |
| CTL-T06 | Elevation of privilege | Read-only label | Avoid | V1 exposes no read-only helper classification |
| CTL-T07 | Tampering | Release status | Avoid | Static release set changes only by reviewed release |
</threat_model>

<verification>
- [ ] Schema compatibility and generated-reader tests pass.
- [ ] SEC-04 tests prove every helper operation is mutation-capable.
- [ ] CTL-05 tests reject a helper claim presented as trusted observation.
- [ ] Both pilot packages remain compatible with the frozen V1.
</verification>

<success_criteria>
- V1 contains only the two demonstrated operations.
- Shared SEC-04 and CTL-05 semantics are frozen in Phase 6.
- Release status and immutable qualification evidence remain outside package control.
</success_criteria>

<output>
After completion, create `.planning/phases/06-contract-v1-freeze/06-02-SUMMARY.md`.
</output>
