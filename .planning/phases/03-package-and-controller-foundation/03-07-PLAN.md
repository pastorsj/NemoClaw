---
phase: 03-package-and-controller-foundation
plan: 07
type: execute
wave: 6
depends_on: ["03-04", "03-06"]
files_modified:
  - proposals/agent-runtime-packages/PHASE3-QUALIFICATION.md
autonomous: false
requirements: [PKG-01, PKG-07, SEC-02, CTL-03, STATE-02]
user_setup:
  - service: NemoClaw protected E2E
    why: The foundation changes selection and package identity for every current runtime, so the accepted same-repository live E2E matrix must run against the exact supported OpenShell release.
    dashboard_config:
      - task: Provide access to the repository-owned E2E dispatch and trusted inference secrets when the execution agent encounters an authentication or authorization gate.
        location: NVIDIA/NemoClaw Actions and existing E2E secret configuration
must_haves:
  truths:
    - "D-18: OpenClaw, managed Hermes, and Deep Agents Code remain on their current trusted execution adapters while one catalogue owns their registration and selection metadata."
    - "D-19: Qualification reuses current no-messaging journeys and deterministic messaging tests; it adds no test-only runtime or live messaging dependency."
    - "D-16: OpenShell owns sandbox lifecycle and admitted processes; the three named descendant compatibility wrappers remain unchanged, bounded debt."
    - "D-05: The checked-in release set is the only support authority; every standard entry has one static supported, revoked, or superseded status and an immutable qualification-evidence reference."
    - "D-07: One atomically replaced installed-package index owns local package state and verifies every stored package-tree digest when read."
    - "D-13: Controller tests qualify bounded framing, protocol negotiation, timeouts, output handling, redaction, and deny-by-default failure without inventing a production runtime consumer."
    - "D-23: Product observations come only from trusted core and OpenShell descriptor probes, not runtime-control output."
    - "D-20: Failure and mismatch cases retain the prior exact authority without fallback."
  artifacts:
    - path: proposals/agent-runtime-packages/PHASE3-QUALIFICATION.md
      provides: Deterministic, architecture, and protected live E2E results bound to exact source and environment identities.
  key_links:
    - from: proposals/agent-runtime-packages/PHASE3-QUALIFICATION.md
      to: test/e2e/registry/definitions/baseline.ts
      via: Evidence names targets selected from the existing typed registry rather than defining another target list in code.
    - from: proposals/agent-runtime-packages/PHASE3-QUALIFICATION.md
      to: release/agent-runtime-packages.json
      via: Results identify the exact registered standard release set under test.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Qualify the package foundation against the existing three runtime integrations.

Purpose: Prove that release-set registration, package validation, exact receipts, and controller framing do not change current behavior before any runtime code moves.
Output: One exact qualification record using existing deterministic tests and existing live E2E targets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-package-and-controller-foundation/03-CONTEXT.md
@.planning/phases/03-package-and-controller-foundation/03-04-SUMMARY.md
@.planning/phases/03-package-and-controller-foundation/03-06-SUMMARY.md
@proposals/agent-runtime-packages/VALIDATION-PLAN.md
@release/agent-runtime-packages.json
@test/e2e/README.md
@test/e2e/registry/definitions/baseline.ts
@tools/e2e/workflow-plan.mts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the deterministic foundation gate</name>
  <files>proposals/agent-runtime-packages/PHASE3-QUALIFICATION.md</files>
  <read_first>.planning/phases/03-package-and-controller-foundation/03-CONTEXT.md, proposals/agent-runtime-packages/VALIDATION-PLAN.md, release/agent-runtime-packages.json, package.json, vitest.config.ts, test/package-contract/agent-runtime-release-artifacts.test.ts, test/e2e/README.md</read_first>
  <action>Run the repository checks, CLI and plugin type checks, deterministic test aggregate, package-contract project, and E2E-support project from a clean built checkout. Run the focused release-set, `tar@7.5.21` ingestion, installed-package-index, catalogue, selection, controller-framing, exact-receipt, OpenShell-adapter, compatibility-launcher, and current rebuild-path tests named by Plans 03-01 through 03-06. Create PHASE3-QUALIFICATION.md and record the exact source revision, root package artifact digest, release-set digest, standard package-tree and descriptor digests, supported OpenShell pin, command, and result for each gate. Record the static support status and immutable qualification-evidence reference for every standard release-set entry, the resolved `tar` version and lockfile identity, digest verification of the one installed-package index, and baseline and resulting architecture counts from the repository check. Do not copy raw logs, invent a second status store or test registry, or describe controller protocol fixtures as a runtime implementation.</action>
  <verify><automated>npm run checks:repository &amp;&amp; npm run typecheck:cli &amp;&amp; npm --prefix nemoclaw run typecheck &amp;&amp; npm test &amp;&amp; npm run test:package &amp;&amp; npx vitest run --project e2e-support --coverage=false</automated></verify>
  <acceptance_criteria>
    - All deterministic projects and package-contract checks pass against the exact built artifacts.
    - The three standard entries retain baseline labels, order, aliases, default, and transition-integrated execution adapters.
    - A compatible local package remains listable but cannot become a normal supported selection.
    - Architecture counts do not increase and no mutable support store, status index, status state machine, second catalogue, lifecycle, OpenShell client, migration API, or E2E registry appears.
    - The qualification record proves only framing and security behavior for the controller; `reconcile-native` and `prepare-native-state` remain deferred until Deep Agents Code and Hermes both prove each needed operation.
    - The three named descendant compatibility wrappers retain exact fingerprints and no fourth wrapper appears.
  </acceptance_criteria>
  <done>The foundation has one reproducible deterministic result bound to exact artifacts and current behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Run the accepted protected live E2E matrix</name>
  <files>proposals/agent-runtime-packages/PHASE3-QUALIFICATION.md</files>
  <read_first>proposals/agent-runtime-packages/PHASE3-QUALIFICATION.md, proposals/agent-runtime-packages/VALIDATION-PLAN.md, test/e2e/README.md, tools/e2e/workflow-plan.mts, .github/workflows/e2e.yaml</read_first>
  <action>After deterministic gates pass, use the existing trusted E2E dispatch, typed target registry, and workflow planner to run `ubuntu-repo-cloud-openclaw`, `ubuntu-repo-cloud-hermes`, and `ubuntu-repo-cloud-langchain-deepagents-code` on the same-repository branch and exact NemoClaw-supported OpenShell release. Do not create a target, target list, platform rehearsal, or source-install lane for this phase. Attempt the repository-owned dispatch before requesting help. If authentication, authorization, a trusted secret, or a human-only repository approval blocks the run, stop at a dynamic human-action checkpoint and name only that gate. Do not replace a missing driver, architecture, or registered target with narrower evidence. Record workflow URLs and exact identities rather than copying terminal traces. Any target failure, cleanup failure, identity mismatch, or missing required environment keeps Phase 4 blocked. The real Apple silicon macOS journey on Docker Desktop or Colima and the official staging Launchable run belong to the release gate, not this foundation phase.</action>
  <verify><automated>node -e "const s=require('node:fs').readFileSync('proposals/agent-runtime-packages/PHASE3-QUALIFICATION.md','utf8');for(const x of ['Protected live E2E','source revision','release-set digest','OpenShell release','driver','platform','workflow','cleanup: passed'])if(!s.toLowerCase().includes(x.toLowerCase()))throw new Error('protected gate missing '+x);if(/protected status:\s*(pending|failed|blocked)/i.test(s))throw new Error('protected gate incomplete')"</automated></verify>
  <acceptance_criteria>
    - The three named existing Ubuntu no-messaging targets pass on the trusted repository-owned path.
    - Each result binds source, release set, package, image, OpenShell, capability, driver, and platform identities.
    - No live messaging result is claimed.
    - Cleanup and failure evidence are redacted and owned by the existing target fixtures.
  </acceptance_criteria>
  <done>The accepted foundation matrix has authoritative repository-owned live E2E evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Current execution adapter to package catalogue | Registration must not silently switch a standard runtime to unqualified controller execution. |
| Deterministic evidence to protected evidence | Local checks expose defects early but cannot impersonate repository-owned qualification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-24 | Spoofing | Transition registration | mitigate | Bind all three entries to the exact release set and explicit transition execution adapter. |
| T-03-25 | Repudiation | Platform evidence | mitigate | Record exact producer, workflow, source, package, image, OpenShell, driver, platform, and cleanup identities. |
| T-03-26 | Tampering | E2E selection | mitigate | Resolve targets through the existing typed registry and workflow planner. |
</threat_model>

<verification>
- [ ] The deterministic foundation gate passes.
- [ ] The accepted protected non-messaging matrix passes through the existing E2E system.
- [ ] No test-only agent runtime implementation, mutable support store, status index, generic effect layer, second registry, or second lifecycle implementation was added.
</verification>

<success_criteria>
- Standard users retain the same three current runtime journeys after package registration.
- Deep Agents Code is the first real controller consumer in Phase 4; managed Hermes is the second in Phase 5.
</success_criteria>

<output>
Create .planning/phases/03-package-and-controller-foundation/03-07-SUMMARY.md when done.
</output>
