---
phase: 03-package-and-controller-foundation
plan: 06
type: execute
wave: 5
depends_on: ["03-02", "03-03", "03-05"]
files_modified:
  - src/lib/adapters/openshell/client.ts
  - src/lib/adapters/openshell/client.test.ts
  - src/lib/adapters/openshell/resolve.ts
  - src/lib/adapters/openshell/resolve-shared.ts
  - src/lib/adapters/openshell/resolve.test.ts
  - src/lib/adapters/openshell/restore-gateway-pairing.ts
  - src/lib/adapters/openshell/restore-gateway-pairing.test.ts
  - src/lib/adapters/openshell/hermes-portable-uninstall.ts
  - src/lib/adapters/openshell/hermes-portable-uninstall.test.ts
  - src/lib/agent-runtime-packages/receipt.ts
  - src/lib/agent-runtime-packages/receipt.test.ts
  - src/lib/state/registry/types.ts
  - src/lib/state/registry-normalization.ts
  - src/lib/state/registry-normalization.test.ts
  - src/lib/onboard/managed-image/contract.ts
  - src/lib/onboard/managed-image/contract.test.ts
  - src/lib/onboard/sandbox-registration.ts
  - src/lib/onboard/sandbox-registration.test.ts
  - src/lib/onboard/created-sandbox-finalization.ts
  - src/lib/onboard/created-sandbox-finalization.test.ts
  - src/lib/onboard/workload/rebuild.ts
  - src/lib/onboard/sandbox-workload-rebuild.test.ts
  - src/lib/actions/sandbox/rebuild-preflight-target-phase.ts
  - src/lib/actions/sandbox/rebuild-preflight-target-phase.test.ts
  - src/lib/actions/sandbox/rebuild-recreate-phase.ts
  - src/lib/actions/sandbox/rebuild-agent-runtime-receipt.test.ts
autonomous: true
requirements: [PKG-07, SEC-02, STATE-02]
user_setup: []
must_haves:
  truths:
    - "D-16: Existing NemoClaw workflows retain product and rollback decisions while only the explicitly audited OpenShell adapter findings move behind the client in Phase 3."
    - "D-16: The Deep Agents Code session wrapper and the Hermes and OpenClaw descendant loops remain three exact compatibility-debt exceptions; Phase 3 does not change process behavior."
    - "D-17: One optional receipt reference links exact standard package and current managed-image identity through the active registry and rebuild/recreate paths."
    - "D-23: Transition integrations do not create controller effects, controller evidence, or a runtime implementation."
    - "D-18: This phase does not create another lifecycle, journal, dispatcher, or test runtime."
  artifacts:
    - path: src/lib/adapters/openshell/client.ts
      provides: One transport boundary used by the explicitly migrated OpenShell adapters and future controller negotiation.
    - path: src/lib/agent-runtime-packages/receipt.ts
      provides: Closed exact package and current-runtime identity receipt without mutable support state.
    - path: src/lib/state/registry/types.ts
      provides: Optional package receipt linkage on existing sandbox state without replacing current lifecycle state.
  key_links:
    - from: src/lib/adapters/openshell/resolve.ts
      to: src/lib/adapters/openshell/client.ts
      via: Existing resolve behavior delegates transport through the one client.
    - from: src/lib/state/registry/types.ts
      to: src/lib/agent-runtime-packages/receipt.ts
      via: Existing registry rows reference a validated optional receipt rather than embedding package-owned data.
    - from: src/lib/actions/sandbox/rebuild-preflight-target-phase.ts
      to: src/lib/actions/sandbox/rebuild-recreate-phase.ts
      via: The active rebuild handoff preserves the exact optional receipt across destructive recreate.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Migrate the audited OpenShell adapter subset and add exact package receipt linkage without creating a new lifecycle.

Purpose: Establish the two foundation seams real runtime migrations need while preserving the current onboarding, rebuild, recovery, and rollback implementations.
Output: Fewer audited direct OpenShell calls and a closed optional package receipt carried by current registry, managed-image, and rebuild/recreate contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-package-and-controller-foundation/03-CONTEXT.md
@.planning/phases/03-package-and-controller-foundation/03-02-SUMMARY.md
@.planning/phases/03-package-and-controller-foundation/03-03-SUMMARY.md
@.planning/phases/03-package-and-controller-foundation/03-05-SUMMARY.md
@proposals/agent-runtime-packages/ARCHITECTURE-BASELINE.json
@src/lib/adapters/openshell/client.ts
@src/lib/state/registry/types.ts
@src/lib/onboard/sandbox-registration.ts
@src/lib/onboard/created-sandbox-finalization.ts
@src/lib/onboard/workload/rebuild.ts
@src/lib/actions/sandbox/rebuild-preflight-target-phase.ts
@src/lib/actions/sandbox/rebuild-recreate-phase.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route existing OpenShell operations through one client</name>
  <files>src/lib/adapters/openshell/client.ts, src/lib/adapters/openshell/client.test.ts, src/lib/adapters/openshell/resolve.ts, src/lib/adapters/openshell/resolve-shared.ts, src/lib/adapters/openshell/resolve.test.ts, src/lib/adapters/openshell/restore-gateway-pairing.ts, src/lib/adapters/openshell/restore-gateway-pairing.test.ts, src/lib/adapters/openshell/hermes-portable-uninstall.ts, src/lib/adapters/openshell/hermes-portable-uninstall.test.ts</files>
  <read_first>.planning/phases/03-package-and-controller-foundation/03-CONTEXT.md, proposals/agent-runtime-packages/ARCHITECTURE-BASELINE.json, src/lib/adapters/openshell/client.ts, src/lib/adapters/openshell/resolve.ts, src/lib/adapters/openshell/resolve-shared.ts, src/lib/adapters/openshell/restore-gateway-pairing.ts, src/lib/adapters/openshell/hermes-portable-uninstall.ts</read_first>
  <action>Apply D-16 and the accepted Phase 1 architecture baseline only to `ARCH-OPENSHELL-0113`, `ARCH-OPENSHELL-0115`, `ARCH-OPENSHELL-0116`, `ARCH-OPENSHELL-0117`, and `ARCH-OPENSHELL-0118`. Move the direct subprocess transport used by Hermes portable uninstall, OpenShell resolution and version probing, and restored gateway pairing behind reviewed functions in the existing client. Preserve each adapter's owner, arguments, working directory, environment, timeout, output classification, return type, and error behavior. Keep Portable Hermes outside the managed package and do not change its uninstall authority. Do not migrate any other baseline finding in this task. In particular, do not change `agents/langchain-deepagents-code/dcode-session-supervisor.py`, the descendant loop in `agents/hermes/start.sh`, or the descendant loop in `scripts/nemoclaw-start.sh`; these are the three named compatibility-debt exceptions until a later exact OpenShell pin proves matching behavior. Do not add a lifecycle service, journal, generic dispatcher, retry policy, fallback, public OpenShell API, or fourth descendant wrapper. Record the five removed findings while retaining every other exact baseline exception for Phase 10.</action>
  <verify><automated>npx vitest run --project cli src/lib/adapters/openshell/client.test.ts src/lib/adapters/openshell/resolve.test.ts src/lib/adapters/openshell/restore-gateway-pairing.test.ts src/lib/adapters/openshell/hermes-portable-uninstall.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - Each of the five migrated findings retains its exact arguments, timeout, environment, ordering, result, and error behavior.
    - Direct OpenShell call-site count outside the client decreases by only the audited findings; remaining baseline findings stay explicit.
    - The three named descendant compatibility wrappers remain unchanged, and no new wrapper appears.
    - Portable Hermes remains a separate lifecycle design but uses the shared transport seam.
    - Replacement still republishes providers and confirms final OpenShell release before creating the next sandbox.
  </acceptance_criteria>
  <done>The audited adapter subset uses the existing client with no lifecycle or behavior change; full convergence remains Phase 10 work.</done>
</task>

<task type="auto">
  <name>Task 2: Define the exact package receipt</name>
  <files>src/lib/agent-runtime-packages/receipt.ts, src/lib/agent-runtime-packages/receipt.test.ts</files>
  <read_first>.planning/phases/03-package-and-controller-foundation/03-CONTEXT.md, packages/agent-runtime-contract/schemas/agent-runtime-package-v1.schema.json, packages/agent-runtime-contract/schemas/runtime-control-v1.schema.json, src/lib/agent-runtime-packages/release-set.ts, src/lib/state/registry/types.ts, src/lib/onboard/managed-image/contract.ts</read_first>
  <action>Apply D-17 and D-23. Define one closed `AgentRuntimeReceiptV1` that binds the installed package and descriptor digests, exact release-set digest and entry ID, exact selected managed-image index, platform manifest and image-configuration identities, observed OpenShell release, capability cohort, driver, platform, and negotiated controller protocol when one exists. Do not copy support status into the receipt; callers read current support from the checked-in release set. Current-integration receipts omit controller protocol. Reject credentials, unknown fields, package-defined blobs, mutable image references, mismatched package/agent/image identities, and controller fields on a current-integration receipt. The receipt has no support decision, generation, evidence provenance, effect class, intent, resource journal, or state lineage.</action>
  <verify><automated>npx vitest run --project cli src/lib/agent-runtime-packages/receipt.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - Receipt fields are closed, exact, credential-free, and canonically digestible.
    - Current-integration receipts cannot claim controller negotiation or native behavior.
    - Current support status comes only from the checked-in release set.
  </acceptance_criteria>
  <done>The package foundation has one exact identity receipt without mutable support, effect, evidence, or lifecycle state.</done>
</task>

<task type="auto">
  <name>Task 3: Link optional receipts through current registration and rebuild/recreate</name>
  <files>src/lib/state/registry/types.ts, src/lib/state/registry-normalization.ts, src/lib/state/registry-normalization.test.ts, src/lib/onboard/managed-image/contract.ts, src/lib/onboard/managed-image/contract.test.ts, src/lib/onboard/sandbox-registration.ts, src/lib/onboard/sandbox-registration.test.ts, src/lib/onboard/created-sandbox-finalization.ts, src/lib/onboard/created-sandbox-finalization.test.ts, src/lib/onboard/workload/rebuild.ts, src/lib/onboard/sandbox-workload-rebuild.test.ts, src/lib/actions/sandbox/rebuild-preflight-target-phase.ts, src/lib/actions/sandbox/rebuild-preflight-target-phase.test.ts, src/lib/actions/sandbox/rebuild-recreate-phase.ts, src/lib/actions/sandbox/rebuild-agent-runtime-receipt.test.ts</files>
  <read_first>.planning/phases/03-package-and-controller-foundation/03-CONTEXT.md, src/lib/agent-runtime-packages/receipt.ts, src/lib/agent-runtime-packages/release-set.ts, src/lib/state/registry/types.ts, src/lib/state/registry-normalization.ts, src/lib/onboard/managed-image/contract.ts, src/lib/onboard/sandbox-registration.ts, src/lib/onboard/created-sandbox-finalization.ts, src/lib/onboard/workload/rebuild.ts, src/lib/actions/sandbox/rebuild-preflight-target-phase.ts, src/lib/actions/sandbox/rebuild-recreate-phase.ts</read_first>
  <action>Apply D-17 through the active production path. Add an optional `agentRuntimeReceipt` digest reference to `SandboxEntry` and its strict normalizer. During current managed-image onboarding, `created-sandbox-finalization.ts` resolves the selected supported release-set entry and passes its exact receipt to `sandbox-registration.ts`. Registration validates the package ID against the selected agent and validates image fields against the current `ManagedImageContractV1` and workload receipt before committing the registry row. Legacy rows and nonstandard custom-image rows remain valid without the field. In the active rebuild path, `onboard/workload/rebuild.ts` and `rebuild-preflight-target-phase.ts` preserve and revalidate an existing optional receipt and require the same exact package and managed-image identity before the destructive boundary. A currently `revoked` or `superseded` referenced standard entry stops with remediation before mutation. `rebuild-recreate-phase.ts` carries the same receipt into replacement registration, and existing registry rollback restores the prior row unchanged on failure. Do not touch `onboard/managed-workload/rebuild/**`. Do not add a migration API, second lifecycle record, journal, state machine, dispatcher, fallback, or cleanup implementation.</action>
  <verify><automated>npx vitest run --project cli src/lib/state/registry-normalization.test.ts src/lib/onboard/managed-image/contract.test.ts src/lib/onboard/sandbox-registration.test.ts src/lib/onboard/created-sandbox-finalization.test.ts src/lib/onboard/sandbox-workload-rebuild.test.ts src/lib/actions/sandbox/rebuild-preflight-target-phase.test.ts src/lib/actions/sandbox/rebuild-agent-runtime-receipt.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - Existing records normalize and resume with unchanged legacy fields.
    - New standard managed-image rows receive the matching optional receipt; legacy and custom rows remain valid without one.
    - Current-integration records never claim controller negotiation or native operations.
    - The active rebuild preflight and recreate path preserve the same exact receipt through success and existing rollback behavior through failure.
    - Revoked, superseded, corrupt, missing, or contradictory referenced release data stops before destructive rebuild mutation.
    - Credential values cannot serialize into receipt fields.
  </acceptance_criteria>
  <done>Existing lifecycle state can carry exact package identity without duplicating lifecycle authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Specialized core adapter to OpenShell client | Transport consolidation must preserve each current operation's semantics. |
| Package receipt to product state | Untrusted package identity cannot replace release-set or lifecycle authority. |
| New receipt to legacy registry | Added identity must not overwrite or reinterpret current durable state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-20 | Elevation of Privilege | OpenShell transport | mitigate | Keep effects in existing core adapters and route only transport through one client. |
| T-03-21 | Spoofing | Receipt identity | mitigate | Cross-check package, agent, release-set, managed-image, OpenShell, driver, and platform identities. |
| T-03-22 | Tampering | Registry receipt | mitigate | Validate closed fields and preserve the existing registry transaction owner. |
</threat_model>

<verification>
- [ ] Focused adapter and state tests pass.
- [ ] Direct OpenShell call-site count decreases only by the five explicitly audited findings; remaining findings stay allowlisted for Phase 10.
- [ ] No new lifecycle, journal, migration API, dispatcher, or cleanup owner appears.
- [ ] CLI type checking passes.
</verification>

<success_criteria>
- Existing workflows retain their current owners and behavior while the audited adapter subset moves behind the existing client.
- Current managed-image registration and active rebuild/recreate can preserve exact package identity without another lifecycle state machine.
</success_criteria>

<output>
Create .planning/phases/03-package-and-controller-foundation/03-06-SUMMARY.md when done.
</output>
