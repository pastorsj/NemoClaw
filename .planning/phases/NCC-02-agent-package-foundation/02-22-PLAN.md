---
phase: NCC-02-agent-package-foundation
plan: 22
type: execute
wave: 17
depends_on:
  - "02-21"
files_modified:
  - test/e2e/fixtures/harness-package.ts
  - test/e2e/support/harness-package.test.ts
  - test/e2e/fixtures/phases/onboarding.ts
  - test/e2e/support/e2e-phase-onboarding.test.ts
  - test/e2e/registry/definitions/baseline.ts
  - test/e2e/fixtures/phases/state-validation.ts
  - test/e2e/support/e2e-phase-state-validation.test.ts
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - PKG-04A
  - TEST-03
  - TEST-04
user_setup: []
must_haves:
  truths:
    - "Every existing base standard package-managed live onboarding profile installs its exact reviewed harness package before invoking onboard."
    - "The fixture carries one closed package identity from installation through session and final registry validation."
    - "Receipt-verified inventory, session, and registry identities are exactly equal, including one full lowercase SHA-256 digest."
    - "The existing typed target registry and workflow planner remain the only E2E execution authority; no parallel target or workflow list is added."
  artifacts:
    - path: "test/e2e/fixtures/harness-package.ts"
      provides: "Host-observable install/list parsing and exact harness package evidence"
      contains: "HarnessPackageEvidence"
    - path: "test/e2e/fixtures/phases/onboarding.ts"
      provides: "Install-before-onboard behavior for existing standard profiles"
    - path: "test/e2e/fixtures/phases/state-validation.ts"
      provides: "Final receipt/session/registry identity and digest checks"
  key_links:
    - from: "test/e2e/fixtures/phases/onboarding.ts"
      to: "test/e2e/fixtures/harness-package.ts"
      via: "public installed CLI installs and re-lists the exact package before onboard"
      pattern: "installHarnessPackage"
    - from: "test/e2e/fixtures/phases/state-validation.ts"
      to: "test/e2e/fixtures/phases/onboarding.ts"
      via: "NemoClawInstance carries the installation identity into final validation"
      pattern: "harnessPackage"
---

<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

# Phase 2 Plan 22: Package-Aware E2E Fixtures

<objective>
Extend the existing typed E2E onboarding and state-validation fixtures so every standard live target
installs a reviewed harness package first and proves exact identity continuity after onboarding.

Purpose: The central E2E catalogue already owns environment, onboarding, lifecycle, and expected
state. Package migration coverage belongs in those shared phases, not in a second registry or a new
broad live workflow.

Output: One host-observable package helper plus install-before-onboard and final identity assertions
that all current standard targets inherit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@test/README.md
@test/e2e/README.md
@test/e2e/docs/README.md
@.planning/STATE.md
@.planning/phases/NCC-02-agent-package-foundation/02-21-SUMMARY.md
@.planning/phases/NCC-02-agent-package-foundation/02-CONTEXT.md
@test/e2e/fixtures/phases/onboarding.ts
@test/e2e/fixtures/phases/state-validation.ts
@test/e2e/registry/types.ts
@test/e2e/registry/definitions/baseline.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture exact package evidence through the installed CLI</name>
  <files>test/e2e/fixtures/harness-package.ts, test/e2e/support/harness-package.test.ts</files>
  <read_first>test/e2e/fixtures/clients/host.ts, test/e2e/fixtures/clients/command.ts, test/e2e/fixtures/redaction.ts, test/e2e/fixtures/phases/onboarding.ts, src/lib/harness/package-list.ts, src/lib/harness/package-identity.ts</read_first>
  <action>Add a small fixture helper that accepts a canonical standard harness ID and `HostCliClient`, invokes the public installed CLI as `nemoclaw harness install <id>`, then invokes `nemoclaw harness list` through its existing hidden machine-output convention. Parse only Plan 05's closed, versioned inventory schema and return `HarnessPackageEvidence` containing the exact identity and redacted command results. Require one matching receipt-verified installed row with exact `kind`, canonical `id`, adapter `packageVersion`, `contractVersion`, and full lowercase 64-hex SHA-256 `contentDigest`; reject duplicate, damaged, partial, extra-key, alias, path, control-character, and human-output inputs. Do not import `src/lib/**`, inspect npm/PyPI, execute package content, or create another available-package registry. Unit-test parsing, command order, redaction, exact-ID selection, and fail-closed malformed output with a fake host client.</action>
  <verify>`npx vitest run --project e2e-support test/e2e/support/harness-package.test.ts`</verify>
  <acceptance_criteria>
    - The helper runs install before list and returns one exact full identity.
    - Machine output is parsed as a closed schema rather than scraping human text.
    - Invalid, damaged, duplicate, aliased, or mismatched inventory fails before onboarding.
    - The helper has no CLI-source import and logs no package path, receipt path, or secret.
  </acceptance_criteria>
  <done>The E2E fixture layer can obtain exact receipt-backed package evidence through public behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Install the selected package before standard onboarding</name>
  <files>test/e2e/fixtures/phases/onboarding.ts, test/e2e/support/e2e-phase-onboarding.test.ts, test/e2e/registry/definitions/baseline.ts</files>
  <read_first>test/e2e/fixtures/phases/onboarding.ts, test/e2e/support/e2e-phase-onboarding.test.ts, test/e2e/fixtures/harness-package.ts, test/e2e/registry/runtime-support.ts, test/e2e/registry/definitions/baseline.ts</read_first>
  <action>At the shared onboarding phase boundary, install the canonical package already selected by each supported base standard onboarding profile (`openclaw`, `hermes`, or `langchain-deepagents-code`) before the first `onboard` call. Carry its `HarnessPackageEvidence` on `NemoClawInstance`. Wire the existing non-messaging `cloud-hermes` baseline through the same typed onboarding fixture with `NEMOCLAW_AGENT=hermes`, and update only that existing baseline target's execution coverage from unresolved to Hermes; do not add a target, suite, registry, or workflow branch. Repeated/resume targets must tolerate the public install's idempotent result and retain the same exact identity. Keep profile-to-agent selection in the existing onboarding switch and pass its canonical ID to the helper. Credentialed Hermes Discord/Slack profiles stay outside this change, and candidate Pi/NemoCUA profiles, if added through their existing qualification path, bypass standard installation with an explicit candidate result. Preserve negative preflight behavior, sandbox cleanup registration, timeouts, Docker checks, inference redaction, and existing onboarding command arguments.</action>
  <verify>`npx vitest run --project e2e-support test/e2e/support/e2e-phase-onboarding.test.ts test/e2e/support/harness-package.test.ts`</verify>
  <acceptance_criteria>
    - OpenClaw, non-messaging Hermes, and LangChain Deep Agents Code base onboarding tests observe `harness install` and receipt-verified list before `onboard`.
    - `NemoClawInstance` contains the same full identity returned by the install helper.
    - Repeat/resume installation is idempotent and identity-stable.
    - The existing base Hermes target becomes resolved without adding a target, registry type, suite ID, planner case, or duplicate workflow list.
  </acceptance_criteria>
  <done>All existing standard targets exercise the real package installation boundary before onboarding.</done>
</task>

<task type="auto">
  <name>Task 3: Prove final durable identity equality and digest integrity</name>
  <files>test/e2e/fixtures/phases/state-validation.ts, test/e2e/support/e2e-phase-state-validation.test.ts</files>
  <read_first>test/e2e/fixtures/phases/state-validation.ts, test/e2e/support/e2e-phase-state-validation.test.ts, test/e2e/fixtures/harness-package.ts, test/e2e/fixtures/phases/onboarding.ts, src/lib/state/registry/types.ts, src/lib/state/onboard-session.ts</read_first>
  <action>For a standard package-managed `NemoClawInstance`, re-run the public machine inventory at final state so its package-store/receipt verification is current, then read the host-observable onboard session and sandbox registry row through the fixture's existing bounded local-state boundary. Parse only the closed `harnessPackage` object from each and require field-for-field equality with the installation evidence and final installed inventory, including the full digest. Treat absent, partial, extra-key, malformed, different-digest, wrong-agent, or damaged-object results as a failed state validation. Preserve the OpenClaw null compatibility sentinel separately from effective package ID. Candidate instances explicitly skip this standard identity assertion rather than receiving synthetic evidence. Add support tests for success, each mismatch source, and path/secret-safe diagnostics.</action>
  <verify>`npx vitest run --project e2e-support test/e2e/support/e2e-phase-state-validation.test.ts test/e2e/support/harness-package.test.ts && npm run test:e2e-phases:check`</verify>
  <acceptance_criteria>
    - Final receipt-verified inventory, session, and registry identities are exactly equal.
    - Full digest format and equality are asserted, not inferred from a shortened human display.
    - OpenClaw's compatibility sentinel does not erase its effective package identity.
    - Failure diagnostics identify the authority that drifted without exposing paths or secrets.
  </acceptance_criteria>
  <done>Existing typed E2E targets prove install-to-final-state package identity continuity.</done>
</task>

</tasks>

<threat_model>

- **T-22-01 — Fake package coverage (critical):** E2E calls onboard without crossing public install.
  Mitigation: shared install-before-onboard fixture order with command assertions.
- **T-22-02 — Human-output coupling (high):** Tests pass by scraping unstable terminal text.
  Mitigation: closed machine schema and full identity parser.
- **T-22-03 — Partial equality (critical):** Agent ID matches while digest or version differs.
  Mitigation: field-for-field receipt, session, registry, and final inventory comparison.
- **T-22-04 — E2E authority fork (high):** Package tests add a second target/workflow catalogue.
  Mitigation: extend shared phases only; retain the typed registry and planner unchanged.

</threat_model>

<verification>

- [ ] Focused `e2e-support` helper, onboarding, and state-validation tests pass.
- [ ] `npm run test:e2e-phases:check`
- [ ] `npm run test:projects:check`
- [ ] `npm run typecheck:cli`
- [ ] `npm run checks:repository`
- [ ] `git diff --check`

</verification>

<success_criteria>

- Existing base standard E2E targets install exact reviewed packages before onboarding; credentialed
  messaging profiles remain explicitly excluded.
- Final host-observable authorities agree on one full immutable package identity.
- Coverage remains inside the existing typed phases, registry, and workflow planner.

</success_criteria>

<output>
After completion, create `.planning/phases/NCC-02-agent-package-foundation/02-22-SUMMARY.md`.
</output>
