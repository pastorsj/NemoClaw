---
phase: NCC-02-agent-package-foundation
plan: 15
type: execute
wave: 10
depends_on:
  - "02-11"
  - "02-14"
files_modified:
  - src/lib/state/registry/route-reservation.ts
  - src/lib/state/registry-route-reservation.test.ts
  - src/lib/state/registry/pending-policy-verification.ts
  - src/lib/state/registry.ts
  - src/lib/onboard/machine/handlers/provider-inference.ts
  - src/lib/onboard/machine/handlers/provider-inference.test.ts
  - src/lib/onboard/sandbox-create/orchestration.ts
  - src/lib/onboard/sandbox-create/orchestration.test.ts
  - src/lib/onboard/sandbox-create/policy-creation-receipt.ts
  - src/lib/onboard/sandbox-create/policy-creation-receipt.test.ts
autonomous: true
requirements:
  - AGENT-04
  - PKG-03A
  - COMP-03
  - COMP-04
  - COMP-04A
  - TEST-03
user_setup: []
must_haves:
  truths:
    - "Every package-managed route reservation and pending policy boundary carries the session's full exact package identity; migration provenance remains on the owning Session and SandboxEntry."
    - "Provider-inference and sandbox-create callers pass and revalidate identity at the mutation boundary rather than relying on schema presence alone."
    - "Policy-source bytes come from the pinned package root and policy replay requires both existing policy authority and exact package identity."
    - "Qualified Pi and NemoCUA continue through existing candidate state without fabricated package identity."
  artifacts:
    - path: "src/lib/state/registry/route-reservation.ts"
      provides: "Exact package identity in resumable route authority"
    - path: "src/lib/onboard/machine/handlers/provider-inference.ts"
      provides: "Identity-bearing route reservation calls"
    - path: "src/lib/onboard/sandbox-create/orchestration.ts"
      provides: "Pinned identity and policy-source checks at sandbox creation"
  key_links:
    - from: "src/lib/onboard/machine/handlers/provider-inference.ts"
      to: "src/lib/state/registry.ts"
      via: "route reservation includes selected package identity"
      pattern: "reserveSandboxInferenceRoute"
    - from: "src/lib/onboard/sandbox-create/orchestration.ts"
      to: "src/lib/onboard/sandbox-create/policy-creation-receipt.ts"
      via: "verified-create replay binds policy and package authority"
      pattern: "policyCreationReceipt"
---

<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

# Phase 2 Plan 15: Route and Policy Package Authority

<objective>
Carry and enforce exact package-managed identity through route reservation, pending policy
verification, provider inference, and the sandbox-create boundary.

Purpose: Durable schemas do not protect retries unless the real writers and callers populate and
revalidate the identity before their external effects.

Output: Identity-bearing route/policy writes, pinned policy-source reads, and focused drift tests at
existing mutation boundaries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/NCC-02-agent-package-foundation/02-11-SUMMARY.md
@.planning/phases/NCC-02-agent-package-foundation/02-14-SUMMARY.md
@.planning/phases/NCC-02-agent-package-foundation/02-CONTEXT.md
@src/lib/state/registry.ts
@src/lib/onboard/machine/handlers/provider-inference.ts
@src/lib/onboard/sandbox-create/orchestration.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bind route and pending policy records</name>
  <files>src/lib/state/registry/route-reservation.ts, src/lib/state/registry-route-reservation.test.ts, src/lib/state/registry/pending-policy-verification.ts, src/lib/state/registry.ts</files>
  <read_first>src/lib/state/registry.ts, src/lib/state/registry/route-reservation.ts, src/lib/state/registry/pending-policy-verification.ts, src/lib/state/registry/types.ts, src/lib/harness/package-identity.ts</read_first>
  <action>Require package-managed route reservations and pending policy-verification records to receive the exact session identity and serialize it through the Plan 09 strict schemas. The owning route/policy SandboxEntry also retains any Session migration provenance at its top level; nested policy records do not duplicate it. On retry, rotation, policy checkpoint update, and cleanup, compare every identity field and verify Session/SandboxEntry migration metadata when present before changing registry state. Preserve absent identity only for explicit legacy/candidate state; standard legacy state must pass Plan 13 migration first. Do not add another registry or transaction.</action>
  <verify>`npx vitest run --project cli src/lib/state/registry-route-reservation.test.ts`</verify>
  <acceptance_criteria>
    - Package-managed route and pending policy state round-trip exact identity.
    - ID, version, contract, or digest drift rejects retry before mutation.
    - Candidate absence is distinguished from malformed package identity.
    - Existing route ownership and cleanup semantics remain intact.
  </acceptance_criteria>
  <done>Registry route and policy stages carry one exact package-managed authority.</done>
</task>

<task type="auto">
  <name>Task 2: Pass identity from provider inference</name>
  <files>src/lib/onboard/machine/handlers/provider-inference.ts, src/lib/onboard/machine/handlers/provider-inference.test.ts</files>
  <read_first>src/lib/onboard/machine/handlers/provider-inference.ts, src/lib/onboard/machine/handlers/provider-inference.test.ts, src/lib/state/registry.ts, src/lib/onboard/machine/types.ts</read_first>
  <action>Extend the provider-inference handler dependency and both current route-reservation call sites to pass the selected session package identity. Re-read current session/registry authority immediately before reservation and fail closed on package-managed mismatch. Preserve provider, credential, route-containment, and policy behavior; do not make the inference provider own package selection.</action>
  <verify>`npx vitest run --project cli src/lib/onboard/machine/handlers/provider-inference.test.ts`</verify>
  <acceptance_criteria>
    - Every current package-managed provider route call supplies exact package authority; candidate calls supply explicit absence.
    - Drift is rejected before route publication or credential mutation.
    - Candidate calls remain valid with explicitly absent package identity.
    - Provider behavior remains otherwise unchanged.
  </acceptance_criteria>
  <done>The real route writer receives identity from the selected onboarding session.</done>
</task>

<task type="auto">
  <name>Task 3: Revalidate package and policy at sandbox create</name>
  <files>src/lib/onboard/sandbox-create/orchestration.ts, src/lib/onboard/sandbox-create/orchestration.test.ts, src/lib/onboard/sandbox-create/policy-creation-receipt.ts, src/lib/onboard/sandbox-create/policy-creation-receipt.test.ts</files>
  <read_first>src/lib/onboard/sandbox-create/orchestration.ts, src/lib/onboard/sandbox-create/policy-creation-receipt.ts, src/lib/onboard/sandbox-create/policy-creation-receipt.test.ts, src/lib/state/registry.ts, src/lib/onboard/sandbox-agent.ts</read_first>
  <action>At sandbox-create qualification and verified-create replay, resolve the exact package-managed object again, compare session, reservation, and pending policy identity, verify migration metadata only between owning Session and SandboxEntry when present, and read agent policy sources from the pinned definition root established in Plan 11. Extend the existing policy creation receipt comparison with package identity while retaining its current policy hash/source authority. Reject package, owner-provenance, or policy-source drift before the next policy, OpenShell, or sandbox mutation. Candidate creation retains existing gated authority with absent identity and migration provenance.</action>
  <verify>`npx vitest run --project cli src/lib/onboard/sandbox-create/orchestration.test.ts src/lib/onboard/sandbox-create/policy-creation-receipt.test.ts`</verify>
  <acceptance_criteria>
    - Verified create requires matching session, object, route, pending policy, and policy hash.
    - Package or policy-source drift stops before the next external effect.
    - Reads use the pinned package root rather than repository or active state.
    - Existing sandbox-create transaction ownership is unchanged.
  </acceptance_criteria>
  <done>Sandbox creation cannot proceed through stale package or policy authority.</done>
</task>

</tasks>

<threat_model>

- **T-15-01 — Partial route identity (critical):** A retry matches agent ID but not digest. Mitigation:
  exact all-field comparison in registry operations.
- **T-15-02 — Caller omission (critical):** Schema supports identity but provider writes omit it.
  Mitigation: update and test the actual provider call sites.
- **T-15-03 — Policy source drift (critical):** Verified create uses changed repository/package bytes.
  Mitigation: pinned-root read plus existing policy hash and package identity.
- **T-15-04 — Candidate coercion (high):** Candidate records are treated as malformed packages.
  Mitigation: explicit gated candidate absence distinct from malformed present identity.

</threat_model>

<verification>

- [ ] Focused route, provider, sandbox-create, and policy receipt tests pass.
- [ ] `npm run typecheck:cli`
- [ ] `npm run test:fast`
- [ ] `npm run checks:repository`
- [ ] `git diff --check`

</verification>

<success_criteria>

- Package-managed route, policy, and create authorities agree exactly.
- Drift fails before the next external mutation.
- Existing core and candidate lifecycle ownership remains unchanged.

</success_criteria>

<output>
After completion, create `.planning/phases/NCC-02-agent-package-foundation/02-15-SUMMARY.md`.
</output>
