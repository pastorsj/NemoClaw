---
phase: NCC-02-agent-package-foundation
plan: 07
type: execute
wave: 6
depends_on:
  - "02-05"
files_modified:
  - src/lib/agent/list-command.ts
  - src/lib/agent/list-command.test.ts
  - src/commands/agents/list.ts
  - src/commands/agents/list.test.ts
autonomous: true
requirements:
  - UX-01
  - UX-02
  - PKG-05
  - AGENT-01
user_setup: []
must_haves:
  truths:
    - "D-08: `agents list` becomes an installed-only compatibility view without becoming package-management authority."
    - "D-03: The compatibility command reads the same installed inventory as `harness list`."
    - "D-02: Pi and NemoCUA retain their separate candidate gates and never receive fabricated package identity."
    - "D-13: Compatibility rendering remains isolated from catalogue and command parsing."
  artifacts:
    - path: "src/lib/agent/list-command.ts"
      provides: "Installed-only compatibility entries and legacy text rendering"
      contains: "listAgentRuntimeEntries"
    - path: "src/commands/agents/list.ts"
      provides: "Unchanged public compatibility command surface"
      contains: "agents:list"
  key_links:
    - from: "src/lib/agent/list-command.ts"
      to: "src/lib/harness/package-list.ts"
      via: "shared receipt-verified installed inventory"
      pattern: "HarnessPackageInventory"
    - from: "src/commands/agents/list.ts"
      to: "src/lib/agent/list-command.ts"
      via: "legacy renderer"
      pattern: "printAgentRuntimeList"
---

<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

# Phase 2 Plan 07: Installed Agent List Compatibility

<objective>
Keep `nemoclaw agents list` available while changing its source of truth from repository manifests
to receipt-verified installed standard harnesses.

Purpose: Existing users and aliases retain their familiar compatibility command while `harness
list/install` own package management.

Output: An installed-only compatibility projection and the unchanged public `agents list` command.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@WRITING.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/NCC-02-agent-package-foundation/02-05-SUMMARY.md
@.planning/phases/NCC-02-agent-package-foundation/02-CONTEXT.md
@src/lib/agent/list-command.ts
@src/commands/agents.ts
@src/commands/agents/list.ts
@src/lib/harness/package-list.ts
@src/lib/harness/package-catalog.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project installed packages into legacy list text</name>
  <files>src/lib/agent/list-command.ts, src/lib/agent/list-command.test.ts</files>
  <read_first>src/lib/agent/list-command.ts, src/lib/harness/package-list.ts, src/lib/harness/package-catalog.ts, src/lib/agent/aliases.ts, WRITING.md</read_first>
  <action>Replace repository-manifest discovery with the shared Plan 02-05 inventory and project only receipt-verified installed, selectable standard harnesses into the existing agent-list entry shape. Preserve canonical IDs, descriptions, OpenClaw-first ordering, and the exact empty text `No agent runtimes are installed.` Keep current alias resolution semantics elsewhere unchanged; do not add alias rows or reinterpret an available-only package as installed. Explicitly exclude damaged, available-only, unsupported, and candidate entries. Pi and NemoCUA remain on their accepted candidate gates and do not receive synthetic installation receipts or package identities. Inject the inventory in tests so no source test reads `dist/harnesses` or real HOME.</action>
  <verify>`npx vitest run --project cli src/lib/agent/list-command.test.ts`</verify>
  <acceptance_criteria>
    - Zero, one, multiple, damaged, available-only, and candidate cases have deterministic tests.
    - Empty text, row format, descriptions, and OpenClaw-first ordering retain compatibility semantics.
    - Existing alias targets remain canonical and no alias is rendered as a duplicate installed row.
    - Listing performs no installation, activation, qualification, or state mutation.
  </acceptance_criteria>
  <done>The legacy renderer reports the same selectable installed standard harness set as package inventory.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve the agents list command surface</name>
  <files>src/commands/agents/list.ts, src/commands/agents/list.test.ts</files>
  <read_first>src/commands/agents.ts, src/commands/agents/list.ts, src/lib/agent/list-command.ts, src/lib/cli/nemoclaw-oclif-command.ts</read_first>
  <action>Keep the `agents:list` ID, `nemoclaw agents list` usage, existing summary/description semantics, topic behavior, compatibility shims, and command aliases unchanged. Delegate output to the installed-only renderer and do not expose available packages, package install flags, or new machine schema from this compatibility command. Update command tests to assert delegation and unchanged help/text behavior with injected installed inventory.</action>
  <verify>`npx vitest run --project cli src/commands/agents/list.test.ts && npm run typecheck:cli`</verify>
  <acceptance_criteria>
    - Existing command ID, usage, topic, aliases, and empty message remain unchanged.
    - The command shows installed standard entries only and never triggers installation.
    - Tests do not require built artifacts or host package state.
  </acceptance_criteria>
  <done>`nemoclaw agents list` remains a compatible installed-only view over the new package model.</done>
</task>

</tasks>

<threat_model>

- **T-07-01 — Compatibility drift (medium):** Migration changes command names, aliases, or empty
  output. Mitigation: exact legacy text and metadata assertions.
- **T-07-02 — Available-as-installed confusion (high):** Bundled presence becomes selectability.
  Mitigation: receipt-verified installed projection only.
- **T-07-03 — Candidate promotion (high):** Pi or NemoCUA acquires ordinary package authority.
  Mitigation: explicit candidate exclusion and gate-preservation tests.
- **T-07-04 — Corruption masking (high):** Damaged state silently falls back to a repository
  manifest. Mitigation: no repository fallback and damaged-entry exclusion/error assertions.

</threat_model>

<verification>

- [ ] `npx vitest run --project cli src/lib/agent/list-command.test.ts src/commands/agents/list.test.ts`
- [ ] `npm run typecheck:cli`
- [ ] `npm run checks:repository`
- [ ] `git diff --check`

</verification>

<success_criteria>

- Existing `agents list` users retain its command, aliases, ordering, and text semantics.
- The compatibility view and package list agree on receipt-verified installed standard harnesses.
- Candidate and available-only content cannot become installed through compatibility rendering.

</success_criteria>

<output>
After completion, create `.planning/phases/NCC-02-agent-package-foundation/02-07-SUMMARY.md`.
</output>
