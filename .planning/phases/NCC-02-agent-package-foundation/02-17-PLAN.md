---
phase: NCC-02-agent-package-foundation
plan: 17
type: execute
wave: 12
depends_on:
  - "02-16"
files_modified:
  - src/lib/state/registry.ts
  - src/lib/onboard/sandbox-registration.ts
  - src/lib/onboard/sandbox-registration.test.ts
  - src/lib/onboard/created-sandbox-finalization.ts
  - src/lib/onboard/created-sandbox-finalization.test.ts
  - src/lib/onboard/agent-resume-state.ts
  - src/lib/onboard/agent-resume-state.test.ts
  - src/lib/onboard/cancel-rollback.ts
  - src/lib/onboard/cancel-rollback.test.ts
  - src/lib/onboard/cancel-resume-state.test.ts
autonomous: true
requirements:
  - UX-01
  - PKG-03A
  - COMP-03
  - COMP-04
  - COMP-04A
  - COMP-04B
  - TEST-03
user_setup: []
must_haves:
  truths:
    - "Final package-managed registration publishes only when every intermediate identity agrees and owning Session/SandboxEntry migration provenance agrees when present."
    - "OpenClaw final state preserves the compatibility `agent === null` sentinel alongside separate exact effective package authority."
    - "Cancellation after durable sandbox creation preserves sandbox, incomplete registry row, session, checkpoint, immutable package object, and active-session pointer for original-digest resume."
    - "Qualified Pi and NemoCUA retain current gated finalization and cancellation semantics without fabricated package identity."
  artifacts:
    - path: "src/lib/onboard/sandbox-registration.ts"
      provides: "Final all-authority package identity gate"
      contains: "assertMatchingHarnessIdentity"
    - path: "src/lib/onboard/created-sandbox-finalization.ts"
      provides: "Identity-preserving incomplete-to-final registry transition"
    - path: "src/lib/onboard/cancel-resume-state.test.ts"
      provides: "Cross-state cancellation and original-digest resume evidence"
  key_links:
    - from: "src/lib/onboard/sandbox-registration.ts"
      to: "src/lib/state/registry.ts"
      via: "final publication after exact authority comparison"
      pattern: "registerSandbox"
    - from: "src/lib/onboard/cancel-rollback.ts"
      to: "src/lib/state/onboard-session.ts"
      via: "preserve resumable package-bound state"
      pattern: "markCancelled"
---

<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

# Phase 2 Plan 17: Final Registry and Resumable Cancellation

<objective>
Close the package-managed onboarding transaction with an exact final registry gate and preserve all
original-digest authority when the user cancels after sandbox creation.

Purpose: Identity propagation is incomplete until publication and cancellation agree on what must
remain recoverable.

Output: Final all-authority registration, identity-preserving created-sandbox finalization, and a
cross-state cancellation/resume test beyond the existing helper coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/NCC-02-agent-package-foundation/02-16-SUMMARY.md
@.planning/phases/NCC-02-agent-package-foundation/02-CONTEXT.md
@src/lib/state/registry.ts
@src/lib/onboard/sandbox-registration.ts
@src/lib/onboard/created-sandbox-finalization.ts
@src/lib/onboard/cancel-rollback.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish only exact final registry authority</name>
  <files>src/lib/state/registry.ts, src/lib/onboard/sandbox-registration.ts, src/lib/onboard/sandbox-registration.test.ts, src/lib/onboard/created-sandbox-finalization.ts, src/lib/onboard/created-sandbox-finalization.test.ts</files>
  <read_first>src/lib/state/registry.ts, src/lib/onboard/sandbox-registration.ts, src/lib/onboard/created-sandbox-finalization.ts, src/lib/harness/package-identity.ts, src/lib/onboard/sandbox-agent.ts</read_first>
  <action>Before final publication, resolve the package-managed pinned object again and compare full identity across Session, checkpoint, recreate state when present, route reservation, pending policy verification, created-sandbox row, and requested final entry. Compare optional migration provenance only between the owning Session, current SandboxEntry, and requested final SandboxEntry. Publish only after all authority agrees, preserve the identity, and copy or retain the same owner provenance while clearing pending fields. Keep OpenClaw's recorded `agent: null` compatibility sentinel while its separate package identity proves effective OpenClaw. Reject missing, malformed, or drifted standard state without active/bundled fallback. Qualified Pi and NemoCUA continue through existing finalization gates with absent package identity and migration provenance.</action>
  <verify>`npx vitest run --project cli src/lib/onboard/sandbox-registration.test.ts src/lib/onboard/created-sandbox-finalization.test.ts`</verify>
  <acceptance_criteria>
    - Final package-managed registry identity equals the original session identity byte for byte.
    - Any authority mismatch stops before final registry publication.
    - Pending-state cleanup cannot drop exact identity or owning SandboxEntry migration provenance.
    - OpenClaw sentinel and candidate finalization remain compatible.
  </acceptance_criteria>
  <done>Published package-managed sandboxes have one proven immutable package authority.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve created state for original-digest resume</name>
  <files>src/lib/onboard/agent-resume-state.ts, src/lib/onboard/agent-resume-state.test.ts, src/lib/onboard/cancel-rollback.ts, src/lib/onboard/cancel-rollback.test.ts, src/lib/onboard/cancel-resume-state.test.ts</files>
  <read_first>src/lib/onboard/agent-resume-state.ts, src/lib/onboard/cancel-rollback.ts, src/lib/onboard/created-sandbox-finalization.ts, src/lib/state/onboard-session.ts, src/lib/state/registry.ts, src/lib/harness/package-store.ts</read_first>
  <action>Preserve the existing post-create cancellation rule and do not restore delete-by-name rollback. After durable create, retain the incomplete sandbox, package-bearing registry row, session, checkpoint, immutable object, and active-session pointer; mark only the existing cancellation state required for `onboard --resume`. Clear only disposable pre-create state through current ownership rules. Add one orchestration/state test crossing created-sandbox finalization, cancellation, active-pointer advancement, and resume to prove the original digest/root is used and no retained object/state is deleted. Candidate cancellation remains governed by existing gated state without package assertions.</action>
  <verify>`npx vitest run --project cli src/lib/onboard/agent-resume-state.test.ts src/lib/onboard/cancel-rollback.test.ts src/lib/onboard/cancel-resume-state.test.ts`</verify>
  <acceptance_criteria>
    - Package-managed post-create cancellation retains every state/object required for resume.
    - Active-pointer advancement cannot change resumed package bytes.
    - Missing retained content stops before further external mutation.
    - Pre-create cleanup and candidate behavior remain unchanged.
  </acceptance_criteria>
  <done>Cancellation cannot strand a created sandbox or substitute its package authority.</done>
</task>

</tasks>

<threat_model>

- **T-17-01 — Final publication drift (critical):** Registry publishes identity different from the
  session or pending authorities. Mitigation: final all-authority exact comparison.
- **T-17-02 — Sentinel rewrite (high):** Package adoption changes compatibility `agent: null`.
  Mitigation: store effective package authority separately.
- **T-17-03 — Destructive cancellation (critical):** Cancel removes state or bytes needed to resume.
  Mitigation: preservation-only post-create rule and cross-state test.
- **T-17-04 — Resume pointer substitution (critical):** Cancellation resume follows a newer active
  object. Mitigation: retained session/registry identity and explicit-digest resolution.

</threat_model>

<verification>

- [ ] Focused finalization, registration, cancellation, and resume tests pass.
- [ ] `npm run typecheck:cli`
- [ ] `npm run test:fast`
- [ ] `npm run checks:repository`
- [ ] `git diff --check`

</verification>

<success_criteria>

- Final package-managed registry state is exactly identity-bound.
- Post-create cancellation remains resumable with the original immutable object.
- Compatibility sentinels and qualified candidates remain behaviorally unchanged.

</success_criteria>

<output>
After completion, create `.planning/phases/NCC-02-agent-package-foundation/02-17-SUMMARY.md`.
</output>
