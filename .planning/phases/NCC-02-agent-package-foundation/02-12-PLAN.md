---
phase: NCC-02-agent-package-foundation
plan: 12
type: execute
wave: 7
depends_on:
  - "02-06"
  - "02-07"
  - "02-10"
files_modified:
  - test/helpers/harness-packages.ts
  - src/lib/onboard/package-selection.ts
  - src/lib/onboard/package-selection.test.ts
  - src/lib/onboard/agent-selection.ts
  - src/lib/onboard/agent-selection.test.ts
  - src/lib/actions/sandbox/pi-candidate-lifecycle.test.ts
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - AGENT-04
  - COMP-03
  - TEST-03
user_setup: []
must_haves:
  truths:
    - "Standard bundled-harness selection uses installed selectable packages only."
    - "Zero, one, many interactive, many non-interactive, and explicit-uninstalled outcomes match UX-03 without mutating durable or external state."
    - "Source tests use explicit private bundle/store roots and never the real HOME or `dist/harnesses`."
    - "Qualified Pi and NemoCUA retain explicit gated invocation outside the standard installed picker and carry no fabricated package identity."
  artifacts:
    - path: "test/helpers/harness-packages.ts"
      provides: "Shared private temporary bundled-artifact and installed-store fixture"
    - path: "src/lib/onboard/package-selection.ts"
      provides: "Pure installed-package selection outcomes and exact package resolution"
      contains: "selectOnboardHarnessPackage"
  key_links:
    - from: "src/lib/onboard/package-selection.ts"
      to: "src/lib/harness/package-catalog.ts"
      via: "explicit catalogue and store roots"
      pattern: "listHarnessPackageInventory"
    - from: "src/lib/onboard/package-selection.ts"
      to: "src/lib/onboard/sandbox-agent.ts"
      via: "selected exact object becomes the effective definition"
      pattern: "resolveSandboxAgent"
---

<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

# Phase 2 Plan 12: Installed Harness Selection

<objective>
Implement deterministic installed-only selection for standard harness packages and make its source
tests independent of real user or compiled-package state.

Purpose: The user-facing zero/one/many workflow must be testable as a pure decision before it is
placed inside the onboarding writer lock.

Output: One reusable private fixture and a focused selection service preserving aliases and
qualified candidate gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/NCC-02-agent-package-foundation/02-06-SUMMARY.md
@.planning/phases/NCC-02-agent-package-foundation/02-07-SUMMARY.md
@.planning/phases/NCC-02-agent-package-foundation/02-10-SUMMARY.md
@.planning/phases/NCC-02-agent-package-foundation/02-CONTEXT.md
@src/lib/onboard/agent-selection.ts
@src/lib/harness/package-catalog.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Provide private installed-package test roots</name>
  <files>test/helpers/harness-packages.ts</files>
  <read_first>test/helpers/isolate-test-state.ts, src/lib/harness/package-catalog.test.ts, src/lib/harness/package-store.test.ts, src/lib/harness/package-install.test.ts</read_first>
  <action>Add one shared helper that creates private temporary bundled-artifact and package-store roots, writes only validated reviewed fixture artifacts through existing builders, installs through the shared transaction, and returns explicit roots plus cleanup. It must never read or mutate real HOME, the user store, or `dist/harnesses`; never execute fixture package content; and permit zero, one, many, damaged, and active-pointer-advanced arrangements. Normalize fixture permissions and clean exact temporary paths.</action>
  <verify>`npx vitest run --project cli src/lib/harness/package-catalog.test.ts src/lib/harness/package-store.test.ts`</verify>
  <acceptance_criteria>
    - Tests can construct installed inventories without global state.
    - Fixture installation uses production validation and store transactions.
    - Cleanup targets only fixture-created temporary roots.
    - No authoring test or package content executes during fixture install.
  </acceptance_criteria>
  <done>Selection and resume tests share one safe, explicit installed-package setup.</done>
</task>

<task type="auto">
  <name>Task 2: Select standard installed harnesses deterministically</name>
  <files>src/lib/onboard/package-selection.ts, src/lib/onboard/package-selection.test.ts, src/lib/onboard/agent-selection.ts, src/lib/onboard/agent-selection.test.ts, src/lib/actions/sandbox/pi-candidate-lifecycle.test.ts</files>
  <read_first>test/helpers/harness-packages.ts, src/lib/onboard/agent-selection.ts, src/lib/harness/package-catalog.ts, src/lib/harness/package-store.ts, src/lib/onboard/sandbox-agent.ts, src/lib/actions/sandbox/pi-candidate-lifecycle.test.ts</read_first>
  <action>Add a dependency-injected selection service over explicit bundled-artifact and store roots. For standard packages: zero returns an install-guidance stop outcome; one selects automatically; many interactive uses the existing numbered picker; many non-interactive selects OpenClaw only when installed and otherwise requires `--agent`; explicit uninstalled ID fails with `nemoclaw harness install <id>`. Preserve current aliases. Return exact resolved package/effective definition, not only an ID. Keep separately qualified Pi and NemoCUA out of installed inventory and the picker while preserving their current explicit invocation gates. Use the shared private fixture for every new test.</action>
  <verify>`npx vitest run --project cli src/lib/onboard/package-selection.test.ts src/lib/onboard/agent-selection.test.ts src/lib/actions/sandbox/pi-candidate-lifecycle.test.ts`</verify>
  <acceptance_criteria>
    - Zero/one/many outcomes match UX-03 exactly.
    - Non-interactive behavior never invents an uninstalled default.
    - The result contains exact package and effective definition authority.
    - Pi and NemoCUA remain gated, non-package-managed, and absent from the picker.
  </acceptance_criteria>
  <done>Onboarding has one testable installed-package decision without mutating user state.</done>
</task>

</tasks>

<threat_model>

- **T-12-01 — Ambient selection (critical):** Tests or production consult real HOME or compiled
  artifacts. Mitigation: explicit bundle/store roots and dependency injection.
- **T-12-02 — Uninstalled default (high):** Non-interactive selection chooses OpenClaw when absent.
  Mitigation: installed-membership check before defaulting.
- **T-12-03 — Candidate promotion (high):** Pi or NemoCUA enters the standard picker. Mitigation:
  preserve independent qualification gates and no package identity.
- **T-12-04 — Fixture execution (critical):** Test installation runs package code. Mitigation: data
  validation and existing copy-only install transaction.

</threat_model>

<verification>

- [ ] Focused selection and candidate tests pass with private fixture roots.
- [ ] `npm run typecheck:cli`
- [ ] `npm run test:fast`
- [ ] `npm run test:projects:check`
- [ ] `npm run checks:repository`
- [ ] `git diff --check`

</verification>

<success_criteria>

- Standard selection is installed-only and deterministic.
- Source tests depend on no user or compiled-package state.
- Existing candidate and alias behavior remains available.

</success_criteria>

<output>
After completion, create `.planning/phases/NCC-02-agent-package-foundation/02-12-SUMMARY.md`.
</output>
