---
phase: 02-explicit-runtime-identity
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/lib/onboard.ts
  - src/lib/state/onboard-session.ts
  - src/lib/state/onboard-checkpoint-types.ts
  - src/lib/state/onboard-checkpoint.ts
  - src/lib/state/onboard-checkpoint.test.ts
  - src/lib/state/onboard-checkpoint-migrate.ts
  - src/lib/state/onboard-checkpoint-migrate.test.ts
  - src/lib/state/onboard-step-state.ts
  - src/lib/state/onboard-step-state.test.ts
  - src/lib/onboard/machine/types.ts
  - src/lib/onboard/machine/definition.ts
  - src/lib/onboard/machine/transitions.ts
  - src/lib/onboard/machine/phase-progress.ts
  - src/lib/onboard/machine/flow-slices.ts
  - src/lib/onboard/machine/core-flow-phases.ts
  - src/lib/onboard/machine/initial-flow-phases.ts
  - src/lib/onboard/machine/initial-flow-phases.test.ts
  - src/lib/onboard/machine/flow-sequence.ts
  - src/lib/onboard/machine/flow-sequence.test.ts
  - src/lib/onboard/machine/final-flow-phases.ts
  - src/lib/onboard/machine/final-flow-composition.ts
  - src/lib/onboard/machine/flow-phases/agent-policy-finalization.ts
  - src/lib/onboard/machine/handlers/agent-setup.ts
  - src/lib/onboard/machine/handlers/sandbox.ts
  - src/lib/onboard/machine/handlers/agent-setup.test.ts
  - src/lib/onboard/machine/final-flow-phases.test.ts
  - src/lib/onboard/machine/final-flow-phases.runtime.test.ts
  - src/lib/onboard/machine/core-flow-phases.test.ts
  - src/lib/onboard/machine/transitions.test.ts
  - src/lib/onboard/machine/transition-traces.test.ts
  - src/lib/onboard/machine/README.md
  - src/lib/onboard/agent-selection.test.ts
  - src/lib/onboard/agent-resume-state.test.ts
autonomous: true
requirements: [UX-03, STATE-01]
user_setup: []
must_haves:
  truths:
    - "D-06: All standard runtimes pass through one runtime_setup machine state and handler, including sequence and initial-flow registries."
    - "D-08: The accepted NemoClaw/OpenShell policy, credential, state, lifecycle, and rollback authority split stays unchanged."
    - "D-09: Machine snapshot V1 and checkpoint V4 migrate explicitly to machine V2 and checkpoint V5 without replay."
    - "D-10: Updated characterization and machine tests prove the same observable flow, error propagation, and all legacy resume paths."
    - "D-05: Runtime-specific presentation remains baseline-compatible without runtime-specific state-machine branches."
  artifacts:
    - path: src/lib/onboard/machine/handlers/agent-setup.ts
      provides: One explicit-definition runtime setup handler with current OpenClaw and non-OpenClaw behavior.
    - path: src/lib/onboard/machine/transitions.ts
      provides: Unified runtime_setup transition graph.
    - path: src/lib/state/onboard-session.ts
      provides: Machine snapshot V2 writer and explicit V1 state, step, pointer, and recovery-receipt migration.
    - path: src/lib/state/onboard-checkpoint.ts
      provides: Checkpoint V5 writer and explicit V4 machine-state migration.
  key_links:
    - from: src/lib/onboard/machine/handlers/sandbox.ts
      to: src/lib/onboard/machine/transitions.ts
      via: One sandbox-to-runtime_setup branch.
    - from: src/lib/state/onboard-session.ts
      to: src/lib/onboard/machine/types.ts
      via: Legacy state-label normalization.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Collapse the OpenClaw and non-OpenClaw onboarding branches into one explicit runtime setup flow.

Purpose: Remove control-flow semantics derived from null without changing runtime behavior or user experience.
Output: One runtime_setup state, one handler, bounded legacy state-label migration, and parity tests for all three standard runtimes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-explicit-runtime-identity/02-CONTEXT.md
@.planning/phases/02-explicit-runtime-identity/02-02-SUMMARY.md
@src/lib/onboard.ts
@src/lib/state/onboard-step-state.ts
@src/lib/onboard/machine/handlers/agent-setup.ts
@src/lib/onboard/machine/handlers/sandbox.ts
@src/lib/onboard/machine/final-flow-phases.ts
@src/lib/onboard/machine/transitions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce one runtime setup state with legacy state readers</name>
  <files>src/lib/state/onboard-session.ts, src/lib/state/onboard-checkpoint-types.ts, src/lib/state/onboard-checkpoint.ts, src/lib/state/onboard-checkpoint.test.ts, src/lib/state/onboard-checkpoint-migrate.ts, src/lib/state/onboard-checkpoint-migrate.test.ts, src/lib/state/onboard-step-state.ts, src/lib/state/onboard-step-state.test.ts, src/lib/onboard/machine/types.ts, src/lib/onboard/machine/definition.ts, src/lib/onboard/machine/transitions.ts, src/lib/onboard/machine/phase-progress.ts, src/lib/onboard/machine/flow-slices.ts, src/lib/onboard/machine/core-flow-phases.ts, src/lib/onboard/machine/initial-flow-phases.ts, src/lib/onboard/machine/initial-flow-phases.test.ts, src/lib/onboard/machine/flow-sequence.ts, src/lib/onboard/machine/flow-sequence.test.ts, src/lib/onboard/machine/final-flow-phases.ts, src/lib/onboard/machine/final-flow-composition.ts, src/lib/onboard/machine/README.md</files>
  <read_first>.planning/phases/02-explicit-runtime-identity/02-CONTEXT.md, src/lib/state/onboard-session.ts, src/lib/state/onboard-checkpoint-types.ts, src/lib/state/onboard-checkpoint.ts, src/lib/state/onboard-checkpoint-migrate.ts, src/lib/state/onboard-step-state.ts, src/lib/onboard/machine/types.ts, src/lib/onboard/machine/definition.ts, src/lib/onboard/machine/transitions.ts, src/lib/onboard/machine/phase-progress.ts, src/lib/onboard/machine/flow-slices.ts, src/lib/onboard/machine/core-flow-phases.ts, src/lib/onboard/machine/initial-flow-phases.ts, src/lib/onboard/machine/flow-sequence.ts, src/lib/onboard/machine/final-flow-phases.ts, src/lib/onboard/machine/final-flow-composition.ts, src/lib/onboard/machine/README.md</read_first>
  <action>D-06 and D-09: add `runtime_setup` as the single post-sandbox execution state and step across types, transitions, initial and final phase composition, flow sequence, stop-state sets, progress accounting, and next-step mapping. Increment `MACHINE_SNAPSHOT_VERSION` from 1 to 2 and `CHECKPOINT_SCHEMA_VERSION` from 4 to 5. Keep named, strict machine-V1 and checkpoint-V4 readers. Migrate `openclaw` or `agent_setup` consistently across `machine.state`, `checkpoint.machineState`, step-map keys, `lastStepStarted`, `lastCompletedStep`, `failure.step`, and recovery-receipt `entry`; recompute the recovery receipt ID from the translated entry while preserving reason, revision, and time. Merge a legacy step only when its counterpart is absent or byte-for-value equivalent; reject conflicting duplicate branch steps, inconsistent machine/checkpoint state, impossible completion order, unknown identity, and future versions. Preserve all unrelated decisions, effect groups, bindings, sandbox replacement state, and timestamps. Perform no write during read; the next authorized save writes session V2, machine V2, and checkpoint V5 atomically. Remove new writes of `openclaw` and `agent_setup`, but keep those labels in the bounded V1/V4 readers until Phase 10. D-05: preserve current user-visible progress labels from the explicit runtime definition while keeping execution state generic. Update the active machine README in the same change.</action>
  <verify><automated>npx vitest run --project cli src/lib/state/onboard-step-state.test.ts src/lib/onboard/machine/transitions.test.ts src/lib/onboard/machine/final-flow-phases.test.ts src/lib/onboard/machine/core-flow-phases.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - New machine snapshots use runtime_setup only.
    - Legacy openclaw and agent_setup snapshots resume at the equivalent generic state without repeated completed effects.
    - Checkpoint V4 and machine V1 migrate together; no V5 checkpoint can retain a legacy state or step reference.
    - Contradictory duplicate steps, future versions, and inconsistent recovery receipts fail before any effect.
    - The transition graph has one sandbox-to-runtime_setup edge.
    - Current progress wording for each selected runtime remains characterized.
  </acceptance_criteria>
  <done>The persisted machine model has one runtime setup state and a bounded two-label legacy reader.</done>
</task>

<task type="auto">
  <name>Task 2: Run all current runtime setup through one explicit handler</name>
  <files>src/lib/onboard.ts, src/lib/onboard/machine/initial-flow-phases.ts, src/lib/onboard/machine/flow-sequence.ts, src/lib/onboard/machine/flow-phases/agent-policy-finalization.ts, src/lib/onboard/machine/handlers/agent-setup.ts, src/lib/onboard/machine/handlers/sandbox.ts, src/lib/onboard/machine/handlers/agent-setup.test.ts, src/lib/onboard/machine/initial-flow-phases.test.ts, src/lib/onboard/machine/flow-sequence.test.ts, src/lib/onboard/machine/final-flow-phases.runtime.test.ts</files>
  <read_first>.planning/phases/02-explicit-runtime-identity/02-CONTEXT.md, src/lib/agent/onboard.ts, src/lib/onboard.ts, src/lib/onboard/machine/initial-flow-phases.ts, src/lib/onboard/machine/flow-sequence.ts, src/lib/onboard/machine/flow-phases/agent-policy-finalization.ts, src/lib/onboard/machine/handlers/agent-setup.ts, src/lib/onboard/machine/handlers/agent-setup.test.ts, src/lib/onboard/machine/handlers/sandbox.ts, src/lib/onboard/machine/final-flow-phases.runtime.test.ts</read_first>
  <action>D-06: pass the explicit AgentDefinition into one runtime setup handler, register that handler once in `flow-sequence.ts`, and return `runtime_setup` from the sandbox phase for every runtime. Remove the separate OpenClaw and agentSetup handler fields and phase entries from the active sequence. Within the unified handler, preserve the current OpenClaw setup operation and current manifest-driven non-OpenClaw setup operation as temporary internal strategies selected by explicit trusted identity or capability, not nullable presence and not separate machine states. D-08: preserve NemoClaw product workflow, logical binding, required-policy, state-linkage, and rollback responsibilities and the existing OpenShell lifecycle, durable-state, effective-policy, provider/credential, inference, and admitted-execution boundary; keep the three existing descendant wrappers unchanged and move none of those effects into a runtime strategy. Replace `agent?.name || openclaw` and `agent ? agent_setup : openclaw` decisions on this flow with explicit identity. Keep skip and resume bookkeeping equivalent for V1/V4 snapshots.</action>
  <verify><automated>npx vitest run --project cli src/lib/onboard/machine/handlers/agent-setup.test.ts src/lib/onboard/machine/final-flow-phases.runtime.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - OpenClaw, Hermes, and Deep Agents Code enter the same handler with explicit definitions.
    - Runtime-native setup behavior remains the same as the Phase 1 baseline.
    - No credential, policy, OpenShell, registry-commit, or rollback authority moves into runtime strategy code.
    - Resume does not repeat a completed legacy setup step.
  </acceptance_criteria>
  <done>Null presence no longer chooses onboarding execution flow.</done>
</task>

<task type="auto">
  <name>Task 3: Prove branch collapse and legacy resume parity</name>
  <files>src/lib/state/onboard-checkpoint.test.ts, src/lib/state/onboard-checkpoint-migrate.test.ts, src/lib/onboard/machine/core-flow-phases.test.ts, src/lib/onboard/machine/initial-flow-phases.test.ts, src/lib/onboard/machine/flow-sequence.test.ts, src/lib/onboard/machine/final-flow-phases.test.ts, src/lib/onboard/machine/final-flow-phases.runtime.test.ts, src/lib/onboard/machine/transitions.test.ts, src/lib/onboard/machine/transition-traces.test.ts, src/lib/onboard/agent-selection.test.ts, src/lib/onboard/agent-resume-state.test.ts</files>
  <read_first>.planning/phases/02-explicit-runtime-identity/02-CONTEXT.md, .planning/phases/01-scope-and-behavior-baseline/01-02-SUMMARY.md, src/lib/state/onboard-checkpoint.test.ts, src/lib/state/onboard-checkpoint-migrate.test.ts, src/lib/onboard/machine/core-flow-phases.test.ts, src/lib/onboard/machine/initial-flow-phases.test.ts, src/lib/onboard/machine/flow-sequence.test.ts, src/lib/onboard/machine/final-flow-phases.test.ts, src/lib/onboard/machine/final-flow-phases.runtime.test.ts, src/lib/onboard/machine/transitions.test.ts, src/lib/onboard/machine/transition-traces.test.ts, src/lib/onboard/agent-selection.test.ts, src/lib/onboard/agent-resume-state.test.ts</read_first>
  <action>D-10: update characterization expectations only for explicit OpenClaw identity, schema versions, and `runtime_setup`. Add table-driven traces for fresh and resumed OpenClaw, Hermes, and Deep Agents Code; every valid combination of session V1/V2, machine V1/V2, and checkpoint V4/V5; legacy `openclaw` and `agent_setup` at pending, in-progress, complete, and failed steps; cancellation and recovery; and unknown recorded identity. Assert resulting state, migrated pointers and receipt IDs, invoked and skipped effects, registry identity, and unchanged selection and prompt behavior. Add negative cases for contradictory step aliases, inconsistent session/checkpoint state, unsupported future versions, read denial, malformed state, and an unknown identity that `loadSession`, resume resolution, and save all propagate. Add a static assertion that production writers and active phase/sequence registries no longer emit `openclaw` or `agent_setup` as machine destinations.</action>
  <verify><automated>npx vitest run --project cli src/lib/onboard/machine/core-flow-phases.test.ts src/lib/onboard/machine/final-flow-phases.test.ts src/lib/onboard/machine/final-flow-phases.runtime.test.ts src/lib/onboard/machine/transitions.test.ts src/lib/onboard/machine/transition-traces.test.ts src/lib/onboard/agent-selection.test.ts src/lib/onboard/agent-resume-state.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - Fresh and resumed traces cover all three standard runtimes.
    - Both legacy state labels migrate at every meaningful step status.
    - Session V1, machine V1, and checkpoint V4 fixtures migrate once and save as V2/V2/V5 without replay.
    - Unknown identity stops before any setup effect.
    - The Phase 1 selection and prompt outcomes remain unchanged.
  </acceptance_criteria>
  <done>Tests prove one runtime-neutral branch and preserve legacy resume behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Persisted machine snapshot to resumed effects | Incorrect state translation can replay privileged or destructive onboarding steps. |
| Generic flow to runtime strategy | Identity unification must not give runtime-native code core authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-07 | Tampering | Legacy machine state | mitigate | Validate compatible step evidence before translating to runtime_setup. |
| T-02-08 | Elevation of Privilege | Unified setup handler | mitigate | Keep product decisions in NemoClaw and lifecycle, enforcement, credential custody, and admitted execution in OpenShell-owned dependencies; do not expand the three pinned-version descendant wrappers. |
| T-02-09 | Repudiation | Resume effect history | mitigate | Translate every step pointer and recovery receipt consistently and test no-repeat behavior across both legacy labels. |
| T-02-11 | Tampering | Mixed checkpoint versions | mitigate | Reject contradictory V1/V2 and V4/V5 state instead of guessing or resetting. |
</threat_model>

<verification>
- [ ] All three focused test commands pass in less than 60 seconds each.
- [ ] Production code creates no new openclaw or agent_setup machine destination.
- [ ] New state writes use session V2, machine V2, and checkpoint V5; all supported legacy combinations have no-replay tests.
- [ ] npm run typecheck:cli passes after the full phase change.
</verification>

<success_criteria>
- Active and persisted paths carry explicit runtime identity.
- One runtime_setup branch serves every standard runtime.
- Legacy sessions resume from their recorded progress and unknown identities fail closed.
</success_criteria>

<output>
Create .planning/phases/02-explicit-runtime-identity/02-03-SUMMARY.md when done.
</output>
