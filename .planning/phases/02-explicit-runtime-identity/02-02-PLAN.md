---
phase: 02-explicit-runtime-identity
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/lib/state/onboard-session.ts
  - src/lib/state/onboard-session-normalization.test.ts
  - src/lib/state/onboard-session-persistence.test.ts
  - src/lib/onboard/agent-resume-state.ts
  - src/lib/onboard/agent-resume-state.test.ts
  - src/lib/state/registry/types.ts
  - src/lib/state/registry-normalization.ts
  - src/lib/state/registry-normalization.test.ts
  - src/lib/onboard/sandbox-agent.ts
  - src/lib/onboard/sandbox-agent.test.ts
  - src/lib/onboard/sandbox-registry-metadata.ts
  - src/lib/onboard/sandbox-registry-metadata.test.ts
autonomous: true
requirements: [UX-03, STATE-01]
user_setup: []
must_haves:
  truths:
    - "D-02: Legacy absent or null persisted identities normalize to openclaw."
    - "D-03: New session and completed sandbox writes always record a canonical nonempty identity."
    - "D-04: Explicit unknown persisted identities stop instead of being rewritten."
    - "D-09: Session V1 reads migrate to V2, new writes use V2, and load or save never swallows an unknown identity or future version."
  artifacts:
    - path: src/lib/state/onboard-session.ts
      provides: Explicit V2 session schema, V1 reader migration, fail-closed errors, and explicit new writes.
    - path: src/lib/state/registry-normalization.ts
      provides: Bounded completed-sandbox identity migration and fail-closed validation.
    - path: src/lib/onboard/sandbox-registry-metadata.ts
      provides: Explicit identity preservation on reused sandbox writes.
  key_links:
    - from: src/lib/state/onboard-session.ts
      to: src/lib/agent/runtime-identity.ts
      via: Legacy normalization and canonical validation.
    - from: src/lib/onboard/sandbox-registry-metadata.ts
      to: src/lib/state/registry/types.ts
      via: Completed sandbox write contract.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Migrate persisted session and sandbox identity without breaking legacy resume.

Purpose: Make new durable state explicit while keeping pre-migration OpenClaw state usable and rejecting unknown identities.
Output: Version-bounded session and registry normalization plus explicit write paths and regression fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-explicit-runtime-identity/02-CONTEXT.md
@.planning/phases/02-explicit-runtime-identity/02-01-SUMMARY.md
@src/lib/state/onboard-session.ts
@src/lib/state/registry/types.ts
@src/lib/state/registry-normalization.ts
@src/lib/onboard/sandbox-agent.ts
@src/lib/onboard/sandbox-registry-metadata.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate onboard session identity at read and write boundaries</name>
  <files>src/lib/state/onboard-session.ts, src/lib/state/onboard-session-normalization.test.ts, src/lib/state/onboard-session-persistence.test.ts, src/lib/onboard/agent-resume-state.ts, src/lib/onboard/agent-resume-state.test.ts</files>
  <read_first>.planning/phases/02-explicit-runtime-identity/02-CONTEXT.md, src/lib/agent/runtime-identity.ts, src/lib/state/onboard-session.ts, src/lib/state/onboard-session-normalization.test.ts, src/lib/state/onboard-session-persistence.test.ts, src/lib/onboard/agent-resume-state.ts, src/lib/onboard/agent-resume-state.test.ts</read_first>
  <action>D-02 and D-09: increment `SESSION_VERSION` from 1 to 2. Keep a named V1 parser that accepts only the historical V1 shape and translates absent or null `agent` to openclaw. Add a strict V2 parser that requires a canonical nonempty identity. New sessions and every save or update path write V2 and never write null. Preserve every unrelated session, machine snapshot, checkpoint, messaging, credential-reference, and step field, perform no disk write during read, and write the migrated form only on the next already-authorized save. D-04: reject a nonempty unknown recorded identity through the typed identity error and core-owned remediation. Refactor `loadSession` to treat only an absent session file as no session; do not catch and convert typed identity, unsupported-version, permission, I/O, or parse failures to null. Refactor `saveSession` to propagate normalization failure instead of calling `createSession()`. Ensure resume callers preserve those errors and begin no onboarding effect. Add fixtures for V1 absent, V1 null, valid V1 identities, valid V2 identities, unknown identity in each version, malformed state, future version, read denial, and repeated V1-read/V2-save/V2-read cycles.</action>
  <verify><automated>npx vitest run --project cli src/lib/state/onboard-session-normalization.test.ts src/lib/state/onboard-session-persistence.test.ts src/lib/onboard/agent-resume-state.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - New sessions contain agent: openclaw without caller overrides.
    - Legacy null and absent sessions resume as OpenClaw.
    - Unknown nonempty identities fail with remediation.
    - Unknown identity, future version, parse, permission, and I/O errors cannot become a missing session or fresh session.
    - Normalization is idempotent and preserves unrelated fields byte-for-value where their schema allows.
  </acceptance_criteria>
  <done>Onboard session state has an explicit write contract and a bounded legacy reader.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate completed sandbox registry identity</name>
  <files>src/lib/state/registry/types.ts, src/lib/state/registry-normalization.ts, src/lib/state/registry-normalization.test.ts, src/lib/onboard/sandbox-agent.ts, src/lib/onboard/sandbox-agent.test.ts, src/lib/onboard/sandbox-registry-metadata.ts, src/lib/onboard/sandbox-registry-metadata.test.ts</files>
  <read_first>.planning/phases/02-explicit-runtime-identity/02-CONTEXT.md, src/lib/agent/runtime-identity.ts, src/lib/state/registry/types.ts, src/lib/state/registry-normalization.ts, src/lib/state/registry-normalization.test.ts, src/lib/onboard/sandbox-agent.ts, src/lib/onboard/sandbox-agent.test.ts, src/lib/onboard/sandbox-registry-metadata.ts, src/lib/onboard/sandbox-registry-metadata.test.ts</read_first>
  <action>D-02: treat absent or null agent on an existing completed or reusable sandbox row as legacy OpenClaw. Keep pre-sandbox pendingRouteReservation rows identity-free because they do not yet bind runtime code. D-03: require getSandboxAgentRegistryFields, initial registration, reused metadata, rebuild, and update paths to write the selected canonical identity explicitly. D-04: reject unknown nonempty registry identity before lookup or mutation. D-09: preserve unrelated registry fields and the prior identity on reuse, normalize only at the registry boundary, and make repeat normalization stable. Test full rows, pending reservations, exact known identities, unknown text, partial updates, and unrelated messaging, MCP, policy, port, workload, and lifecycle fields.</action>
  <verify><automated>npx vitest run --project cli src/lib/state/registry-normalization.test.ts src/lib/onboard/sandbox-agent.test.ts src/lib/onboard/sandbox-registry-metadata.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - Every new completed sandbox row contains a canonical runtime identity.
    - A legacy completed row without identity reads as OpenClaw and becomes explicit only on authorized write.
    - A pending route reservation remains distinguishable from a completed sandbox.
    - Unknown identity prevents reuse, rebuild, connect, and mutation.
  </acceptance_criteria>
  <done>Registry state distinguishes pre-sandbox reservations from explicit completed runtime identity and fails closed for unknown rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User-writable JSON to trusted state | Session and registry files influence runtime selection and lifecycle mutation. |
| Read migration to durable write | A reader must not silently rewrite or discard unrelated state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-04 | Tampering | Session and registry identity | mitigate | Validate canonical identities and reject explicit unknown values before mutation. |
| T-02-05 | Spoofing | Absent registry identity | mitigate | Translate only the historically valid absent or null shape on completed legacy rows. |
| T-02-06 | Denial of Service | Migration rewrite | mitigate | Keep normalization pure and idempotent; persist only through existing authorized save paths. |
| T-02-10 | Tampering | Session load fallback | mitigate | Catch only absence and propagate unknown identity, future version, parse, permission, and I/O errors. |
</threat_model>

<verification>
- [ ] Focused tests pass in less than 60 seconds.
- [ ] New write fixtures never contain a null runtime identity.
- [ ] Unknown persisted identities have a deterministic fail-closed test.
- [ ] V1 fixtures read without mutation, save as V2, and retain all unrelated state.
</verification>

<success_criteria>
- Legacy OpenClaw sessions and sandboxes resume unchanged.
- New session and completed registry state always records exact canonical runtime identity.
</success_criteria>

<output>
Create .planning/phases/02-explicit-runtime-identity/02-02-SUMMARY.md when done.
</output>
