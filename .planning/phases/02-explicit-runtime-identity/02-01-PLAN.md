---
phase: 02-explicit-runtime-identity
plan: 01
type: execute
wave: 1
depends_on: ["01-04"]
files_modified:
  - src/lib/agent/runtime-identity.ts
  - src/lib/agent/runtime-identity.test.ts
  - src/lib/agent/defs.ts
  - src/lib/agent/defs.test.ts
  - src/lib/agent/onboard.ts
  - src/lib/agent/onboard.test.ts
  - src/lib/onboard/agent-selection.ts
  - src/lib/onboard/agent-selection.test.ts
  - src/lib/agent/runtime.ts
  - src/lib/agent/runtime.test.ts
autonomous: true
requirements: [UX-03, STATE-01]
user_setup: []
must_haves:
  truths:
    - "D-01: Active selection and trusted lookup return an explicit definition for OpenClaw, Hermes, and Deep Agents Code."
    - "D-04: Unknown explicit identities fail closed with source-specific remediation."
    - "D-05: The menu, OpenClaw default, aliases, and non-interactive behavior remain unchanged."
    - "D-07: Default and alias metadata remain explicit core-owned data."
  artifacts:
    - path: src/lib/agent/runtime-identity.ts
      provides: Canonical default, persisted-identity normalization, typed unknown-identity errors, and trusted definition loading.
    - path: src/lib/onboard/agent-selection.ts
      provides: Non-null explicit selection for every menu and non-interactive path.
    - path: src/lib/agent/runtime.ts
      provides: Fail-closed explicit session and registry runtime lookup.
  key_links:
    - from: src/lib/onboard/agent-selection.ts
      to: src/lib/agent/runtime-identity.ts
      via: Canonical explicit definition resolution.
    - from: src/lib/agent/runtime.ts
      to: src/lib/agent/defs.ts
      via: Trusted manifest inventory and loadAgent.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Make runtime selection and trusted lookup explicit and fail closed.

Purpose: Remove the active null-means-OpenClaw convention before package identity and receipts are introduced.
Output: One identity helper plus explicit AgentDefinition results from selection, onboard resolution, session lookup, and registry lookup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-explicit-runtime-identity/02-CONTEXT.md
@.planning/phases/01-scope-and-behavior-baseline/01-04-SUMMARY.md
@src/lib/agent/defs.ts
@src/lib/agent/onboard.ts
@src/lib/agent/runtime.ts
@src/lib/onboard/agent-selection.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the canonical explicit identity boundary</name>
  <files>src/lib/agent/runtime-identity.ts, src/lib/agent/runtime-identity.test.ts, src/lib/agent/defs.ts, src/lib/agent/defs.test.ts</files>
  <read_first>.planning/phases/02-explicit-runtime-identity/02-CONTEXT.md, src/lib/agent/aliases.ts, src/lib/agent/defs.ts, src/lib/agent/defs.test.ts, src/lib/agent/manifest-readers.ts, agents/openclaw/manifest.yaml, agents/hermes/manifest.yaml, agents/langchain-deepagents-code/manifest.yaml</read_first>
  <action>D-01: define DEFAULT_AGENT_RUNTIME_ID as openclaw and expose narrow helpers that canonicalize trusted runtime names and load an explicit AgentDefinition. D-04: create a typed unknown-runtime error carrying only source kind, rejected value, allowed canonical names, and core-owned remediation; change resolveAgentName so an unknown recorded session identity throws instead of warning and returning OpenClaw. D-07: continue using the existing alias table and manifests for default, aliases, display data, and candidate qualification. Cover canonical names, every current alias, default input, candidate gates, control characters, path-shaped values, and unknown flag, environment, session, and registry sources.</action>
  <verify><automated>npx vitest run --project cli src/lib/agent/runtime-identity.test.ts src/lib/agent/defs.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - The canonical default constant has one definition.
    - Alias resolution is data-driven and still returns the current canonical names.
    - Unknown recorded identity no longer logs a warning and returns OpenClaw.
    - Error output contains remediation and no untrusted terminal controls.
  </acceptance_criteria>
  <done>Core has one tested, explicit, fail-closed runtime identity boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Return explicit definitions from selection and lookup</name>
  <files>src/lib/agent/onboard.ts, src/lib/agent/onboard.test.ts, src/lib/onboard/agent-selection.ts, src/lib/onboard/agent-selection.test.ts, src/lib/agent/runtime.ts, src/lib/agent/runtime.test.ts</files>
  <read_first>.planning/phases/02-explicit-runtime-identity/02-CONTEXT.md, src/lib/agent/runtime-identity.ts, src/lib/agent/onboard.ts, src/lib/agent/onboard.test.ts, src/lib/onboard/agent-selection.ts, src/lib/onboard/agent-selection.test.ts, src/lib/agent/runtime.ts, src/lib/agent/runtime.test.ts, .planning/phases/01-scope-and-behavior-baseline/01-02-SUMMARY.md</read_first>
  <action>D-01: change resolveAgent, createSelectOnboardAgent, getSessionAgent, and getRegisteredAgent to return AgentDefinition for OpenClaw as well as other known runtimes. Remove the interactive OpenClaw-to-null conversion and catch-to-null lookup behavior. D-04: distinguish absent legacy input, which becomes OpenClaw at the migration helper, from an explicit unknown value, which throws before runtime-specific work. D-05: keep current menu ordering, prompt, note, alias, candidate, health, gateway, display, and policy outcomes; update Phase 1 tests only where explicit OpenClaw replaces null. Leave helper input unions tolerant of null only where a downstream Phase 2 plan still reads legacy state.</action>
  <verify><automated>npx vitest run --project cli src/lib/agent/onboard.test.ts src/lib/onboard/agent-selection.test.ts src/lib/agent/runtime.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - Selecting or loading OpenClaw returns the OpenClaw AgentDefinition.
    - Registry and session lookup reject an explicit unknown identity.
    - Default, flag, environment, resume, interactive, and non-interactive paths select the same canonical runtime as the baseline.
    - Candidate qualification behavior remains enforced.
  </acceptance_criteria>
  <done>No active selection or trusted lookup uses null to mean OpenClaw.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User or persisted text to manifest loader | A runtime name can become a filesystem lookup and executable behavior selector. |
| Compatibility alias to canonical runtime | Aliases must not bypass candidate or qualification gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | Spoofing | Runtime identity | mitigate | Canonicalize against the trusted manifest inventory and reject unknown explicit values. |
| T-02-02 | Path Traversal | Manifest lookup | mitigate | Never pass unvalidated persisted or user text to loadAgent. |
| T-02-03 | Elevation of Privilege | Candidate aliases | mitigate | Preserve candidate qualification after alias canonicalization. |
</threat_model>

<verification>
- [ ] Focused tests pass in less than 60 seconds.
- [ ] rg finds no OpenClaw-to-null return in agent-selection.ts, agent/onboard.ts, or agent/runtime.ts.
- [ ] TypeScript callers receive an explicit definition without weakening unknown-identity handling.
</verification>

<success_criteria>
- OpenClaw is represented by the same explicit definition shape as the other standard runtimes.
- Unknown explicit identities stop before selecting or loading runtime behavior.
</success_criteria>

<output>
Create .planning/phases/02-explicit-runtime-identity/02-01-SUMMARY.md when done.
</output>
