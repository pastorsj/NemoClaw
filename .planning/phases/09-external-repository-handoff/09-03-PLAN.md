---
phase: 09-external-repository-handoff
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - ../nemoclaw-deepagents-code/
  - test/package-contract/runtime-packages/external-deepagents-code.test.ts
autonomous: false
requirements: [DIST-02, DIST-03, DIST-04, DIST-05, PKG-06, PKG-07, CTL-05]
user_setup:
  - service: GitHub
    why: Publish the accepted Deep Agents Code package repository and enable its trusted release workflow.
    dashboard_config:
      - task: Create or provide the accepted repository and trusted publishing environment.
        location: Repository named by EXTERNAL-DISTRIBUTION-DECISION.md
must_haves:
  truths:
    - "D-06: Deep Agents Code is the first external handoff and must qualify before managed Hermes or OpenClaw moves."
    - Deep Agents Code builds, tests, and publishes outside NemoClaw without contract changes.
    - NemoClaw qualifies its exact external artifact before selecting it.
  artifacts:
    - ../nemoclaw-deepagents-code/agent-runtime-package.yaml
    - test/package-contract/runtime-packages/external-deepagents-code.test.ts
  key_links:
    - The package-contract test verifies the exact external artifact and records evidence for the later reviewed static release-set change; the shipped release remains Phase 8.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Hand off the qualified Deep Agents Code package to its external repository without changing its contract or behavior.

Purpose: Prove the repository handoff with the first and least coupled standard package.
Output: An independent package checkout, publication workflow, exact external-artifact test, and central qualification evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-external-repository-handoff/09-CONTEXT.md
@.planning/phases/04-deep-agents-code-pilot/04-04-SUMMARY.md
@.planning/phases/08-in-tree-release-qualification/08-05-SUMMARY.md
@proposals/agent-runtime-packages/EXTERNAL-DISTRIBUTION-DECISION.md
@packages/nemoclaw-deepagents-code/
@packages/agent-runtime-contract/repository-template/package-contract.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Materialize and validate the independent Deep Agents Code repository</name>
  <files>../nemoclaw-deepagents-code/</files>
  <read_first>.planning/phases/09-external-repository-handoff/09-CONTEXT.md, proposals/agent-runtime-packages/EXTERNAL-DISTRIBUTION-DECISION.md, packages/nemoclaw-deepagents-code/agent-runtime-package.yaml, packages/agent-runtime-contract/repository-template/README.md</read_first>
  <action>Per D-03 and D-06, materialize the complete qualified package root only in the exact sibling checkout authorized by 09-01; treat the whole sibling repository as this task's mutation closure and do not modify a different checkout, remote, branch, or repository. D-08: preserve the canonical Phase 8 package-content manifest and archive bytes exactly, including `dcode-session-supervisor.py`. D-11: keep that script as the one package-local Deep Agents Code descendant wrapper; it may manage only descendants of the admitted process and may not call OpenShell or own sandbox lifecycle. Add repository-owned CI, contribution, provenance-envelope, retention, and incident files only outside the canonical package manifest. Independently regenerate and compare canonical manifests and archives; any packaged source, descriptor, helper, test, lockfile, mode, or archive-byte difference requires a new package version and normal qualification instead of handoff equivalence. Add the accepted independent build, type-check, unit, image, conformance, provenance, software-bill-of-materials, and publication workflow. Run the repository verifier without allowing the new workflow to select NemoClaw central suites.</action>
  <verify><automated>npm --prefix ../nemoclaw-deepagents-code test</automated></verify>
  <acceptance_criteria>
    - The external checkout imports or copies no NemoClaw source outside its package.
    - Descriptor and protocol compatibility match the qualified in-tree package.
    - Canonical package-content manifests and archive digests are byte-equivalent to Phase 8; only accepted repository-only files differ.
    - Repository CI emits every accepted exact evidence artifact.
  </acceptance_criteria>
  <done>The local external Deep Agents Code repository passes standalone and shared repository-contract checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Qualify the exact external artifact through NemoClaw</name>
  <files>test/package-contract/runtime-packages/external-deepagents-code.test.ts</files>
  <read_first>release/agent-runtime-packages.json, ../nemoclaw-deepagents-code/agent-runtime-package.yaml, packages/agent-runtime-contract/scripts/verify-repository-package.mts, test/package-contract/managed-image-registry-transport.test.ts, .planning/phases/08-in-tree-release-qualification/08-05-SUMMARY.md</read_first>
  <behavior>
    - An exact external Deep Agents Code artifact passes descriptor, image, protocol, provenance, and conformance verification.
    - Any mismatch produces no accepted handoff evidence and leaves the shipped Phase 8 release unchanged.
  </behavior>
  <action>Per D-04, test the accepted immutable external coordinate and its complete evidence envelope directly; do not create a second release-set file or mutate `release/agent-runtime-packages.json` in this plan. Bind the evidence to exact transport coordinate, canonical package/archive digest, descriptor, image index, platform image, helper, provenance, and suite identities. CTL-05: preserve repository results as publisher claims and require separately produced NemoClaw verification evidence with producer identity and limitations. PKG-07: record the immutable qualification evidence reference for the final reviewed Phase 9 release-set pull request, but do not change product support here.</action>
  <verify><automated>npx vitest run --project package-contract test/package-contract/runtime-packages/external-deepagents-code.test.ts</automated></verify>
  <acceptance_criteria>
    - Verification failure produces no accepted handoff evidence and cannot change the shipped release.
    - The static release set, installed receipts, and running workloads remain byte-for-byte unchanged.
    - The test detects any exact-identity mismatch.
    - No package-controlled callback or suite selection is evaluated.
  </acceptance_criteria>
  <done>NemoClaw can independently qualify the exact external Deep Agents Code publication while retaining rollback to Phase 8.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Confirm trusted publication and central lifecycle evidence</name>
  <files>None</files>
  <decision>Does an authorized maintainer accept this exact external Deep Agents Code publication and central evidence as the first handoff artifact?</decision>
  <context>Managed Hermes work cannot begin until the first external package has an immutable accepted identity and evidence record. A failed, deferred, or rejected result stops Phase 9 and leaves the Phase 8 release shipped.</context>
  <action>Pause handoff acceptance until a maintainer reviews the exact trusted publication and central lifecycle evidence described below. Record the accepted external tuple and immutable evidence digest for the one final static release-set pull request in 09-06. Make no product-support or release-set change here. Any missing, failed, or rejected result stops Phase 9 before 09-04; do not create an acceptance summary that can satisfy the next plan.</action>
  <what-built>The independent Deep Agents Code package repository and its exact verified external artifact.</what-built>
  <how-to-verify>Confirm the accepted repository published the tested artifact with trusted provenance. Dispatch the existing central Deep Agents Code no-messaging lifecycle target against that exact artifact. Verify install, onboard, inference, restart, snapshot and restore, rebuild, cleanup, and rollback evidence.</how-to-verify>
  <options>
    <option id="accept">Accept the exact artifact and evidence digest for the next handoff and final reviewed release-set change.</option>
    <option id="reject">Reject it and stop Phase 9 with Phase 8 authoritative.</option>
    <option id="defer">Defer it and leave the next handoff blocked.</option>
  </options>
  <resume-signal>Provide the accepted external artifact identity and passing central run URL. A failed or rejected gate terminates this handoff attempt and cannot resume into 09-04.</resume-signal>
  <verify><automated>npm run test:e2e-phases:check</automated></verify>
  <done>The exact external Deep Agents Code artifact has accepted lifecycle evidence and an immutable acceptance record usable by 09-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Deep Agents Code repository to NemoClaw release set | External publication evidence can influence standard runtime selection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-05 | Spoofing | Package publisher | mitigate | Require accepted repository identity, trusted provenance, and exact artifact verification. |
| T-09-06 | Denial of service | External fetch or qualification | mitigate | Retain the Phase 8 in-tree tuple on every failure. |
| T-09-16 | Tampering | Sibling repository contents | mitigate | Limit mutation to the authorized sibling root and require canonical manifest and archive equality with Phase 8. |
</threat_model>

<verification>
- [ ] Standalone repository tests pass.
- [ ] Package-contract verification passes for the exact external artifact.
- [ ] Central lifecycle evidence binds the same exact identity.
- [ ] Phase 8 rollback remains selectable until final release-set switch.
</verification>

<success_criteria>
- Deep Agents Code is independently published without a contract change.
- NemoClaw, not the package repository, decides qualification and whether a static release-set change can merge and ship.
</success_criteria>

<output>
Create `.planning/phases/09-external-repository-handoff/09-03-SUMMARY.md` when done.
</output>
