---
phase: 09-external-repository-handoff
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - packages/agent-runtime-contract/repository-template/README.md
  - packages/agent-runtime-contract/repository-template/package-contract.json
  - packages/agent-runtime-contract/scripts/verify-repository-package.mts
  - packages/agent-runtime-contract/tests/repository-package.test.ts
autonomous: true
requirements: [DIST-02, DIST-03, DIST-05, PKG-07, SEC-04, CTL-05]
must_haves:
  truths:
    - "D-03: Every external package repository preserves the in-tree descriptor, controller protocol, tests, and image behavior."
    - "D-04: Every external package repository proves exact package and platform-image identities, provenance, and central conformance outputs."
    - "D-05: Discovery stays explicit and a package repository cannot add host callbacks, imported Python entry points, or central E2E cases through package metadata."
    - A handoff-equivalent publication has the same canonical package-content manifest and archive bytes as Phase 8; repository-only files are explicitly excluded and cannot enter the package digest.
    - Publication claims, independent observations, immutable qualification evidence, and static release-set support status are distinct; a package repository cannot assert product support.
  artifacts:
    - packages/agent-runtime-contract/repository-template/package-contract.json
    - packages/agent-runtime-contract/scripts/verify-repository-package.mts
  key_links:
    - The repository verifier validates published evidence against the frozen package descriptor and controller contract.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Define and verify the repository-level publication contract shared by all three external packages.

Purpose: Make repository handoff repeatable without giving package repositories control over NemoClaw host execution or central qualification selection.
Output: A repository template, machine-readable publication contract, verifier, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-external-repository-handoff/09-CONTEXT.md
@proposals/agent-runtime-packages/EXTERNAL-DISTRIBUTION-DECISION.md
@packages/agent-runtime-contract/
@.github/workflows/managed-images.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the independent repository publication contract</name>
  <files>packages/agent-runtime-contract/repository-template/README.md, packages/agent-runtime-contract/repository-template/package-contract.json, packages/agent-runtime-contract/tests/repository-package.test.ts</files>
  <read_first>.planning/phases/09-external-repository-handoff/09-CONTEXT.md, proposals/agent-runtime-packages/EXTERNAL-DISTRIBUTION-DECISION.md, packages/agent-runtime-contract/schemas/agent-runtime-package-v1.schema.json, .github/workflows/managed-images.yaml</read_first>
  <behavior>
    - A complete repository record declares package tests, conformance, exact images, provenance, software bill of materials, compatibility, retention, and central evidence.
    - A record that declares host hooks, central suite IDs, mutable images, or missing evidence is rejected.
  </behavior>
  <action>Per D-03 through D-05, add the repository contract and explanatory template. Make package-native commands and outputs explicit. D-08: include the canonical Phase 8 content-manifest schema, normalized modes and paths, canonical archive algorithm, digest, and exact repository-only exclusion list. A repository may wrap those bytes in a provenance envelope but cannot change packaged bytes while claiming handoff equivalence. CTL-05: classify repository-produced results as executor or publisher claims and reserve independent-observation and qualification fields to NemoClaw-owned evidence. PKG-07: prohibit package-controlled support status, order, alias, default, revocation, or supersession fields. SEC-04: declare exactly the mutation-capable `reconcile-native` and `prepare-native-state` helper operations and require evidence that unknown operations and writes outside their declared scopes fail qualification. Contract V1 adds no read-only helper or before/after enforcement. D-11: identify the one existing descendant wrapper in each package as a compatibility implementation detail, not helper or sandbox-lifecycle authority. Prohibit host callbacks, GitHub workflow injection into NemoClaw, package-selected central suite IDs, mutable artifact identity, and ambient Python discovery. Write failing valid and invalid fixture assertions before the schema record.</action>
  <verify><automated>npx vitest run --project cli packages/agent-runtime-contract/tests/repository-package.test.ts</automated></verify>
  <acceptance_criteria>
    - The contract names every artifact and evidence output accepted in 09-01.
    - Invalid authority expansion and mutable identity cases fail.
    - Repository-specific details do not change the agent runtime package descriptor.
    - A packaged-byte, mode, path, lockfile, helper, descriptor, or test change fails handoff equivalence and requires a new release identity.
  </acceptance_criteria>
  <done>The repository publication contract is executable and rejects every prohibited package authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the repository package verifier</name>
  <files>packages/agent-runtime-contract/scripts/verify-repository-package.mts, packages/agent-runtime-contract/tests/repository-package.test.ts</files>
  <read_first>packages/agent-runtime-contract/repository-template/package-contract.json, packages/agent-runtime-contract/tests/repository-package.test.ts, proposals/agent-runtime-packages/EXTERNAL-DISTRIBUTION-DECISION.md</read_first>
  <behavior>
    - The verifier accepts a complete exact publication bundle copied outside NemoClaw whose canonical content digest and archive bytes equal the Phase 8 baseline.
    - The verifier rejects a digest, platform, provenance, contract, or test-evidence mismatch.
  </behavior>
  <action>Per D-04, implement a credential-free verifier that consumes an explicit package repository root and validates exact package, descriptor, image-index, platform-image, helper, test-suite, provenance, and software-bill-of-materials identities. D-08: independently regenerate the canonical package-content manifest and archive from the explicit root, exclude only the accepted repository-only paths, and require byte-for-byte manifest and archive digest equality with Phase 8 for a handoff-equivalent publication. Any content difference must present a new version and enter normal qualification. CTL-05: retain publisher claims as claims and require separately supplied NemoClaw-owned observation evidence with producer and limitations. SEC-04: validate the two declared mutation-capable operations and their declared scopes without trusting package self-attestation. D-11: verify that the canonical package bytes retain exactly the package's accepted descendant wrapper and that repository metadata grants it no OpenShell or sandbox-lifecycle authority. Do not execute repository scripts during evidence validation. Emit bounded structured diagnostics.</action>
  <verify><automated>npx vitest run --project cli packages/agent-runtime-contract/tests/repository-package.test.ts</automated></verify>
  <acceptance_criteria>
    - Verification performs no package-supplied process execution.
    - Every accepted identity is exact and cross-checked.
    - Canonical manifest and archive equality is checked independently; repository-only additions neither perturb nor enter the package digest.
    - Publisher claims cannot create qualification or product support and cannot label themselves independent evidence.
    - Failure diagnostics contain no package-controlled free text.
  </acceptance_criteria>
  <done>A single verifier can qualify the publication envelope from any of the three package repositories.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| External repository evidence to NemoClaw qualification | Package-controlled records enter a core-owned release decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-03 | Elevation of privilege | Repository metadata | mitigate | Reject host hooks, central-suite injection, and package-controlled workflow execution. |
| T-09-04 | Tampering | Publication evidence | mitigate | Cross-check exact digests, platform identities, provenance, and contract versions without executing package code. |
| T-09-14 | Spoofing | Handoff equivalence | mitigate | Regenerate canonical content and archive bytes and compare them with the immutable Phase 8 baseline. |
| T-09-15 | Spoofing | Package evidence authority | mitigate | Preserve claim provenance and reserve independent observation, qualification, and static support status to NemoClaw. |
</threat_model>

<verification>
- [ ] Repository contract tests cover valid and hostile records.
- [ ] The verifier runs against a copied-outside-repository fixture.
- [ ] No verifier path imports or executes package code.
</verification>

<success_criteria>
- All package repositories publish one bounded, independently verifiable evidence envelope.
- Package repositories cannot select or inject NemoClaw host or central E2E execution.
</success_criteria>

<output>
Create `.planning/phases/09-external-repository-handoff/09-02-SUMMARY.md` when done.
</output>
