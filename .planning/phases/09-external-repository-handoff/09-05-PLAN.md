---
phase: 09-external-repository-handoff
plan: 05
type: execute
wave: 5
depends_on: ["09-04"]
files_modified:
  - ../nemoclaw-openclaw/
  - test/package-contract/runtime-packages/external-openclaw.test.ts
autonomous: false
requirements: [DIST-02, DIST-03, DIST-04, DIST-05, PKG-06, PKG-07, CTL-05]
user_setup:
  - service: GitHub
    why: Publish the accepted OpenClaw package and plugin repository with trusted evidence.
    dashboard_config:
      - task: Create or provide the accepted repository and trusted publishing environment.
        location: Repository named by EXTERNAL-DISTRIBUTION-DECISION.md
must_haves:
  truths:
    - OpenClaw and the NemoClaw OpenClaw plugin publish independently with unchanged default behavior.
    - NemoClaw qualifies the exact external image and plugin artifacts before default selection.
  artifacts:
    - ../nemoclaw-openclaw/agent-runtime-package.yaml
    - test/package-contract/runtime-packages/external-openclaw.test.ts
  key_links:
    - The handoff evidence binds the external runtime image and uniquely identified OpenClaw plugin artifacts qualified together while the shipped Phase 8 default remains unchanged.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Hand off the qualified OpenClaw runtime and NemoClaw OpenClaw plugin without changing default onboarding behavior.

Purpose: Complete independent publication for the default and largest standard package.
Output: Independent OpenClaw repository, exact runtime/plugin artifact test, compiled-plugin contract, and central lifecycle evidence gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-external-repository-handoff/09-CONTEXT.md
@.planning/phases/07-openclaw-extraction/07-08-SUMMARY.md
@.planning/phases/09-external-repository-handoff/09-04-SUMMARY.md
@proposals/agent-runtime-packages/EXTERNAL-DISTRIBUTION-DECISION.md
@packages/nemoclaw-openclaw/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Materialize and validate the OpenClaw package repository</name>
  <files>../nemoclaw-openclaw/</files>
  <read_first>.planning/phases/09-external-repository-handoff/09-CONTEXT.md, packages/nemoclaw-openclaw/agent-runtime-package.yaml, .planning/phases/07-openclaw-extraction/07-08-SUMMARY.md, packages/agent-runtime-contract/repository-template/README.md</read_first>
  <action>Before any mutation, verify the 09-03 and 09-04 summaries contain their accepted exact identities and repository-owned acceptance records; stop Phase 9 if either does not. Per D-03 and D-06, materialize the complete qualified OpenClaw package root, including its plugin project, only in the exact sibling checkout authorized by 09-01; the full sibling root is this task's mutation closure. Do not modify a different checkout, remote, branch, or repository. D-08: preserve the canonical Phase 8 package-content manifest and archive bytes exactly, including `nemoclaw-start.sh`. D-11: keep that script as the one package-local OpenClaw descendant wrapper; it may manage only descendants of the admitted process and may not call OpenShell or own sandbox lifecycle. Add repository-owned CI and provenance-envelope files only outside the package digest; any packaged runtime, plugin, descriptor, helper, test, lockfile, mode, or archive-byte difference is a new release requiring normal qualification. PKG-06: preserve the distinct runtime and OpenClaw TUI plugin package identities and deterministic publish-content paths. Preserve descriptor, helper, plugin manifest, slash-command behavior, and native runtime behavior. Add independent runtime-image, plugin build and type-check, package tests, conformance, provenance, software-bill-of-materials, and publication jobs.</action>
  <verify><automated>npm --prefix ../nemoclaw-openclaw test</automated></verify>
  <acceptance_criteria>
    - Runtime and plugin builds use no NemoClaw checkout source.
    - Plugin package-contract behavior matches Phase 7.
    - Runtime and plugin canonical content manifests and archive digests are byte-equivalent to Phase 8; only accepted repository-only files differ.
    - Publication emits exact identities for both runtime and plugin artifacts.
  </acceptance_criteria>
  <done>The external OpenClaw repository passes standalone runtime, plugin, image, and shared publication checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Qualify the external OpenClaw and plugin tuple</name>
  <files>test/package-contract/runtime-packages/external-openclaw.test.ts</files>
  <read_first>release/agent-runtime-packages.json, ../nemoclaw-openclaw/agent-runtime-package.yaml, test/package-contract/cli/command-registry.test.ts, test/package-contract/managed-image-registry-transport.test.ts, packages/agent-runtime-contract/scripts/verify-repository-package.mts</read_first>
  <behavior>
    - Exact external OpenClaw runtime and plugin artifacts pass central verification as one immutable tuple.
    - A runtime, plugin, image, protocol, or provenance mismatch produces no accepted handoff evidence and preserves the shipped Phase 8 default tuple.
  </behavior>
  <action>Per D-04 and D-07, verify the accepted immutable external OpenClaw runtime/plugin tuple directly; do not create a second release-set file or modify `release/agent-runtime-packages.json` in this plan. Test the exact transport coordinate, canonical runtime/plugin archives, image index, platform image, helper, unique plugin identity, command metadata, provenance, and suite identities. CTL-05: retain repository results as publisher claims and require separately produced NemoClaw verification evidence with producer identity and limitations. PKG-07: record the immutable qualification evidence reference for the final reviewed Phase 9 release-set pull request, but do not change the shipped default here.</action>
  <verify><automated>npx vitest run --project package-contract test/package-contract/runtime-packages/external-openclaw.test.ts</automated></verify>
  <acceptance_criteria>
    - Runtime and plugin identities cannot be mixed across publications.
    - Verification runs no package callback.
    - Phase 8 OpenClaw remains the default throughout this plan.
    - Verification success or failure leaves the static release set, installed receipts, and running sandboxes unchanged.
  </acceptance_criteria>
  <done>NemoClaw qualifies an exact external OpenClaw runtime and plugin tuple without changing the shipped default.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Confirm trusted OpenClaw publication and full parity evidence</name>
  <files>None</files>
  <decision>Does an authorized maintainer accept this exact external OpenClaw runtime/plugin tuple and central parity evidence as the third handoff artifact?</decision>
  <context>The complete external release cannot enter final qualification until all three handoffs have immutable accepted identities and evidence records. A failed, deferred, or rejected result stops Phase 9.</context>
  <action>First verify that 09-03 and 09-04 produced accepted immutable handoff records; otherwise stop Phase 9. Pause handoff acceptance until a maintainer reviews the trusted OpenClaw runtime and plugin publication plus central parity evidence. Record the accepted exact tuple and immutable evidence digest for the one final static release-set pull request in 09-06. Make no product-support or release-set change here. Any missing, failed, or rejected result keeps Phase 8 shipped and stops Phase 9 before 09-06.</action>
  <what-built>The independent OpenClaw runtime and plugin repository plus one exact verified tuple.</what-built>
  <how-to-verify>Confirm trusted publication from the accepted repository. Dispatch central default-selection, onboarding, inference, plugin slash commands, pairing, MCP, deterministic messaging render, restart, snapshot and restore, rebuild, and cleanup against that exact tuple. Exclude live messaging-service accounts.</how-to-verify>
  <options>
    <option id="accept">Accept the exact runtime/plugin tuple and evidence digest for final external qualification.</option>
    <option id="reject">Reject it and stop Phase 9 with Phase 8 authoritative.</option>
    <option id="defer">Defer it and leave final external qualification blocked.</option>
  </options>
  <resume-signal>Provide the accepted exact runtime, plugin, and image identities with passing central run URLs. A failed or rejected gate terminates this handoff attempt and cannot resume into 09-06.</resume-signal>
  <verify><automated>npm run test:e2e-phases:check</automated></verify>
  <done>The exact OpenClaw runtime and plugin tuple has accepted parity evidence and an immutable acceptance record usable by 09-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| OpenClaw runtime and plugin publication to default selection | Two external artifacts jointly determine the default runtime behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-09 | Tampering | Runtime and plugin tuple | mitigate | Bind both artifacts, images, protocol, provenance, and tests in one exact evidence record. |
| T-09-10 | Elevation of privilege | Default selection | mitigate | Keep the Phase 8 default until central parity passes and the complete static release-set pull request is reviewed and released. |
| T-09-18 | Tampering | Sibling runtime and plugin contents | mitigate | Limit mutation to the authorized sibling root and require canonical manifest and archive equality with Phase 8. |
</threat_model>

<verification>
- [ ] External runtime and plugin standalone checks pass.
- [ ] Exact tuple package-contract tests pass.
- [ ] Central default and lifecycle parity evidence passes without live messaging accounts.
- [ ] Phase 8 remains authoritative until 09-06.
</verification>

<success_criteria>
- OpenClaw and its plugin publish independently without changing accepted behavior.
- NemoClaw retains exact default-selection and rollback authority.
</success_criteria>

<output>
Create `.planning/phases/09-external-repository-handoff/09-05-SUMMARY.md` when done.
</output>
