---
phase: 09-external-repository-handoff
plan: 06
type: execute
wave: 6
depends_on: ["09-05"]
files_modified:
  - release/agent-runtime-packages.json
  - release/agent-runtime-packages.schema.json
  - packages/nemoclaw-openclaw/
  - packages/nemoclaw-hermes/
  - packages/nemoclaw-deepagents-code/
  - package.json
  - scripts/install.sh
  - src/lib/agent-runtime-packages/external-source.ts
  - src/lib/agent-runtime-packages/external-fetch.ts
  - src/lib/agent-runtime-packages/external-cache.ts
  - src/lib/agent-runtime-packages/external-resolver.ts
  - src/lib/agent-runtime-packages/external-resolver.test.ts
  - src/lib/agent-runtime-packages/release-set.ts
  - src/lib/agent-runtime-packages/release-set.test.ts
  - test/package-contract/runtime-packages/external-release-set.test.ts
  - test/e2e/live/managed-image-activation-e2e.test.ts
  - test/e2e/live/managed-image-activation-e2e-helpers.ts
  - tools/e2e/workflow-plan.mts
  - tools/e2e/release-qualification.mts
  - test/e2e/support/workflow-plan.test.ts
  - test/e2e/support/release-qualification.test.ts
autonomous: false
requirements: [DIST-04, DIST-05, PKG-07, SEC-04, CTL-05]
must_haves:
  truths:
    - "D-07: The one checked-in standard release set switches all three entries together only after their external artifacts pass central qualification."
    - Standard installation and onboarding resolve exact external packages through one accepted core-owned transport without executing package code on the host.
    - The reviewed handoff removes the three in-tree runtime package roots after their exact external replacements are qualified; the shared contract and any Phase 4-proven package-neutral core support remain.
    - Offline, missing, corrupt, untrusted, revoked, or partial input leaves prior installed receipts and running sandboxes unchanged; existing installer and package-store transactions prevent partial registration.
    - The reviewed release-set revision has only static `supported`, `revoked`, or `superseded` status and immutable qualification evidence references.
    - CI and maintainer review gate ordinary merge and release; no separate runtime release-state service is added.
  artifacts:
    - src/lib/agent-runtime-packages/external-resolver.ts
    - release/agent-runtime-packages.json
    - test/package-contract/runtime-packages/external-release-set.test.ts
  key_links:
    - Resolver, installer registration, package catalogue, and existing lifecycle targets consume the same exact checked-in release-set digest.
    - The resolver hands verified archives to the existing content store and installer transaction instead of creating another staging transaction.
    - A failed gate blocks merge or release, so the Phase 8 shipped revision remains authoritative.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Switch the complete static standard release set to qualified external package artifacts.

Purpose: Finish repository handoff without changing the public install or onboarding experience or adding runtime release-state machinery.
Output: Exact external resolver, one reviewed static release set, removal of in-tree runtime package roots, packed-artifact evidence, existing-lifecycle qualification, and a maintainer release decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-external-repository-handoff/09-CONTEXT.md
@.planning/phases/09-external-repository-handoff/09-03-SUMMARY.md
@.planning/phases/09-external-repository-handoff/09-04-SUMMARY.md
@.planning/phases/09-external-repository-handoff/09-05-SUMMARY.md
@release/agent-runtime-packages.json
@scripts/install.sh
@test/e2e/live/managed-image-activation-e2e.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resolve exact external packages from the static release set</name>
  <files>src/lib/agent-runtime-packages/external-source.ts, src/lib/agent-runtime-packages/external-fetch.ts, src/lib/agent-runtime-packages/external-cache.ts, src/lib/agent-runtime-packages/external-resolver.ts, src/lib/agent-runtime-packages/external-resolver.test.ts, release/agent-runtime-packages.json, release/agent-runtime-packages.schema.json, src/lib/agent-runtime-packages/release-set.ts, src/lib/agent-runtime-packages/release-set.test.ts</files>
  <read_first>
    - .planning/phases/09-external-repository-handoff/09-CONTEXT.md
    - proposals/agent-runtime-packages/EXTERNAL-DISTRIBUTION-DECISION.md
    - .planning/phases/09-external-repository-handoff/09-03-SUMMARY.md
    - .planning/phases/09-external-repository-handoff/09-04-SUMMARY.md
    - .planning/phases/09-external-repository-handoff/09-05-SUMMARY.md
    - release/agent-runtime-packages.json
    - src/lib/agent-runtime-packages/release-set.ts
    - src/lib/agent-runtime-packages/archive.ts
    - src/lib/agent-runtime-packages/store.ts
    - src/lib/agent-runtime-packages/install.ts
    - packages/agent-runtime-contract/scripts/verify-repository-package.mts
  </read_first>
  <behavior>
    - One accepted immutable coordinate resolves through the transport named in 09-01 to bounded bytes with authenticated provenance and exact digests.
    - Verified archives enter the existing owner-only content-addressed cache and package store; resolution executes no hook, callback, build, import, or image layer.
    - The static release set contains the three accepted external tuples, immutable qualification evidence references, exact package/image/OpenShell identities, and one closed support status per entry.
    - Offline, missing, changing, oversized, corrupt, invalid, untrusted, non-supported, interrupted, or concurrent input cannot create a partial standard-package registration.
  </behavior>
  <action>Before implementation, verify 09-03 through 09-05 each produced the accepted exact identity and repository-owned evidence record; stop Phase 9 if any handoff gate failed. DIST-05 and D-09: implement only the transport accepted in 09-01. Parse a closed immutable source coordinate, resolve it without mutable-name fallback, stream through strict byte/time limits into a fresh owner-only temporary file, authenticate provenance and publisher namespace, verify archive and canonical tree/descriptor/helper digests, apply existing archive path and type checks, and atomically insert verified bytes into the existing content-addressed cache. Revalidate the checked-in support status and exact digest before cache reuse. Hand verified archives to the existing install/store registration transaction; do not add a second three-package staging transaction. D-08: require external canonical manifest and archive bytes to equal Phase 8 for handoff-equivalent versions. CTL-05: retain publisher/helper results as claims and record verifier observations with producer, method, subject digest, time, and limitations. SEC-04: reject unknown helper operations or mutations outside the declared scope and leave product data unchanged; Contract V1 has no read-only helper operation. Update the single `release/agent-runtime-packages.json` in one ordinary pull request with all three accepted external coordinates, exact identities, immutable qualification evidence references, and static `supported` status. Preserve `nemoclaw harness install &lt;local-artifact&gt;`; add no URL search, Python entry point, or ambient discovery surface.</action>
  <verify>
    <automated>npx vitest run --project cli src/lib/agent-runtime-packages/external-resolver.test.ts src/lib/agent-runtime-packages/release-set.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover accepted fetch, verified cache hit, offline exact-cache hit, cache miss, timeout, size limit, redirect policy, coordinate drift, provenance failure, unsafe archive, digest mismatch, non-supported status, interruption, and concurrent registration.
    - Resolver and verifier execute no package-controlled code and expose no credentials or free-form package diagnostics.
    - Existing install/store transactions publish either all three exact registrations or retain the prior registrations.
    - The static file has exactly three external standard entries and no mutable release-state fields.
    - Focused CLI tests exit 0.
  </acceptance_criteria>
  <done>NemoClaw resolves and registers exact external standard packages through existing transactions and one reviewed static release set.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Qualify the packed external release over existing targets</name>
  <files>packages/nemoclaw-openclaw/, packages/nemoclaw-hermes/, packages/nemoclaw-deepagents-code/, package.json, scripts/install.sh, test/package-contract/runtime-packages/external-release-set.test.ts, test/e2e/live/managed-image-activation-e2e.test.ts, test/e2e/live/managed-image-activation-e2e-helpers.ts, tools/e2e/workflow-plan.mts, tools/e2e/release-qualification.mts, test/e2e/support/workflow-plan.test.ts, test/e2e/support/release-qualification.test.ts</files>
  <read_first>
    - package.json
    - scripts/install.sh
    - packages/nemoclaw-openclaw/package.json
    - packages/nemoclaw-hermes/package.json
    - packages/nemoclaw-deepagents-code/package.json
    - release/agent-runtime-packages.json
    - test/package-contract/runtime-packages/external-release-set.test.ts
    - test/e2e/live/managed-image-activation-e2e.test.ts
    - test/e2e/live/managed-image-activation-e2e-helpers.ts
    - tools/e2e/workflow-plan.mts
    - tools/e2e/release-qualification.mts
    - test/e2e/docs/README.md
  </read_first>
  <behavior>
    - The packed branch revision installs and selects the exact reviewed external release set while a failed PR cannot change the previously shipped release.
    - Package-contract and existing lifecycle evidence bind the same static release-set digest, external coordinates, cache receipts, package receipts, images, OpenShell cohort, helper contract, and suite identity.
    - A command exit or executor claim without independently observed outcomes fails qualification.
  </behavior>
  <action>Per D-04 and D-07, extend the packed-artifact fixture and the Phase 8 planner-selected existing targets to exercise the checked-in external release-set revision. Assert exact fetch/cache receipt, CLI artifact, release-set digest, canonical package/archive digest, descriptor, package receipt, image index, selected platform digest, OpenShell version/capability cohort, helper protocol and fixed mutation-capable operation set, static support status, and suite version. Reuse `managed-image-activation-e2e` for all-three real runtime activation and the existing rebuild/snapshot targets aggregated by `workflow-plan.mts`; do not add another lifecycle executor. After all three exact external artifacts and their byte-equivalence evidence are available, remove the three in-tree runtime package roots and their root workspace/publish entries in the same reviewed change. Keep the shared contract and any package-neutral core support that Phase 4 proved necessary. Make the packed CLI, installer, and tests consume only the static external coordinates and verified cache/store; no root import or copied runtime source may remain. CTL-05: record executor claims separately from OpenShell-controlled or core-functional-probe evidence with producer and limitations. SEC-04: cover unknown-operation and out-of-declared-mutation-scope rejection in deterministic E2E-support or package-contract fixtures; add no read-only helper or before/after enforcement. D-11: preserve exactly the three external package-local descendant wrappers and assert that they do not call OpenShell or own sandbox lifecycle. Preserve normal install, no-channel onboarding, default OpenClaw, aliases, inference, restart, snapshot/restore, rebuild, cleanup, redacted artifacts, image-on-selection behavior, and no image preload. Test fresh fetch, verified cache reuse, offline exact-cache behavior, and fail-closed missing, non-supported, or corrupt input through the existing registration transaction.</action>
  <verify>
    <automated>npx vitest run --project package-contract test/package-contract/runtime-packages/external-release-set.test.ts &amp;&amp; npx vitest run --project e2e-support test/e2e/support/workflow-plan.test.ts test/e2e/support/release-qualification.test.ts &amp;&amp; npm run test:e2e-phases:check</automated>
  </verify>
  <acceptance_criteria>
    - Existing targets bind every external identity used during lifecycle operations.
    - Packed resolution covers fresh fetch, verified cache reuse, offline exact-cache behavior, and fail-closed missing, revoked, superseded, or corrupt input without partial registration.
    - Claim-only, wrong-producer, missing-limitation, unknown-operation, and out-of-declared-scope mutation evidence fails qualification.
    - Assertions cover outcomes and receipts, not incidental terminal output.
    - The root workspace and packed artifact contain no in-tree OpenClaw, Hermes, or Deep Agents Code package source after the handoff.
    - No live messaging service is selected.
  </acceptance_criteria>
  <done>The packed and live paths prove the exact external static release set using existing lifecycle coverage.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Accept the external release-set pull request and release</name>
  <files>None</files>
  <decision>Does an authorized maintainer accept this exact static external release-set revision and complete qualification evidence for normal merge and release?</decision>
  <context>The decision binds all three handoff acceptances plus exact transport, lifecycle, platform, Apple silicon macOS evidence from Docker Desktop or Colima, Brev, provenance, effect, cleanup, and Phase 8 rollback evidence. Passing tests alone does not authorize merge or release.</context>
  <action>Pause before merge or release until a maintainer reviews the ordinary `release/agent-runtime-packages.json` diff and all exact evidence. Require package-contract and external resolver/cache checks, the planner-selected full no-messaging Ubuntu lifecycle for all three packages, deterministic WSL evidence, six native multi-architecture image cells, the real Apple silicon macOS OpenClaw journey on Docker Desktop or Colima, and the official staging Brev Launchable's baked default OpenClaw result from the same revision. Accept protected Brev evidence only when that exact pre-merge commit runs from a maintainer-controlled `NVIDIA/NemoClaw` candidate branch after the fork commit is mirrored unchanged, or when the exact merged `main` commit runs before release. Never expose Launchable secrets to a contributor-fork workflow. The Brev and macOS journeys use no messaging channel. Do not add a generic Brev source-install lane or require Hermes or Deep Agents Code on Brev. Confirm the file contains only exact immutable identities, qualification evidence references, and static support status. If accepted, use the existing NemoClaw review, merge, and release process; that shipped file becomes authoritative. If rejected or deferred, do not merge or release and keep Phase 8 shipped. Rollback is an ordinary reviewed revert or follow-up release restoring the exact Phase 8 entries, not a public package command or mutable runtime pointer.</action>
  <what_built>The bounded external resolver/cache, one complete static external release set, packed-artifact coverage, and existing-lifecycle evidence.</what_built>
  <how-to-verify>Run the packed installer and planner-selected full lifecycle for all three external packages on Ubuntu, deterministic WSL checks, six native package/architecture image cells, the real Apple silicon macOS OpenClaw journey on Docker Desktop or Colima, and the official staging Brev Launchable baked default OpenClaw journey. Confirm trusted fetch or verified cache reuse, default OpenClaw selection, aliases, no image preload, inference, restart, snapshot and restore, rebuild, cleanup, evidence producers and limitations, and exact Phase 8 rollback diff. Confirm failed PR checks cannot affect the shipped release.</how-to-verify>
  <options>
    <option id="accept">Accept this exact release-set revision and evidence for normal merge and release.</option>
    <option id="reject">Reject it and retain the shipped Phase 8 release.</option>
    <option id="defer">Defer it and retain the shipped Phase 8 release.</option>
  </options>
  <resume-signal>Provide the repository-owned accepted release decision with exact revision and evidence digests, or keep Phase 8 shipped.</resume-signal>
  <verify><automated>npm run test:e2e-phases:check</automated></verify>
  <acceptance_criteria>
    - The same exact static release-set digest passes resolver, packed, lifecycle, platform, architecture, Apple silicon macOS on Docker Desktop or Colima, and Brev checks.
    - Protected Brev evidence comes from the exact commit on a maintainer-controlled `NVIDIA/NemoClaw` candidate branch or from exact merged `main`; no contributor-fork workflow receives Launchable secrets.
    - Maintainer review confirms all three external coordinates and immutable qualification references.
    - The release-set diff changes all three standard sources together and preserves menu order, aliases, and OpenClaw default.
    - Failure or rejection results in no merge or release; the shipped Phase 8 file remains authoritative.
    - The exact Phase 8 restoration diff is reviewable and tested.
  </acceptance_criteria>
  <done>The complete external release set ships through normal review and release, or Phase 8 remains authoritative.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| External standard release set to user installation | Exact externally published packages become the default selectable product surface only after review and release. |
| External transport to content-addressed cache | Untrusted remote bytes enter local storage only after bounded fetch, provenance, archive-safety, and digest verification. |
| Evidence and static release-set diff to maintainer decision | Human acceptance, qualification evidence, and the reviewed file must agree on one exact identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-11 | Tampering | Standard release set | mitigate | Bind every entry and the complete set to exact verified digests and immutable qualification references. |
| T-09-12 | Denial of service | External source outage or revocation | mitigate | Keep prior installed receipts, fail closed, and do not preload unrelated images. |
| T-09-19 | Tampering | Fetch and cache | mitigate | Use immutable coordinates, bounded streaming, archive path and type validation, and a verified content-addressed cache. |
| T-09-20 | Spoofing | External postcondition evidence | mitigate | Preserve publisher/helper claims and require separately produced observations with producer identity and limitations. |
| T-09-21 | Elevation of privilege | Release authority | mitigate | Change one core-owned static file only through complete CI, maintainer review, and the ordinary release process. |
</threat_model>

<verification>
- [ ] Packed package-contract tests pass.
- [ ] Resolver tests cover online, offline-cache, trust, archive-safety, non-supported status, interruption, and concurrency without package-code execution.
- [ ] E2E semantic phase validation passes through the existing planner.
- [ ] Required external lifecycle, Apple silicon macOS on Docker Desktop or Colima, multi-architecture, and Brev evidence pass without live messaging.
- [ ] A reviewed Phase 8 restoration diff is proven.
</verification>

<success_criteria>
- Standard NemoClaw installation consumes exact external packages without a user-experience change.
- Every resolution or qualification failure preserves installed authority and blocks the reviewed release.
- One static core-owned release set and existing registration transactions replace custom release-state machinery.
- The three runtime implementations live only in their accepted external repositories after the reviewed handoff.
- Immutable qualification evidence remains separate from static support status.
</success_criteria>

<output>
Create `.planning/phases/09-external-repository-handoff/09-06-SUMMARY.md` when done.
</output>
