---
phase: 09-external-repository-handoff
plan: 04
type: execute
wave: 4
depends_on: ["09-03"]
files_modified:
  - ../nemoclaw-hermes/
  - test/package-contract/runtime-packages/external-hermes.test.ts
autonomous: false
requirements: [DIST-02, DIST-03, DIST-04, DIST-05, PKG-06, PKG-07, CTL-05]
user_setup:
  - service: GitHub
    why: Publish the accepted managed Hermes package repository and enable trusted release evidence.
    dashboard_config:
      - task: Create or provide the accepted repository and trusted publishing environment.
        location: Repository named by EXTERNAL-DISTRIBUTION-DECISION.md
must_haves:
  truths:
    - Managed Hermes publishes independently without portable Hermes or tool-broker scope.
    - NemoClaw qualifies exact Hermes MCP, messaging-render, image, and lifecycle evidence before selection.
  artifacts:
    - ../nemoclaw-hermes/agent-runtime-package.yaml
    - test/package-contract/runtime-packages/external-hermes.test.ts
  key_links:
    - The Hermes evidence references only the exact external artifact that passed canonical-byte and managed-scope conformance; the shipped Phase 8 release does not change.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Hand off the qualified managed Hermes package while preserving its managed-only scope and deterministic integration evidence.

Purpose: Prove an independently published agent-gateway package without expanding its authority or supported modes.
Output: Independent Hermes repository, exact external-artifact test, and lifecycle evidence gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-external-repository-handoff/09-CONTEXT.md
@.planning/phases/05-managed-hermes-pilot/05-05-SUMMARY.md
@.planning/phases/09-external-repository-handoff/09-03-SUMMARY.md
@proposals/agent-runtime-packages/EXTERNAL-DISTRIBUTION-DECISION.md
@packages/nemoclaw-hermes/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Materialize and validate the managed Hermes repository</name>
  <files>../nemoclaw-hermes/</files>
  <read_first>.planning/phases/09-external-repository-handoff/09-CONTEXT.md, packages/nemoclaw-hermes/agent-runtime-package.yaml, .planning/phases/05-managed-hermes-pilot/05-05-SUMMARY.md, packages/agent-runtime-contract/repository-template/README.md</read_first>
  <action>Before any mutation, verify the 09-03 summary contains the accepted exact Deep Agents Code identity and repository-owned acceptance record; stop Phase 9 if it does not. Per D-03 and D-06, materialize the complete qualified managed Hermes package only in the exact sibling checkout authorized by 09-01; the full sibling root is this task's mutation closure. Do not modify a different checkout, remote, branch, or repository. D-08: preserve the canonical Phase 8 package-content manifest and archive bytes exactly, including Hermes `start.sh`, with only accepted repository-owned CI and provenance-envelope files outside the digest. D-11: keep that script as the one package-local Hermes descendant wrapper; it may manage only descendants of the admitted process and may not call OpenShell or own sandbox lifecycle. A packaged source, descriptor, helper, test, lockfile, mode, or archive-byte change is a new version requiring normal qualification. Add independent build, type-check, unit, image, conformance, deterministic MCP and messaging-render, provenance, software-bill-of-materials, and publication jobs. Add a repository check that rejects portable-mode and tool-gateway-broker sources.</action>
  <verify><automated>npm --prefix ../nemoclaw-hermes test</automated></verify>
  <acceptance_criteria>
    - The external repository contains no portable Hermes or host tool-broker authority.
    - Native MCP and messaging tests require no external messaging account.
    - Canonical package-content manifests and archive digests are byte-equivalent to Phase 8; only accepted repository-only files differ.
    - Repository output satisfies the shared publication contract.
  </acceptance_criteria>
  <done>The independent managed Hermes repository passes standalone, scope-boundary, and shared contract tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Qualify the exact external Hermes artifact</name>
  <files>test/package-contract/runtime-packages/external-hermes.test.ts</files>
  <read_first>release/agent-runtime-packages.json, ../nemoclaw-hermes/agent-runtime-package.yaml, packages/agent-runtime-contract/scripts/verify-repository-package.mts, test/generate-hermes-config.test.ts, test/messaging-build-applier.test.ts</read_first>
  <behavior>
    - Exact managed Hermes publication and deterministic MCP and messaging evidence pass central verification.
    - Scope expansion, identity mismatch, or missing evidence produces no accepted handoff evidence and leaves the shipped Phase 8 release unchanged.
  </behavior>
  <action>Per D-04, verify the accepted immutable external Hermes coordinate and its complete evidence envelope directly; do not create a second release-set file or modify `release/agent-runtime-packages.json` in this plan. Bind evidence to the canonical package/archive digest, managed-only descriptor, package and image digests, platform selection, helper protocol, deterministic render fixtures, provenance, and lifecycle suite. CTL-05: retain publisher results as claims and require separately produced NemoClaw evidence with producer and limitation metadata. PKG-07: record the immutable qualification evidence reference for the final reviewed Phase 9 release-set pull request, but do not change product support here. Reject evidence that includes deferred authorities.</action>
  <verify><automated>npx vitest run --project package-contract test/package-contract/runtime-packages/external-hermes.test.ts</automated></verify>
  <acceptance_criteria>
    - External Hermes remains managed-only.
    - Central verification detects contract, image, MCP, messaging, provenance, and suite drift.
    - Failure preserves the in-tree exact tuple.
    - Verification success or failure leaves the static release set, installed receipts, and running workloads unchanged.
  </acceptance_criteria>
  <done>NemoClaw qualifies an exact external managed Hermes artifact without changing its supported scope.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Confirm trusted Hermes publication and no-messaging lifecycle evidence</name>
  <files>None</files>
  <decision>Does an authorized maintainer accept this exact external managed Hermes publication and central no-messaging evidence as the second handoff artifact?</decision>
  <context>OpenClaw handoff cannot begin until Deep Agents Code and managed Hermes both have immutable accepted identities and evidence records. A failed, deferred, or rejected result stops Phase 9.</context>
  <action>First verify that 09-03 produced the accepted Deep Agents Code artifact and immutable acceptance record; otherwise stop Phase 9. Pause handoff acceptance until a maintainer reviews the trusted managed Hermes publication and central no-messaging lifecycle evidence. Record the accepted exact tuple and immutable evidence digest for the one final static release-set pull request in 09-06. Make no product-support or release-set change here. Any missing, failed, or rejected result preserves Phase 8 and stops Phase 9 before 09-05.</action>
  <what-built>The independent managed Hermes repository and exact verified artifact.</what-built>
  <how-to-verify>Confirm trusted publication from the accepted repository. Dispatch the existing central Hermes onboard, inference, dashboard and API health, MCP, restart, snapshot and restore, rebuild, and cleanup targets against the exact artifact. Do not select live messaging-service suites.</how-to-verify>
  <options>
    <option id="accept">Accept the exact managed Hermes artifact and evidence digest for the next handoff.</option>
    <option id="reject">Reject it and stop Phase 9 with Phase 8 authoritative.</option>
    <option id="defer">Defer it and leave the next handoff blocked.</option>
  </options>
  <resume-signal>Provide the accepted exact Hermes artifact and passing central run URL. A failed or rejected gate terminates this handoff attempt and cannot resume into 09-05.</resume-signal>
  <verify><automated>npm run test:e2e-phases:check</automated></verify>
  <done>The exact managed Hermes artifact has accepted lifecycle evidence and an immutable acceptance record usable by 09-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Hermes repository to core credentials and policy | Native package behavior consumes references but must not gain credential or policy authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-09-07 | Elevation of privilege | Deferred Hermes authorities | mitigate | Reject portable and broker sources and capabilities from the managed package. |
| T-09-08 | Information disclosure | Hermes evidence and diagnostics | mitigate | Require credential-free structured evidence and central redaction checks. |
| T-09-17 | Tampering | Sibling repository contents | mitigate | Limit mutation to the authorized sibling root and require canonical manifest and archive equality with Phase 8. |
</threat_model>

<verification>
- [ ] Standalone and shared repository checks pass.
- [ ] Managed-only package-contract verification passes.
- [ ] Central lifecycle evidence binds the published exact identity.
- [ ] No live messaging credential is required.
</verification>

<success_criteria>
- Managed Hermes publishes independently with unchanged scope and behavior.
- NemoClaw retains central qualification, credential, policy, and rollback authority.
</success_criteria>

<output>
Create `.planning/phases/09-external-repository-handoff/09-04-SUMMARY.md` when done.
</output>
