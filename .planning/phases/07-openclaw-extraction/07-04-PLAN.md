---
phase: 07-openclaw-extraction
plan: 04
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - packages/nemoclaw-openclaw/controller/runtime-control.ts
  - packages/nemoclaw-openclaw/controller/reconcile-native.ts
  - packages/nemoclaw-openclaw/controller/prepare-native-state.ts
  - packages/nemoclaw-openclaw/controller/runtime-control.test.ts
  - packages/nemoclaw-openclaw/Dockerfile
  - packages/nemoclaw-openclaw/agent-runtime-package.yaml
  - packages/nemoclaw-openclaw/move-manifest.json
  - test/openclaw-controller-image-contract.test.ts
autonomous: true
requirements: [OCL-03]
must_haves:
  truths:
    - "OpenClaw implements only the frozen shared `reconcile-native` and `prepare-native-state` operations."
    - "Every helper invocation is mutation-capable and every helper result is an executor claim."
    - "Health, readiness, process, service, and state observations use descriptor-declared probes or named trusted core/OpenShell operations without package code."
  artifacts:
    - path: packages/nemoclaw-openclaw/controller/runtime-control.ts
      provides: Fixed OpenClaw implementation of Contract V1.
    - path: packages/nemoclaw-openclaw/controller/runtime-control.test.ts
      provides: Two-operation, hostile-output, and authority coverage.
    - path: test/openclaw-controller-image-contract.test.ts
      provides: Installed helper identity and mode contract.
  key_links:
    - from: packages/nemoclaw-openclaw/controller/runtime-control.ts
      to: packages/agent-runtime-contract/schemas/runtime-control-v1.schema.json
      via: Frozen two-operation parser.
    - from: packages/nemoclaw-openclaw/agent-runtime-package.yaml
      to: src/lib/agent/definition-types.ts
      via: Existing fixed entry, forward-port, dashboard, and health-probe declarations.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Implement and install the frozen Contract V1 helper for OpenClaw.

Purpose: Move bounded native translation behind the package boundary without adding observation or lifecycle operations.
Output: Two shared handlers, fixed image installation, descriptor declarations, and image contract tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-openclaw-extraction/07-CONTEXT.md
@.planning/phases/06-contract-v1-freeze/06-02-SUMMARY.md
@.planning/phases/07-openclaw-extraction/07-02-SUMMARY.md
@packages/agent-runtime-contract/schemas/runtime-control-v1.schema.json
@packages/agent-runtime-contract/schemas/operation-receipt-v1.schema.json
@packages/nemoclaw-openclaw/runtime/generate-openclaw-config.mts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the two frozen mutation-capable operations</name>
  <files>packages/nemoclaw-openclaw/controller/runtime-control.ts, packages/nemoclaw-openclaw/controller/reconcile-native.ts, packages/nemoclaw-openclaw/controller/prepare-native-state.ts, packages/nemoclaw-openclaw/controller/runtime-control.test.ts</files>
  <read_first>packages/agent-runtime-contract/schemas/runtime-control-v1.schema.json, packages/agent-runtime-contract/schemas/operation-receipt-v1.schema.json, packages/nemoclaw-openclaw/runtime/generate-openclaw-config.mts, packages/nemoclaw-openclaw/runtime/nemoclaw-start.sh</read_first>
  <action>Implement exactly `reconcile-native` and `prepare-native-state` with the frozen shared fields. `reconcile-native` invokes bounded package-owned native translation; `prepare-native-state` prepares only native OpenClaw state inside a caller-owned existing transaction. Both are mutation-capable and return only an executor claim. Reject observe, health, status, session, process, start, stop, restart, supervise, sandbox, rollback, command, callback, credential-value, unknown-field, and raw-native-output requests. Preserve Phase 3 identity/framing bounds and Phase 6 error semantics. Package code never calls OpenShell or commits registry/product state.</action>
  <verify><automated>npm --prefix packages/nemoclaw-openclaw test -- controller/runtime-control.test.ts</automated></verify>
  <acceptance_criteria>
    - The helper accepts exactly two operation names.
    - Every response is an executor claim with fixed mutation-capable effect.
    - No helper observation, health, status, session, lifecycle, or process operation exists.
    - Hostile fields, credentials, oversized output, and identity mismatch fail closed.
  </acceptance_criteria>
  <done>OpenClaw implements the frozen shared helper surface and nothing broader.</done>
</task>

<task type="auto">
  <name>Task 2: Declare current entry and trusted probe data</name>
  <files>packages/nemoclaw-openclaw/agent-runtime-package.yaml, packages/nemoclaw-openclaw/move-manifest.json, packages/nemoclaw-openclaw/controller/runtime-control.test.ts</files>
  <read_first>agents/openclaw/manifest.yaml, packages/nemoclaw-openclaw/agent-runtime-package.yaml, packages/nemoclaw-openclaw/move-manifest.json, src/lib/agent/definition-types.ts, src/lib/agent/gateway-readiness.ts</read_first>
  <action>Carry current fixed entry, interaction, `forward_ports`, dashboard, and `health_probe` data into the descriptor using existing fields. Current core/OpenShell paths execute and interpret those probes; the package only declares data. Keep canonical process actions on the single OpenShell client. Retain any current in-image descendant-wrapper/supervision behavior as exact move-manifest compatibility debt until its OpenShell 0.0.106 parity gate proves equivalent behavior. Do not create a helper session or health operation.</action>
  <verify><automated>npm --prefix packages/nemoclaw-openclaw test -- controller/runtime-control.test.ts -t 'descriptor|lifecycle rejection|probe declarations'</automated></verify>
  <acceptance_criteria>
    - Descriptor data preserves current entry, port, dashboard, and probe behavior.
    - Core/OpenShell, not package code, executes and interprets observations.
    - Helper lifecycle/process requests fail closed.
    - Compatibility debt has an exact caller and removal gate.
  </acceptance_criteria>
  <done>OpenClaw declares current interaction data while trusted core/OpenShell paths retain authority.</done>
</task>

<task type="auto">
  <name>Task 3: Install and verify the fixed helper</name>
  <files>packages/nemoclaw-openclaw/Dockerfile, packages/nemoclaw-openclaw/agent-runtime-package.yaml, test/openclaw-controller-image-contract.test.ts</files>
  <read_first>packages/nemoclaw-openclaw/Dockerfile, packages/nemoclaw-openclaw/agent-runtime-package.yaml, packages/nemoclaw-openclaw/controller/runtime-control.ts, test/openclaw-controller-image-contract.test.ts</read_first>
  <action>Install one root-owned non-writable executable at `/opt/nemoclaw/bin/runtime-control`. Bind its digest, exact protocol range, and two operations in image/package evidence. Test ownership, mode, dependency closure, writable path shadowing, credential exclusion, and absence of lifecycle, process, observation, or OpenShell capability. Preserve current OpenClaw image names and numeric sandbox identity.</action>
  <verify><automated>npx vitest run --project integration test/openclaw-controller-image-contract.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - The image contains one protected fixed helper.
    - Evidence names only the frozen protocol and two operations.
    - Writable shadow and extra capability fixtures fail.
    - Public image and runtime identity remain unchanged.
  </acceptance_criteria>
  <done>The exact OpenClaw image contains the protected frozen helper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Untrusted input | Control |
|---|---|---|
| Helper request | Core-normalized intent | Frozen schema, identity, size, and deadline |
| Helper result | Package executor claim | Cannot author trusted observation or commit state |
| Descriptor probe | Package-declared data | Current trusted core/OpenShell execution and interpretation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-04-01 | Spoofing | Helper executable | Mitigate | Exact digest, owner, mode, and image identity |
| T-07-04-02 | Elevation of privilege | Operation dispatch | Avoid | Exactly two mutation-capable operations; no lifecycle/observe |
| T-07-04-03 | Information disclosure | Helper output | Mitigate | Closed schema, redaction, and bounds |
</threat_model>

<verification>
- [ ] Package controller tests pass.
- [ ] Descriptor probe/lifecycle rejection tests pass.
- [ ] Image helper identity tests pass.
- [ ] Repository boundary checks pass.
</verification>

<success_criteria>
- OpenClaw implements only frozen shared V1 behavior.
- Helper output remains an executor claim.
- Trusted observations and lifecycle remain in current core/OpenShell paths.
</success_criteria>

<output>
After completion, create `.planning/phases/07-openclaw-extraction/07-04-SUMMARY.md`.
</output>
