---
phase: 07-openclaw-extraction
plan: 02
type: execute
wave: 1
depends_on: ["06-03"]
files_modified:
  - packages/nemoclaw-openclaw/move-manifest.json
  - packages/nemoclaw-openclaw/build-inputs.manifest.json
  - packages/nemoclaw-openclaw/package.json
  - packages/nemoclaw-openclaw/agent-runtime-package.yaml
  - packages/nemoclaw-openclaw/Dockerfile
  - packages/nemoclaw-openclaw/Dockerfile.base
  - src/lib/agent/base-image.ts
  - src/lib/agent/base-image.test.ts
  - scripts/install.sh
  - scripts/check-production-build-args.sh
  - scripts/checks/dependency-pins.mts
  - .github/actions/resolve-sandbox-base-image/action.yaml
  - .github/workflows/docker-pin-check.yaml
  - .github/workflows/base-image.yaml
  - .github/workflows/managed-images.yaml
  - src/lib/sandbox-base-image/source-identity.ts
  - src/lib/sandbox-base-image/source-identity.test.ts
  - test/dependency-pins-check.test.ts
  - test/openclaw-package-path-closure.test.ts
  - test/package-independence-openclaw.test.ts
  - package.json
  - package-lock.json
autonomous: true
requirements: [OCL-01]
must_haves:
  truths:
    - "OpenClaw image/runtime inputs have one exact generated move manifest and one package root."
    - "The copied package builds without repository-root source."
    - "Public image names, dependency pins, architectures, and the current managed-image cohort remain unchanged."
  artifacts:
    - path: packages/nemoclaw-openclaw/move-manifest.json
      provides: Exact source, destination, consumer, test, and compatibility-debt dispositions.
    - path: packages/nemoclaw-openclaw/build-inputs.manifest.json
      provides: Exact source owner, package destination, mode, and digest for every immutable Docker build input.
    - path: packages/nemoclaw-openclaw/agent-runtime-package.yaml
      provides: OpenClaw descriptor using current manifest fields and frozen V1.
    - path: test/package-independence-openclaw.test.ts
      provides: Copied-package build boundary.
  key_links:
    - from: packages/nemoclaw-openclaw/move-manifest.json
      to: proposals/agent-runtime-packages/OPENCLAW-CANDIDATES.tsv
      via: Generated dispositions plus a fresh consumer scan.
    - from: src/lib/agent/base-image.ts
      to: packages/nemoclaw-openclaw/Dockerfile
      via: Package-root build context.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Create a self-contained OpenClaw image/runtime package and cut over its exact consumers.

Purpose: Make OpenClaw inputs independently movable without redesigning runtime behavior.
Output: Exact move manifest, package-root image source, consumer cutover, and copied-package proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-openclaw-extraction/07-CONTEXT.md
@.planning/phases/06-contract-v1-freeze/06-03-SUMMARY.md
@proposals/agent-runtime-packages/OPENCLAW-CANDIDATES.tsv
@Dockerfile
@Dockerfile.base
@agents/openclaw/manifest.yaml
@src/lib/agent/base-image.ts
@.github/workflows/base-image.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the exact OpenClaw move manifest</name>
  <files>packages/nemoclaw-openclaw/move-manifest.json, packages/nemoclaw-openclaw/build-inputs.manifest.json, packages/nemoclaw-openclaw/build-inputs/</files>
  <read_first>proposals/agent-runtime-packages/OPENCLAW-CANDIDATES.tsv, Dockerfile, Dockerfile.base, agents/openclaw/manifest.yaml, scripts/nemoclaw-start.sh, scripts/generate-openclaw-config.mts, nemoclaw-blueprint/model-specific-setup/openclaw/gpt-5-o-series-managed-inference.json</read_first>
  <action>Generate exact rows for every image/runtime source, Docker COPY, import, script, workflow, source-identity check, dependency check, and detailed test. Record source, destination, active consumers, test owner, and move/keep/split disposition. Classify every Docker COPY source as a package-owned moved path, an existing exact Phase 4 runtime-support path, or an immutable package build-input snapshot. Record each snapshot's authoritative core source, package destination under `build-inputs/`, mode, digest, owner, and tests in `build-inputs.manifest.json`; copy no credential, effective policy, executable host hook, or mutable state, and reject an unclassified Docker input. Record current in-image startup or descendant-wrapper supervision as bounded compatibility debt with its exact caller and an OpenShell 0.0.106 parity removal gate. Use the Phase 4 runtime-support payload only for files already present in its exact shared manifest; do not expand it during OpenClaw extraction.</action>
  <verify><automated>node -e "const m=require('./packages/nemoclaw-openclaw/move-manifest.json');const b=require('./packages/nemoclaw-openclaw/build-inputs.manifest.json');if(!m.entries?.length)throw new Error('empty OpenClaw move manifest');for(const e of m.entries)if(!e.source||!e.disposition||!e.owner)throw new Error('incomplete row');if(!Array.isArray(b.entries))throw new Error('build-input entries missing');for(const e of b.entries)for(const k of ['source','destination','mode','digest','owner'])if(!e[k])throw new Error('incomplete build input')"</automated></verify>
  <acceptance_criteria>
    - Every moved source and test has an exact destination and owner.
    - Every active consumer is listed before the old path is removed.
    - Compatibility debt has a named caller and removal gate.
    - No broad directory entry substitutes for row-level ownership.
    - Every Docker COPY resolves inside the package or an exact declared packed dependency; each copied core input retains its authoritative core source and digest.
  </acceptance_criteria>
  <done>The OpenClaw physical move is fully enumerated before execution.</done>
</task>

<task type="auto">
  <name>Task 2: Move image inputs and cut over exact consumers</name>
  <files>packages/nemoclaw-openclaw/package.json, packages/nemoclaw-openclaw/agent-runtime-package.yaml, packages/nemoclaw-openclaw/Dockerfile, packages/nemoclaw-openclaw/Dockerfile.base, src/lib/agent/base-image.ts, src/lib/agent/base-image.test.ts, scripts/install.sh, scripts/check-production-build-args.sh, scripts/checks/dependency-pins.mts, .github/actions/resolve-sandbox-base-image/action.yaml, .github/workflows/docker-pin-check.yaml, .github/workflows/base-image.yaml, .github/workflows/managed-images.yaml, src/lib/sandbox-base-image/source-identity.ts, src/lib/sandbox-base-image/source-identity.test.ts, test/dependency-pins-check.test.ts, test/openclaw-package-path-closure.test.ts, package.json, package-lock.json</files>
  <read_first>packages/nemoclaw-openclaw/move-manifest.json, agents/openclaw/manifest.yaml, src/lib/agent/base-image.ts, scripts/install.sh, scripts/check-production-build-args.sh, scripts/checks/dependency-pins.mts, .github/actions/resolve-sandbox-base-image/action.yaml, .github/workflows/base-image.yaml, .github/workflows/managed-images.yaml, src/lib/sandbox-base-image/source-identity.ts</read_first>
  <action>Move exact image/runtime rows and update only their manifest-listed consumers. Translate the current manifest into the package descriptor using existing `forward_ports`, `dashboard`, `health_probe`, state, interaction, and frozen V1 fields; do not add a generic service model. Point base-image staging, installer, dependency/source-identity checks, actions, and workflows at the package root. Preserve installed paths, exact pins, public image repositories, `linux/amd64` and `linux/arm64`, job topology, and managed-image cohort. Delete an old source only after every listed consumer passes. Add an old-path scanner for moved rows.</action>
  <verify><automated>npx vitest run --project cli src/lib/agent/base-image.test.ts src/lib/sandbox-base-image/source-identity.test.ts --coverage=false &amp;&amp; npx vitest run --project integration test/dependency-pins-check.test.ts test/openclaw-package-path-closure.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - OpenClaw builds from the package-root Dockerfiles only.
    - Descriptor service data uses existing narrow fields.
    - Every listed production and workflow consumer uses the new path.
    - Image identities, architectures, pins, and cohort remain unchanged.
  </acceptance_criteria>
  <done>One package root owns OpenClaw image/runtime inputs and all active consumers use it.</done>
</task>

<task type="auto">
  <name>Task 3: Prove package-root independence</name>
  <files>test/package-independence-openclaw.test.ts, packages/nemoclaw-openclaw/package.json, package-lock.json</files>
  <read_first>packages/nemoclaw-openclaw/move-manifest.json, packages/nemoclaw-openclaw/Dockerfile, packages/nemoclaw-openclaw/Dockerfile.base, packages/nemoclaw-openclaw/agent-runtime-package.yaml, test/package-independence-openclaw.test.ts</read_first>
  <action>Create a test-owned workspace envelope in a temporary directory containing the root workspace manifest and lock, the OpenClaw package and contract sources, the package manifests needed to preserve exact workspace links, and no undeclared sibling runtime source. If the OpenClaw package declares the Phase 4 runtime-support package as `required: true`, include that exact source package and verify its declared manifest and digest. Run source build and package tests there. Separately install the exact packed OpenClaw and contract artifacts into an empty consumer, plus the exact packed runtime-support artifact when it is required. Validate descriptor paths, the absence of a nested workspace lock, Docker COPY closure through `build-inputs.manifest.json`, exact dependency pins, executable modes, declared support identity and digest when present, and required installed files. Build the image from the copied package root when the normal package-boundary environment supplies Docker; otherwise run the deterministic static boundary and report the existing stable skip. The root lock belongs to the test-owned build envelope and is not part of the canonical package tree. Reject parent traversal, checkout absolute paths, sibling source reads, undeclared inputs, build-input drift, and package-authored release status.</action>
  <verify><automated>npx vitest run --project integration test/package-independence-openclaw.test.ts --coverage=false &amp;&amp; npm run checks:repository</automated></verify>
  <acceptance_criteria>
    - The fixture copies no undeclared sibling runtime source or checkout content; envelope metadata and declared dependency sources remain outside the canonical package tree.
    - A required runtime-support dependency is present as the exact declared source and packed artifact, with matching identity and digest.
    - Static closure fails for any missing or parent-relative input.
    - Docker uses only the copied root when available.
    - Static release status remains core-owned.
  </acceptance_criteria>
  <done>The OpenClaw image package is independent of repository-private source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Untrusted input | Control |
|---|---|---|
| Move manifest | Mixed root source | Exact row, consumer closure, and old-path scanner |
| Image build context | Package files | Copied-root closure and exact pins |
| Compatibility wrapper | In-image process behavior | Named caller and OpenShell parity removal gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-02-01 | Tampering | Build context | Mitigate | Exact manifest and copied-root build |
| T-07-02-02 | Elevation of privilege | Descriptor | Mitigate | Frozen schema and no host command/callback |
| T-07-02-03 | Denial of service | Wrapper removal | Mitigate | Retain until OpenShell 0.0.106 parity passes |
</threat_model>

<verification>
- [ ] Move-manifest and old-path checks pass.
- [ ] Focused base-image, source-identity, dependency, and path-closure tests pass.
- [ ] Copied-package boundary passes.
- [ ] Repository checks pass.
</verification>

<success_criteria>
- OpenClaw image/runtime inputs have one package root.
- The package builds outside the checkout.
- Current public image behavior remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/07-openclaw-extraction/07-02-SUMMARY.md`.
</output>
