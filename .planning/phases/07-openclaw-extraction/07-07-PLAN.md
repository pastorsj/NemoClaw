---
phase: 07-openclaw-extraction
plan: 07
type: execute
wave: 5
depends_on: ["07-05", "07-06"]
files_modified:
  - src/lib/onboard/openclaw-setup.ts
  - src/lib/onboard/machine/handlers/agent-setup.ts
  - src/lib/onboard/machine/final-flow-phases.ts
  - src/lib/agent/onboard.ts
  - src/lib/onboard/managed-startup/image-runtime.ts
  - src/lib/onboard/managed-startup/profile.ts
  - src/lib/onboard/managed-startup-image-runtime.test.ts
  - src/lib/onboard/managed-startup/profile.test.ts
  - src/lib/agent-runtime-packages/release-set.ts
  - src/lib/agent-runtime-packages/release-set.test.ts
  - scripts/checks/agent-runtime-package-boundary.mts
  - test/package-contract/agent-runtime-core-boundary.test.ts
autonomous: true
requirements: [CORE-02, OCL-03, SEC-02]
must_haves:
  truths:
    - "OpenClaw uses the package-backed onboarding path and remains the checked-in release-set default while status is `supported`."
    - "Managed startup invokes only frozen helper operations and trusts only current core/OpenShell probes."
    - "The Phase 6 boundary check, not a second checker, rejects OpenClaw-native executable dispatch in runtime-neutral core."
  artifacts:
    - path: src/lib/agent-runtime-packages/release-set.ts
      provides: Static OpenClaw default and support status.
    - path: scripts/checks/agent-runtime-package-boundary.mts
      provides: Existing Phase 6 boundary check extended for OpenClaw.
  key_links:
    - from: src/lib/onboard/machine/handlers/agent-setup.ts
      to: src/lib/agent-runtime-packages/release-set.ts
      via: Exact supported default package selection.
    - from: src/lib/onboard/managed-startup/image-runtime.ts
      to: src/lib/adapters/openshell/client.ts
      via: Existing single OpenShell client and controller transport.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Route OpenClaw onboarding and managed startup through the package-backed path.

Purpose: Remove final OpenClaw-native executable branches from core while preserving default behavior.
Output: Package-backed onboarding, frozen helper invocation, static default selection, and extended boundary guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-openclaw-extraction/07-CONTEXT.md
@.planning/phases/07-openclaw-extraction/07-05-SUMMARY.md
@.planning/phases/07-openclaw-extraction/07-06-SUMMARY.md
@src/lib/onboard/openclaw-setup.ts
@src/lib/onboard/managed-startup/image-runtime.ts
@src/lib/agent-runtime-packages/release-set.ts
@scripts/checks/agent-runtime-package-boundary.mts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route default onboarding through exact package selection</name>
  <files>src/lib/onboard/openclaw-setup.ts, src/lib/onboard/machine/handlers/agent-setup.ts, src/lib/onboard/machine/final-flow-phases.ts, src/lib/agent/onboard.ts, src/lib/agent-runtime-packages/release-set.ts, src/lib/agent-runtime-packages/release-set.test.ts</files>
  <read_first>src/lib/onboard/openclaw-setup.ts, src/lib/onboard/machine/handlers/agent-setup.ts, src/lib/onboard/machine/final-flow-phases.ts, src/lib/agent/onboard.ts, src/lib/agent-runtime-packages/release-set.ts, src/lib/state/onboard-checkpoint-migrate.ts, test/cli/onboard-compatibility.test.ts</read_first>
  <action>Resolve OpenClaw through the exact checked-in release-set entry and existing package catalogue. It remains first/default only while its static status is `supported`; `revoked` and `superseded` fail with current remediation. Preserve menu order, aliases, prompts, `--agent`, `NEMOCLAW_AGENT`, and legacy checkpoint reading through the current bounded normalizer. Replace native OpenClaw setup execution with the package-backed setup inside the existing onboarding transaction. Keep sandbox, policy, credential, port, state, registry, rollback, and process effects on current NemoClaw/OpenShell paths. Do not add a mutable status mechanism, migration API, or alternate lifecycle.</action>
  <verify><automated>npx vitest run --project cli src/lib/agent-runtime-packages/release-set.test.ts src/lib/onboard/agent-selection.test.ts src/lib/agent/onboard.test.ts src/lib/onboard/agent-resume-state.test.ts --coverage=false &amp;&amp; npx vitest run --project integration test/cli/onboard-compatibility.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - OpenClaw default selection comes only from a static `supported` release-set entry.
    - Revoked and superseded fixtures fail without fallback.
    - Legacy checkpoints are read by the current normalizer and no new migration API exists.
    - Public menu, prompt, alias, and selection behavior remains unchanged.
  </acceptance_criteria>
  <done>Default onboarding selects the exact supported OpenClaw package through the existing product workflow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove native OpenClaw managed-startup dispatch</name>
  <files>src/lib/onboard/managed-startup/image-runtime.ts, src/lib/onboard/managed-startup/profile.ts, src/lib/onboard/managed-startup-image-runtime.test.ts, src/lib/onboard/managed-startup/profile.test.ts</files>
  <read_first>src/lib/onboard/managed-startup/image-runtime.ts, src/lib/onboard/managed-startup/profile.ts, src/lib/agent-runtime-packages/controller-client.ts, src/lib/adapters/openshell/client.ts, packages/nemoclaw-openclaw/controller/runtime-control.ts, packages/nemoclaw-openclaw/agent-runtime-package.yaml</read_first>
  <action>Replace core OpenClaw generator/config/hash translation with `reconcile-native` inside the existing managed-startup transaction. Treat completion as a mutation-capable helper claim. Use current descriptor-declared probes or named trusted OpenShell facts for readiness before registry publication. Keep bootstrap authorization, credential cleanup, deadlines, rollback, and transaction checkpoints in core; keep sandbox-local execution and process authority in OpenShell. Preserve any in-image compatibility wrapper recorded by the move manifest until its named OpenShell 0.0.106 parity gate passes. Do not add an OpenClaw-only helper operation or direct OpenShell escape hatch.</action>
  <verify><automated>npx vitest run --project cli src/lib/onboard/managed-startup-image-runtime.test.ts src/lib/onboard/managed-startup/profile.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - Core contains no OpenClaw-native generator/config/hash construction branch.
    - Helper claims cannot complete startup without current trusted readiness facts.
    - Package code cannot call OpenShell or supervise the process.
    - Compatibility wrapper debt remains narrow until parity proves removal.
  </acceptance_criteria>
  <done>Managed startup uses the frozen package helper inside the current transaction.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the Phase 6 core boundary check</name>
  <files>scripts/checks/agent-runtime-package-boundary.mts, test/package-contract/agent-runtime-core-boundary.test.ts</files>
  <read_first>scripts/checks/agent-runtime-package-boundary.mts, test/package-contract/agent-runtime-core-boundary.test.ts, packages/nemoclaw-openclaw/move-manifest.json, src/lib/onboard/managed-startup/image-runtime.ts, src/lib/agent-runtime-packages/release-set.ts</read_first>
  <action>Extend the existing checker to reject OpenClaw package imports, native command/config construction, runtime-name executable dispatch, helper observe/health/status/lifecycle/process operations, and direct OpenShell calls outside the existing adapter. Permit identity literals only in static release data, presentation/selection, current bounded legacy readers, tests, and exact compatibility debt from the move manifest. Do not create a second checker or unrestricted text allowlist.</action>
  <verify><automated>npx vitest run --project package-contract test/package-contract/agent-runtime-core-boundary.test.ts --coverage=false &amp;&amp; npm run checks:repository</automated></verify>
  <acceptance_criteria>
    - OpenClaw-native executable fixtures fail the Phase 6 checker.
    - Helper observation/lifecycle and alternate OpenShell-client fixtures fail.
    - Static release data and exact bounded legacy/debt paths pass.
    - Every exception names one current owner and reason.
  </acceptance_criteria>
  <done>One repository check protects the Deep Agents Code, Hermes, and OpenClaw package boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Untrusted input | Control |
|---|---|---|
| Default selection | Static release entry | Exact tuple and `supported` status |
| Startup helper | Package executor claim | Existing transaction and trusted readiness probes |
| Core source | Native convenience branch | One Phase 6 repository boundary check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-07-01 | Spoofing | Default package | Mitigate | Exact checked-in tuple and status |
| T-07-07-02 | Elevation of privilege | Startup helper | Avoid | No direct OpenShell/lifecycle/process operation |
| T-07-07-03 | Repudiation | Boundary regression | Mitigate | One exact repository check |
</threat_model>

<verification>
- [ ] Release-set, selection, resume, and compatibility tests pass.
- [ ] Managed-startup tests pass with trusted readiness probes.
- [ ] Phase 6 boundary and repository checks pass.
- [ ] CLI typecheck passes.
</verification>

<success_criteria>
- OpenClaw uses the package-backed onboarding/startup path.
- Static checked-in release data preserves the default.
- Runtime-neutral core contains no OpenClaw-native executable branch.
</success_criteria>

<output>
After completion, create `.planning/phases/07-openclaw-extraction/07-07-SUMMARY.md`.
</output>
