---
phase: 04-deep-agents-code-pilot
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - packages/nemoclaw-deepagents-code/move-manifest.json
  - packages/nemoclaw-deepagents-code/package.json
  - test/package-contract/deep-agents-code-package-boundary.test.ts
  - test/package-contract/deep-agents-code-path-cutover.test.ts
  - test/e2e/registry/definitions/baseline.ts
  - test/e2e/live/registry-targets.test.ts
  - test/e2e/fixtures/phases/lifecycle-dcode-invalid-credential.ts
  - test/e2e/support/dcode-base-image-contract.test.ts
  - .github/workflows/e2e.yaml
  - vitest.config.ts
autonomous: true
requirements: [DCO-01, DCO-02, TEST-02]
must_haves:
  truths:
    - "Every detailed Deep Agents Code test has an exact package or central owner."
    - "Old native branches are removed only after deterministic parity and the existing protected Ubuntu target pass."
    - "Any in-image supervision compatibility debt is explicit and retained until OpenShell 0.0.106 parity proves equivalent behavior."
  artifacts:
    - path: packages/nemoclaw-deepagents-code/move-manifest.json
      provides: Final source, test, and compatibility-debt dispositions.
    - path: test/e2e/registry/definitions/baseline.ts
      provides: Existing protected Deep Agents Code target with package identity assertions.
    - path: test/package-contract/deep-agents-code-package-boundary.test.ts
      provides: Central packed-package contract.
  key_links:
    - from: test/e2e/registry/definitions/baseline.ts
      to: test/e2e/live/registry-targets.test.ts
      via: Existing typed target registry.
    - from: packages/nemoclaw-deepagents-code/move-manifest.json
      to: vitest.config.ts
      via: Each moved test runs exactly once.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Finish Deep Agents Code test ownership, remove proven superseded branches, and qualify the existing protected target.

Purpose: Close the pilot with code-grounded parity evidence and no parallel E2E lane.
Output: Final move dispositions, central contracts, exact cutover, and protected Ubuntu E2E evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-deep-agents-code-pilot/04-CONTEXT.md
@.planning/phases/04-deep-agents-code-pilot/04-03-SUMMARY.md
@packages/nemoclaw-deepagents-code/move-manifest.json
@test/e2e/README.md
@test/e2e/registry/definitions/baseline.ts
@.github/workflows/e2e.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move detailed tests and keep central contracts</name>
  <files>packages/nemoclaw-deepagents-code/move-manifest.json, packages/nemoclaw-deepagents-code/package.json, test/package-contract/deep-agents-code-package-boundary.test.ts, test/package-contract/deep-agents-code-path-cutover.test.ts, vitest.config.ts</files>
  <read_first>packages/nemoclaw-deepagents-code/move-manifest.json, vitest.config.ts, test/package-contract/deep-agents-code-package-boundary.test.ts, test/package-contract/deep-agents-code-path-cutover.test.ts</read_first>
  <action>Apply each test disposition in the move manifest. Move Deep Agents Code-native unit and image tests with the package; keep package artifact, core authority, current transaction, and E2E tests central. Update package and root Vitest membership so each test runs once. Classify `dcode-session-supervisor.py` and related tests as compatibility debt if OpenShell 0.0.106 does not yet replace the behavior; name its exact caller, risk, and removal test instead of deleting it or calling it a new supported supervisor.</action>
  <verify><automated>npm --prefix packages/nemoclaw-deepagents-code test &amp;&amp; npm run test:projects:check &amp;&amp; npm run test:package</automated></verify>
  <acceptance_criteria>
    - Every candidate test has one owner and runs once.
    - Core retains package, authority, and transaction contracts.
    - Compatibility debt has a precise caller and removal gate.
    - No broad source/test directory claim replaces the move manifest.
  </acceptance_criteria>
  <done>Detailed tests live with Deep Agents Code while central product contracts remain independent.</done>
</task>

<task type="auto">
  <name>Task 2: Remove only proven superseded branches</name>
  <files>packages/nemoclaw-deepagents-code/move-manifest.json, test/package-contract/deep-agents-code-path-cutover.test.ts, src/lib/onboard/managed-startup/image-runtime.ts, src/lib/actions/sandbox/mcp-bridge-adapter-deepagents.ts, src/lib/actions/sandbox/rebuild-dcode-orchestrator.ts</files>
  <read_first>packages/nemoclaw-deepagents-code/move-manifest.json, src/lib/onboard/managed-startup/image-runtime.ts, src/lib/actions/sandbox/mcp-bridge-adapter-deepagents.ts, src/lib/actions/sandbox/rebuild-dcode-orchestrator.ts, test/package-contract/deep-agents-code-path-cutover.test.ts</read_first>
  <action>After package and central tests pass, remove only native translation branches whose package replacements have parity. Keep normalized intent, the current rebuild owners, OpenShell calls, registry publication, rollback, release-set selection, and any narrowly classified compatibility debt. Reject fallback to the removed native path. Do not add runtime-name dispatch, a feature flag, a lifecycle API, or a dynamic release-status action.</action>
  <verify><automated>npx vitest run --project package-contract test/package-contract/deep-agents-code-path-cutover.test.ts --coverage=false &amp;&amp; npm run typecheck:cli</automated></verify>
  <acceptance_criteria>
    - Removed branches have passing package replacements.
    - Current rebuild and OpenShell boundaries remain.
    - No fallback or parallel native dispatch remains.
    - Release status is not changed by test or runtime output.
  </acceptance_criteria>
  <done>The active path uses the package without deleting unproven OpenShell compatibility behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the protected Ubuntu baseline target</name>
  <files>test/e2e/registry/definitions/baseline.ts, test/e2e/live/registry-targets.test.ts, test/e2e/fixtures/phases/lifecycle-dcode-invalid-credential.ts, test/e2e/support/dcode-base-image-contract.test.ts, .github/workflows/e2e.yaml</files>
  <read_first>test/e2e/README.md, test/e2e/registry/definitions/baseline.ts, test/e2e/live/registry-targets.test.ts, test/e2e/fixtures/phases/lifecycle-dcode-invalid-credential.ts, .github/workflows/e2e.yaml</read_first>
  <action>Extend `ubuntu-repo-cloud-langchain-deepagents-code`; do not add a target. Preserve its current onboarding, inference, invalid-credential rebuild, and cleanup phases, and add exact package, descriptor, image, release-set, and OpenShell identity assertions. Keep messaging absent. Run it through the current protected workflow and typed registry. This is same-repository pilot evidence, not a generic Brev source-install lane or official staging Launchable evidence.</action>
  <verify><automated>NEMOCLAW_RUN_LIVE_E2E=1 E2E_TARGET_ID=ubuntu-repo-cloud-langchain-deepagents-code TARGET_ID=ubuntu-repo-cloud-langchain-deepagents-code npx tsx tools/e2e/live-vitest-invocation.mts run --test-path test/e2e/live/registry-targets.test.ts --selector '^ubuntu-repo-cloud-langchain-deepagents-code:'</automated></verify>
  <acceptance_criteria>
    - The existing target, not a duplicate lane, supplies the live result.
    - Evidence binds the exact source, package, image, release set, and OpenShell identities.
    - Invalid-credential rebuild fails before destructive mutation as currently specified.
    - Cleanup succeeds and no messaging service is used.
  </acceptance_criteria>
  <done>The Deep Agents Code pilot passes deterministic gates and its existing protected Ubuntu target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Untrusted input | Control |
|---|---|---|
| Test move | Candidate ledger rows | Exact move manifest and disjoint Vitest projects |
| Compatibility debt | In-image helper behavior | Named caller, current tests, and removal gate |
| Live evidence | Sandbox output | Exact identities, redaction, and owned cleanup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| DCO-T10 | Repudiation | Test transfer | Mitigate | One manifest disposition and one test owner per row |
| DCO-T11 | Denial of service | Premature supervisor deletion | Mitigate | Retain until OpenShell 0.0.106 parity proves removal |
| DCO-T12 | Information disclosure | E2E evidence | Mitigate | Existing redaction and no messaging credentials |
</threat_model>

<verification>
- [ ] Package, package-contract, project-membership, and CLI checks pass.
- [ ] The old-path scanner reports no unclassified active reference.
- [ ] Compatibility debt is explicit or removed after its named parity gate passes.
- [ ] `ubuntu-repo-cloud-langchain-deepagents-code` passes with cleanup.
</verification>

<success_criteria>
- Deep Agents Code detailed tests are package-owned.
- Core authority and artifact contracts remain central.
- The current protected target proves package-backed no-messaging parity.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deep-agents-code-pilot/04-04-SUMMARY.md`.
</output>
