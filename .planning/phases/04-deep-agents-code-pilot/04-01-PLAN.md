---
phase: 04-deep-agents-code-pilot
plan: 01
type: execute
wave: 1
depends_on: ["03-07"]
files_modified:
  - packages/nemoclaw-deepagents-code/move-manifest.json
  - packages/nemoclaw-deepagents-code/build-inputs.manifest.json
  - packages/nemoclaw-deepagents-code/package.json
  - packages/nemoclaw-deepagents-code/agent-runtime-package.yaml
  - packages/nemoclaw-deepagents-code/Dockerfile
  - packages/nemoclaw-deepagents-code/Dockerfile.base
  - packages/nemoclaw-runtime-support/package.json
  - packages/nemoclaw-runtime-support/manifest.json
  - packages/nemoclaw-runtime-support/manifest.test.ts
  - agents/hermes/Dockerfile
  - agents/hermes/Dockerfile.base
  - src/lib/agent/defs.ts
  - src/lib/agent/base-image.ts
  - .github/workflows/base-image.yaml
  - .github/workflows/managed-images.yaml
  - test/package-contract/deep-agents-code-package-boundary.test.ts
  - test/package-contract/deep-agents-code-path-cutover.test.ts
  - package.json
  - package-lock.json
autonomous: true
requirements: [DCO-01, TEST-02]
must_haves:
  truths:
    - "Deep Agents Code has one package source root and one exact generated move manifest."
    - "The copied package builds without repository-root reads."
    - "A fresh inventory records whether an exact package-neutral image-build payload has both Deep Agents Code and managed Hermes consumers; runtime-support stays absent when it does not."
  artifacts:
    - path: packages/nemoclaw-deepagents-code/move-manifest.json
      provides: Exact source, destination, consumer, and test dispositions for the move.
    - path: packages/nemoclaw-deepagents-code/build-inputs.manifest.json
      provides: Exact source owner, package destination, mode, and digest for every immutable Docker build input.
    - path: packages/nemoclaw-deepagents-code/agent-runtime-package.yaml
      provides: Deep Agents Code package descriptor.
    - path: test/package-contract/deep-agents-code-package-boundary.test.ts
      provides: Copied-package boundary proof.
  key_links:
    - from: packages/nemoclaw-deepagents-code/move-manifest.json
      to: proposals/agent-runtime-packages/TERMINAL-CANDIDATES.tsv
      via: Generated dispositions plus a fresh consumer scan.
    - from: packages/nemoclaw-deepagents-code/move-manifest.json
      to: packages/nemoclaw-deepagents-code/Dockerfile
      via: The recorded dual-consumer decision keeps support dependencies absent or selects the exact shared payload.
    - from: packages/nemoclaw-deepagents-code/move-manifest.json
      to: agents/hermes/Dockerfile
      via: The same decision governs the second managed-image consumer before Hermes moves.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Create the independently buildable Deep Agents Code package and cut over its exact source consumers.

Purpose: Establish the first runtime package without adding a general lifecycle or host-ingestion layer.
Output: Exact move manifest, package files, an explicit runtime-support absent/present decision, path cutover, and copied-package proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-deep-agents-code-pilot/04-CONTEXT.md
@.planning/phases/03-package-and-controller-foundation/03-07-SUMMARY.md
@proposals/agent-runtime-packages/TERMINAL-CANDIDATES.tsv
@agents/langchain-deepagents-code/Dockerfile
@agents/langchain-deepagents-code/Dockerfile.base
@agents/hermes/Dockerfile
@agents/hermes/Dockerfile.base
@src/lib/agent/defs.ts
@src/lib/agent/base-image.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the exact move manifest and decide shared-input ownership</name>
  <files>packages/nemoclaw-deepagents-code/move-manifest.json, packages/nemoclaw-deepagents-code/build-inputs.manifest.json, packages/nemoclaw-deepagents-code/build-inputs/, packages/nemoclaw-runtime-support/package.json, packages/nemoclaw-runtime-support/manifest.json, packages/nemoclaw-runtime-support/manifest.test.ts</files>
  <read_first>proposals/agent-runtime-packages/TERMINAL-CANDIDATES.tsv, agents/langchain-deepagents-code/Dockerfile, agents/langchain-deepagents-code/Dockerfile.base, agents/hermes/Dockerfile, agents/hermes/Dockerfile.base, package.json</read_first>
  <action>Generate the Deep Agents Code move manifest from the audited ledger and a fresh import, Docker COPY, script, workflow, and test scan. For each row record the exact old path, new path, current consumers, test owner, and move/keep/split disposition. Classify every Docker COPY source as a package-owned moved path, an exact runtime-support path, or an immutable package build-input snapshot. Record each snapshot's authoritative core source, package destination under `build-inputs/`, mode, digest, owner, and tests in `build-inputs.manifest.json`; copy no executable host hook or mutable state, and reject an unclassified Docker input. Inventory current repository-owned files copied by both managed-image Dockerfile pairs and record one `runtimeSupport` decision in the move manifest. If the exact package-neutral intersection is empty, record `required: false`, keep `packages/nemoclaw-runtime-support` absent, and leave both Dockerfile pairs without a support-package dependency. If the intersection is non-empty, record `required: true`, create the support package from only that intersection, and make Deep Agents Code and managed Hermes consume it in this change. A created payload is static image-build content with path, mode, and digest; it contains no lifecycle code, plugin API, host loader, runtime-specific input, or mutable compatibility service.</action>
  <verify><automated>node -e "const fs=require('node:fs');const cp=require('node:child_process');const m=require('./packages/nemoclaw-deepagents-code/move-manifest.json');const b=require('./packages/nemoclaw-deepagents-code/build-inputs.manifest.json');if(!Array.isArray(b.entries))throw new Error('build-input entries missing');for(const e of b.entries)for(const k of ['source','destination','mode','digest','owner'])if(!e[k])throw new Error('incomplete build input');const r=m.runtimeSupport;if(!r||typeof r.required!=='boolean')throw new Error('runtimeSupport decision missing');const paths=['packages/nemoclaw-runtime-support/package.json','packages/nemoclaw-runtime-support/manifest.json','packages/nemoclaw-runtime-support/manifest.test.ts'];const exists=paths.map((p)=>fs.existsSync(p));if(r.required&amp;&amp;!exists.every(Boolean))throw new Error('required runtimeSupport files are missing');if(!r.required&amp;&amp;exists.some(Boolean))throw new Error('runtimeSupport must be absent when not required');if(r.required){const x=cp.spawnSync('npm',['--prefix','packages/nemoclaw-runtime-support','test'],{stdio:'inherit'});if(x.status!==0)process.exit(x.status??1)}"</automated></verify>
  <acceptance_criteria>
    - Every moved Deep Agents Code row has an exact destination and consumer closure.
    - The move manifest records one reproducible absent/present runtime-support decision from the fresh inventory.
    - When runtime-support is absent, no empty workspace, manifest, Docker dependency, or test command remains.
    - When runtime-support is present, every payload file has both current managed-image consumers and both Dockerfile pairs consume the same exact payload.
    - A present support package exposes no lifecycle, plugin, host-ingestion, or runtime-specific API.
    - Every Docker COPY resolves inside the package or an exact declared packed dependency; each copied core input retains its authoritative core source and digest.
  </acceptance_criteria>
  <done>The physical move and any shared build payload have exact, reviewable ownership.</done>
</task>

<task type="auto">
  <name>Task 2: Move source and cut over exact consumers</name>
  <files>packages/nemoclaw-deepagents-code/package.json, packages/nemoclaw-deepagents-code/agent-runtime-package.yaml, packages/nemoclaw-deepagents-code/Dockerfile, packages/nemoclaw-deepagents-code/Dockerfile.base, src/lib/agent/defs.ts, src/lib/agent/base-image.ts, .github/workflows/base-image.yaml, .github/workflows/managed-images.yaml, test/package-contract/deep-agents-code-path-cutover.test.ts, package.json, package-lock.json</files>
  <read_first>packages/nemoclaw-deepagents-code/move-manifest.json, src/lib/agent/defs.ts, src/lib/agent/base-image.ts, .github/workflows/base-image.yaml, .github/workflows/managed-images.yaml, test/helpers/vitest-watch-triggers.ts, vitest.config.ts</read_first>
  <action>Move every `move` row to its exact destination and update only the consumers named by the manifest. Resolve Deep Agents Code through the Phase 3 catalogue, point image builds at the package root, and preserve current image names, dependency pins, menu text, commands, and aliases. Add a scanner that rejects active references to the removed source path while allowing historical planning evidence. Do not use directory-wide search-and-edit claims as the ownership record; update the manifest when a newly discovered consumer is real.</action>
  <verify><automated>npx vitest run --project package-contract test/package-contract/deep-agents-code-path-cutover.test.ts --coverage=false &amp;&amp; npm run typecheck:cli</automated></verify>
  <acceptance_criteria>
    - Package discovery and image build context use the package root.
    - Every manifest-listed active consumer uses the new path.
    - No unclassified old-path production reference remains.
    - Current selection and image identity remain unchanged.
  </acceptance_criteria>
  <done>Deep Agents Code has one active source root and a complete atomic consumer cutover.</done>
</task>

<task type="auto">
  <name>Task 3: Prove copied-package independence</name>
  <files>test/package-contract/deep-agents-code-package-boundary.test.ts, test/package-contract/deep-agents-code-path-cutover.test.ts, package.json, package-lock.json</files>
  <read_first>test/package-contract/helpers/package-fixture.ts, packages/nemoclaw-deepagents-code/package.json, packages/nemoclaw-deepagents-code/move-manifest.json</read_first>
  <action>Create a test-owned workspace envelope in a temporary directory containing the root workspace manifest and lock, the selected package and contract sources, the package manifests needed to preserve exact workspace links, and no sibling runtime source. Run the Deep Agents Code build and detailed tests there. Separately pack the package and its declared dependencies, including runtime-support only when the move manifest records `required: true`, and install those exact archives into an empty consumer outside the checkout. Reject parent traversal, checkout absolute paths, sibling source reads, undeclared files, a nested workspace lock, build-input digest drift, an unrecorded support package, or a missing required support package. The root lock belongs to the test-owned build envelope and is not part of the canonical package tree. Record installation identity only; package data cannot set checked-in release status. Add the package suite to the existing root aggregate.</action>
  <verify><automated>npx vitest run --project package-contract test/package-contract/deep-agents-code-package-boundary.test.ts test/package-contract/deep-agents-code-path-cutover.test.ts --coverage=false &amp;&amp; npm test</automated></verify>
  <acceptance_criteria>
    - The copied package runs without the original checkout.
    - The source copy uses the root workspace lock without copying sibling runtime source, and only declared inputs are present in the packed consumer.
    - Package output cannot author `supported`, `revoked`, or `superseded` status.
    - The deterministic repository aggregate passes after the move.
  </acceptance_criteria>
  <done>The package boundary and exact path cutover are independently verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Untrusted input | Control |
|---|---|---|
| Package archive | Moved runtime files | Exact move manifest and copied-package test |
| Shared image payload | Repository-owned build inputs | Dual-consumer inventory and per-file digests |
| Release status | Package descriptor or output | Read only from the checked-in core release set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| DCO-T01 | Tampering | Build context | Mitigate | Exact manifest, digests, and copied-root build |
| DCO-T02 | Elevation of privilege | Shared payload | Avoid | No lifecycle, plugin, host loader, or runtime-specific API |
| DCO-T03 | Spoofing | Support status | Avoid | Package schema and tests reject authored status |
</threat_model>

<verification>
- [ ] Exact move-manifest and old-path checks pass.
- [ ] Both managed-image consumers use the same exact shared payload if it exists.
- [ ] Copied-package and deterministic aggregate tests pass.
- [ ] CLI typecheck passes.
</verification>

<success_criteria>
- Deep Agents Code has one source root.
- The package builds outside the checkout.
- Runtime-support is absent unless exact shared image inputs have two current consumers; a present package has no broader API.
</success_criteria>

<output>
After completion, create `.planning/phases/04-deep-agents-code-pilot/04-01-SUMMARY.md`.
</output>
