---
phase: NCC-03-fabric-headless
plan: 01
type: execute
wave: 1
depends_on:
  - "02-22"
autonomous: true
requirements:
  - FABRIC-01
  - FABRIC-02
  - FABRIC-02A
must_haves:
  truths:
    - "A built agent runtime artifact has one public data-only conformance command."
    - "One generic Fabric runner invokes a package-selected adapter without an agent-name branch."
    - "LangChain Deep Agents Code keeps native commands and selects Fabric for compatible manifest-driven headless turns."
    - "Package data stops Fabric before discovery when the released adapter cannot preserve required model settings."
    - "Runner-owned outcomes and tested SDK lifecycle failures have deterministic evidence before live E2E."
    - "Pi can adopt the same contract after a released adapter exists."
  artifacts:
    - path: "packages/nemoclaw-fabric"
      provides: "Generic Fabric command, configuration loader, runner, and package-owned tests"
    - path: "src/lib/harness/package-validation.ts"
      provides: "Data-only built-artifact conformance API"
    - path: "agents/langchain-deepagents-code/manifest.yaml"
      provides: "Native interactive and Fabric headless command selection"
---

<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

# Phase 3 Plan 1: Fabric Headless Integration

<objective>
Add the smallest reproducible Fabric integration that proves a released Fabric-compatible agent
runtime can run through NemoClaw without changing NemoClaw's lifecycle or interactive experience.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Publish built-artifact conformance</name>
  <action>Add `nemoclaw harness validate &lt;artifact-directory&gt; [--json]`. Compose the existing hostile-tree, envelope, manifest, definition, digest, and final authority checks. Do not install, import, or execute package code.</action>
  <verify>Unit and compiled CLI tests prove valid, malformed, changed, and no-mutation paths.</verify>
</task>

<task type="auto">
  <name>Task 2: Implement the generic Fabric runner</name>
  <action>Create `packages/nemoclaw-fabric` with `doctor`, `run`, and `--version`. Load a normal Fabric config, accept exactly one prompt source, normalize plain and JSON output, redact diagnostics, and preserve Fabric lifecycle cleanup. Keep adapter selection in package data.</action>
  <verify>Package unit and released-SDK integration tests cover every declared status, exception boundary, signal, ordered invocation, and cleanup result.</verify>
</task>

<task type="auto">
  <name>Task 3: Connect the released Deep Agents adapter without changing native commands</name>
  <action>Make terminal `sandbox agent` dispatch prefer `headless_command`. Set the Deep Agents manifest headless command to the generic runner, add its exact Fabric config, stop configurations whose model settings cannot be preserved, lock the released Fabric graph separately, and include the runner in the reviewed package artifact and image.</action>
  <verify>Core synthetic routing, package contract, released-adapter text and workspace turns, incompatible-model rejection, lock, and image-content tests pass.</verify>
</task>

<task type="auto">
  <name>Task 4: Evaluate the no-messaging composition</name>
  <action>Run deterministic checks, a Mac arm64 Docker/OpenShell journey against the fake endpoint, and a Brev Linux amd64 journey through the approved inference route. Use the pinned non-Ultra model without reasoning effort. Verify result, workspace artifact, redaction, exact package identity, and cleanup. Do not run messaging tests.</action>
  <verify>Record exact commit, package digest, Fabric versions, platform, commands, outcomes, and bounded cleanup as development evidence.</verify>
</task>

</tasks>

<success_criteria>

- A released `harness.adapter_id` adapter that fits the runner's configuration, credential
  indirection, and deadline contract needs package data and dependencies, not a new runner.
- Native interactive behavior remains unchanged.
- The result does not claim native DCode parity or Fabric support.
- Fast package and conformance lanes pass without Docker, OpenShell, or inference.
- Mac and Brev prove only the external boundaries that deterministic tests cannot prove.

</success_criteria>

<output>
After completion, create `03-01-SUMMARY.md` and `03-QUALIFICATION.md`.
</output>
