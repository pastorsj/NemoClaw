---
phase: NCC-03-fabric-headless
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
autonomous: true
requirements:
  - AGENT-03
  - FABRIC-01
  - FABRIC-03
  - TEST-01
must_haves:
  truths:
    - "Hermes owns its Fabric configuration, dependencies, image integration, and tests."
    - "Hermes uses the released Fabric adapter without adding a Hermes branch to the generic runner."
    - "Native Hermes and Fabric model routes change or recover as one transaction."
    - "Fabric configuration contains no inference credential value."
---

<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

# Phase 3 Plan 3: Hermes Fabric Adapter

<objective>
Connect Hermes to the generic Fabric runner through the released Hermes adapter. Preserve the
native gateway, dashboard, MCP, state, and configuration lifecycle.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add the released Hermes adapter</name>
  <action>Install the exact released adapter in an isolated environment. Add credential-free Fabric configuration, manifest command, image files, and doctor checks. Pass the managed credential only by environment name.</action>
  <verify>`npm --prefix packages/nemoclaw-hermes run test:fabric` and `npm --prefix packages/nemoclaw-hermes run test:fabric:composed` pass.</verify>
</task>

<task type="auto">
  <name>Task 2: Make route changes atomic</name>
  <action>Project the native model and base URL into `fabric.json`. Validate and commit native configuration, Fabric configuration, MCP state, and integrity hashes through the existing journal and rollback path. Reject links, unsafe endpoints, races, stale state, and serialized secrets.</action>
  <verify>Hermes config, guard, transaction, recovery, state, startup, and image tests pass.</verify>
</task>

<task type="auto">
  <name>Task 3: Extend lifecycle evidence</name>
  <action>Use the public Fabric command in the existing inference-switch, Shields, managed-activation, and rebuild E2E paths. Provision the Fabric artifact directory before Shields locks the root. Keep messaging live tests excluded.</action>
  <verify>Focused Hermes package, core lifecycle, E2E-support, CLI typecheck, and diff checks pass.</verify>
</task>

</tasks>

<success_criteria>

- Hermes-specific Fabric configuration and dependencies live under `packages/nemoclaw-hermes`.
- The generic runner discovers `nvidia.fabric.hermes` from the package descriptor.
- Existing native Hermes behavior and integrity controls include, but do not depend on, Fabric.
- Plan 03-04 owns live Mac and Brev results and all support claims remain excluded.

</success_criteria>

<output>
After deterministic verification is final, create `03-03-SUMMARY.md`. Plan 03-04 records live
qualification.
</output>
