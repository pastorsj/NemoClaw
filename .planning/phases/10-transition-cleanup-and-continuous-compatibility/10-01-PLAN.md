---
phase: 10-transition-cleanup-and-continuous-compatibility
plan: 01
type: execute
wave: 1
depends_on: ["09-06"]
files_modified:
  - src/lib/agent-runtime-packages/legacy-receipt.ts
  - src/lib/agent-runtime-packages/legacy-receipt.test.ts
  - src/lib/agent-runtime-packages/references.ts
  - src/lib/state/registry/types.ts
  - src/lib/state/registry/persistence.ts
  - src/lib/state/registry/rebuild-authority.ts
  - src/lib/state/sandbox.ts
  - src/lib/state/registry-package-migration.test.ts
  - test/package-contract/state/previous-release-package-migration.test.ts
autonomous: true
requirements: [COMP-03, PKG-07, CTL-05]
must_haves:
  truths:
    - "D-01: Legacy sandbox and snapshot state migrates only when checked-in evidence proves its exact package identity."
    - "D-02: Package availability, sandbox deletion, snapshot retention, and durable-state deletion remain separate lifecycle decisions."
    - "D-07: Existing state owners call the one Phase 8 pure legacy-evidence-to-receipt resolver; Phase 10 adds no mapper or schema-migration service."
    - An ambiguous record remains bounded legacy state or fails with remediation instead of selecting another runtime.
    - Registry, session, checkpoint, snapshot, rebuild, restore, and recovery reuse the same exact resolved receipt while each state owner preserves its current parsing and persistence boundary.
  artifacts:
    - src/lib/state/registry-package-migration.test.ts
    - test/package-contract/state/previous-release-package-migration.test.ts
  key_links:
    - Registry persistence and other state owners call `resolveLegacyPackageReceipt`; rebuild, restore, and recovery consume the persisted exact receipt and static support status.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Complete exact package-receipt migration for supported legacy registry and snapshot state.

Purpose: Preserve previous-release sandboxes without guessing an identity or weakening rebuild authority.
Output: Bounded migration reader, exact authority wiring, and previous-release package-contract fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-transition-cleanup-and-continuous-compatibility/10-CONTEXT.md
@.planning/phases/08-in-tree-release-qualification/08-04-SUMMARY.md
@.planning/phases/09-external-repository-handoff/09-06-SUMMARY.md
@src/lib/state/registry/types.ts
@src/lib/state/registry/persistence.ts
@src/lib/state/registry/rebuild-authority.ts
@src/lib/state/sandbox.ts
</context>

<decision_coverage>
- D-01: Exact legacy migration requires checked-in identity evidence; ambiguous state is retained or rejected.
- D-02: Package, sandbox, snapshot, and durable-state lifecycles stay separate.
- D-03: Transition code is removed only after its accepted support window and fixtures permit removal.
- D-04: Remaining runtime names must fit an approved data, migration, presentation, qualification, or shim category.
- D-05: A reviewed release-set change binds exact NemoClaw, OpenShell, package, image, protocol, provider, render, and suite identities.
- D-06: Every failed update or revocation path retains the previous exact tuple and credential-free evidence.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define exact and bounded legacy migration outcomes</name>
  <files>src/lib/agent-runtime-packages/legacy-receipt.ts, src/lib/agent-runtime-packages/legacy-receipt.test.ts, src/lib/agent-runtime-packages/references.ts, src/lib/state/registry/types.ts, src/lib/state/registry/persistence.ts, src/lib/state/registry-package-migration.test.ts</files>
  <read_first>.planning/phases/10-transition-cleanup-and-continuous-compatibility/10-CONTEXT.md, .planning/phases/08-in-tree-release-qualification/08-04-SUMMARY.md, .planning/phases/09-external-repository-handoff/09-06-SUMMARY.md, src/lib/agent-runtime-packages/legacy-receipt.ts, src/lib/agent-runtime-packages/references.ts, src/lib/state/registry/types.ts, src/lib/state/registry/persistence.ts, src/lib/agent/defs.ts, src/lib/state/registry-normalization.test.ts, release/agent-runtime-packages.json</read_first>
  <behavior>
    - A supported legacy OpenClaw, Hermes, or Deep Agents Code record maps to one exact qualified package receipt only when immutable evidence matches.
    - Unknown, conflicting, or incomplete evidence never falls back to OpenClaw or the current default.
    - Snapshot and durable-state references remain independent of package availability and sandbox deletion.
  </behavior>
  <action>Per D-01, D-02, and D-07, extend—not fork—the pure `resolveLegacyPackageReceipt` function with only the remaining checked-in legacy identity rows and closed exact, bounded-retention, and remediation-required outcomes. Registry persistence supplies already-normalized legacy agent, version, image, source revision, cohort, platform, and `workload` evidence; the resolver does not read, parse, migrate, or persist registry files. Return the canonical package receipt and immutable qualification evidence reference. Registry persistence retains its own schema migration and atomic write path and keeps package, sandbox, snapshot, and durable-state references separate. Require static `supported` status for a package-dependent mutation; `revoked` or `superseded` returns remediation without fallback and never rewrites qualification evidence. Do not infer from display names, current defaults, mutable tags, source location, or external transport.</action>
  <verify><automated>npx vitest run --project cli src/lib/state/registry-package-migration.test.ts</automated></verify>
  <acceptance_criteria>
    - Exact migration requires all evidence defined by the retained compatibility table.
    - Ambiguous evidence produces no new package receipt.
    - Package, sandbox, snapshot, and durable-state lifecycles remain separate.
    - Static and mutation guards prove registry code delegates identity mapping to the pure resolver and contains no parallel table.
    - Qualification identity remains unchanged while static support status is evaluated independently.
  </acceptance_criteria>
  <done>Registry persistence returns one deterministic, fail-closed migration outcome for every supported legacy fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind rebuild and previous-release compatibility to migration results</name>
  <files>src/lib/state/registry/rebuild-authority.ts, src/lib/state/sandbox.ts, test/package-contract/state/previous-release-package-migration.test.ts</files>
  <read_first>src/lib/agent-runtime-packages/legacy-receipt.ts, src/lib/agent-runtime-packages/references.ts, src/lib/state/registry/rebuild-authority.ts, src/lib/state/sandbox.ts, src/lib/state/registry-package-migration.test.ts, src/lib/onboard/sandbox-recreate-transaction.ts, src/lib/actions/sandbox/rebuild-pipeline.ts, src/lib/actions/sandbox/rebuild-recreate-journal.ts, test/package-contract/helpers/package-fixture.ts</read_first>
  <behavior>
    - Rebuild and restore use an exact migrated receipt or stop before destructive work.
    - A previous-release packed state fixture preserves the same sandbox authority after migration.
  </behavior>
  <action>Per D-01, D-03, and D-07, require an exact persisted receipt plus static `supported` status before the production rebuild, restore, or recovery path creates replacement state. Add compiled-artifact fixtures from the accepted previous-release window. Assert that remediation-required, `revoked`, `superseded`, missing qualification evidence, or receipt mismatch performs no provider, policy, credential, port, state, or registry mutation. Preserve the existing sandbox recreate/rebuild journal and authority-transfer semantics. Treat helper readiness as an executor claim and require separately produced evidence with producer identity and limitations before authority transfer.</action>
  <verify><automated>npx vitest run --project package-contract test/package-contract/state/previous-release-package-migration.test.ts</automated></verify>
  <acceptance_criteria>
    - Destructive phases cannot start from ambiguous identity.
    - Exact migration preserves package, image, provider, policy, credential-binding, port, and state authority.
    - The packed CLI passes previous-release fixtures.
    - `revoked` and `superseded` preserve qualification evidence, block package-dependent mutations without fallback, and do not silently stop, migrate, or delete an existing sandbox.
  </acceptance_criteria>
  <done>Supported legacy state reaches the exact package lifecycle, while ambiguous state stops before mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Legacy persisted state to current lifecycle authority | Old or edited registry and snapshot data can influence destructive operations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-10-01 | Spoofing | Legacy package identity | mitigate | Require retained immutable evidence and reject current-default inference. |
| T-10-02 | Tampering | Registry and snapshot migration | mitigate | Validate before mutation and bind rebuild to the exact migration receipt. |
| T-10-09 | Spoofing | Duplicate migration authority | mitigate | Keep schema migration in existing state owners and reject duplicate legacy identity maps through tests and the boundary guard. |
| T-10-10 | Elevation of privilege | Support during destructive lifecycle | mitigate | Require static `supported` status and independent readiness evidence before mutation or authority transfer. |
</threat_model>

<verification>
- [ ] Source migration tests pass.
- [ ] Compiled previous-release package-contract tests pass.
- [ ] Ambiguous identity performs no destructive operation.
</verification>

<success_criteria>
- Every supported legacy record has a deterministic exact or bounded outcome.
- Rebuild and restore never guess package authority.
</success_criteria>

<output>
Create `.planning/phases/10-transition-cleanup-and-continuous-compatibility/10-01-SUMMARY.md` when done.
</output>
