---
phase: 10-transition-cleanup-and-continuous-compatibility
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/lib/agent/definition-types.ts
  - src/lib/agent/defs.ts
  - src/lib/agent/runtime-manifest.ts
  - scripts/checks/agent-runtime-core-boundary.mts
  - scripts/checks/run.mts
  - test/agent-runtime-core-boundary.test.ts
  - proposals/agent-runtime-packages/FINAL-DISPOSITION-LEDGER.md
autonomous: true
requirements: [COMP-01, SEC-02]
must_haves:
  truths:
    - "D-03: A compatibility reader or transition field is removed only after its accepted support window and previous-release fixtures permit removal."
    - "D-04: Runtime-neutral core contains no runtime-native import, repository-root package dependency, or runtime-name executable dispatch; every remaining standard runtime name has an approved data, migration, presentation, qualification, or compatibility reason."
  artifacts:
    - scripts/checks/agent-runtime-core-boundary.mts
    - proposals/agent-runtime-packages/FINAL-DISPOSITION-LEDGER.md
  key_links:
    - The repository check consumes the approved remaining-reference inventory and runs through `npm run checks:repository`.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Remove expired transition models and enforce the final core-to-package source boundary.

Purpose: Convert the architecture rule into a repository check that prevents runtime-native code from returning to core.
Output: Reduced legacy types, final disposition ledger, boundary checker, and behavior tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-transition-cleanup-and-continuous-compatibility/10-CONTEXT.md
@.planning/phases/10-transition-cleanup-and-continuous-compatibility/10-01-SUMMARY.md
@src/lib/agent/definition-types.ts
@src/lib/agent/defs.ts
@scripts/checks/run.mts
@proposals/agent-runtime-packages/RUNTIME-INVENTORY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove transition fields and close the disposition ledger</name>
  <files>src/lib/agent/definition-types.ts, src/lib/agent/defs.ts, src/lib/agent/runtime-manifest.ts, proposals/agent-runtime-packages/FINAL-DISPOSITION-LEDGER.md</files>
  <read_first>.planning/phases/10-transition-cleanup-and-continuous-compatibility/10-CONTEXT.md, .planning/phases/10-transition-cleanup-and-continuous-compatibility/10-01-SUMMARY.md, src/lib/agent/definition-types.ts, src/lib/agent/defs.ts, proposals/agent-runtime-packages/RUNTIME-INVENTORY.md</read_first>
  <action>Per D-03 and D-04, remove `_legacy_paths`, convention-derived package paths, closed runtime adapter unions, shell-form package commands, and compatibility readers whose accepted window ended. Preserve only migration behavior proven necessary by 10-01. Create the final ledger from the audited candidate lists and classify every remaining runtime-name reference as release data, bounded migration, public presentation, qualification, reviewed shim, or defect.</action>
  <verify><automated>npm run typecheck:cli</automated></verify>
  <acceptance_criteria>
    - No expired field or path inference remains in current writes.
    - Every audited candidate has a final disposition and owning repository.
    - Remaining compatibility behavior cites its accepted support window and test.
  </acceptance_criteria>
  <done>Core types model exact packages and generic lifecycle authority without expired repository-layout compatibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the one agent-runtime core boundary check</name>
  <files>scripts/checks/agent-runtime-core-boundary.mts, scripts/checks/run.mts, test/agent-runtime-core-boundary.test.ts</files>
  <read_first>proposals/agent-runtime-packages/FINAL-DISPOSITION-LEDGER.md, scripts/checks/agent-runtime-core-boundary.mts, scripts/checks/run.mts, scripts/checks/vitest-project-overlap.mts, test/agent-runtime-core-boundary.test.ts, test/growth-guardrails.test.ts</read_first>
  <behavior>
    - The check rejects a core import from an external package source or runtime-native module.
    - The check rejects a new runtime-name executable switch in a runtime-neutral directory.
    - The check rejects direct OpenShell calls outside the one NemoClaw adapter and rejects package helper lifecycle or process-supervision authority, while narrowly allowing only the three inventoried package-local descendant compatibility wrappers.
    - Approved release-data, migration, presentation, qualification, and shim references pass.
  </behavior>
  <action>Per D-04 and D-10, finish the one `agent-runtime-core-boundary` checker created in Phase 6; do not create or retain a parallel runtime-package guard. Drive its narrow allowlist from the final ledger. Reject runtime-native imports, repository-root dependencies, name-based executable dispatch, a package/helper call to OpenShell, a direct OpenShell call outside `src/lib/adapters/openshell/client.ts`, and any package helper create/start/stop/restart/delete/recover/supervise authority for the sandbox or admitted process. Add one closed exception for the existing OpenClaw `nemoclaw-start.sh`, Hermes `start.sh`, and Deep Agents Code `dcode-session-supervisor.py` package-local wrappers, and verify they manage descendants only. Reject a fourth wrapper, a shared supervisor abstraction, an OpenShell call, or sandbox-lifecycle ownership. Register the existing checker once in the repository-check runner and test representative accepted and rejected fixtures. Do not use a broad text ban that would reject release sets, tests, migrations, or user-visible names.</action>
  <verify><automated>npx vitest run --project integration test/agent-runtime-core-boundary.test.ts</automated></verify>
  <acceptance_criteria>
    - Tests prove package-import, repo-root dependency, and executable-dispatch rejection.
    - Tests prove the single OpenShell adapter rule, the exact three-wrapper descendant-only exception, and rejection of every broader package lifecycle or process-supervision authority.
    - The allowlist contains a reason and owning test for each permitted category.
    - `npm run checks:repository` invokes the new check.
  </acceptance_criteria>
  <done>Repository checks prevent runtime-native execution and source coupling from re-entering core.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| External package source to core host process | An import or dispatch regression could execute package-controlled code with host authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-10-03 | Elevation of privilege | Core imports and dispatch | mitigate | Reject runtime-native dependencies and name-based execution through a repository check. |
| T-10-04 | Repudiation | Remaining runtime references | mitigate | Require a final classified ledger with an owner and reason. |
| T-10-11 | Elevation of privilege | OpenShell and process authority | mitigate | Enforce one NemoClaw client boundary, allow only the three audited descendant wrappers, and reject every broader package lifecycle or process-supervision authority. |
</threat_model>

<verification>
- [ ] Type checking passes after transition removal.
- [ ] Boundary behavior tests pass.
- [ ] `npm run checks:repository` includes the boundary check.
- [ ] The final ledger has no unclassified candidate.
</verification>

<success_criteria>
- Core source cannot silently regain runtime-native execution.
- Remaining runtime names are explicit, bounded, and reviewable.
</success_criteria>

<output>
Create `.planning/phases/10-transition-cleanup-and-continuous-compatibility/10-02-SUMMARY.md` when done.
</output>
