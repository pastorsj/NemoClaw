---
phase: 10-transition-cleanup-and-continuous-compatibility
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - release/agent-runtime-packages.json
  - release/agent-runtime-packages.schema.json
  - src/lib/agent-runtime-packages/release-set.ts
  - src/lib/agent-runtime-packages/release-set.test.ts
  - src/lib/agent-runtime-packages/catalogue.ts
  - src/lib/agent-runtime-packages/catalogue.test.ts
  - src/lib/agent-runtime-packages/selection.ts
  - src/lib/agent-runtime-packages/selection.test.ts
  - src/lib/agent-runtime-packages/references.ts
  - src/lib/onboard/sandbox-recreate-transaction.ts
  - src/lib/onboard/sandbox-recreate-transaction.test.ts
  - src/lib/actions/sandbox/rebuild-pipeline.ts
  - src/lib/actions/sandbox/rebuild-recreate-journal.ts
  - src/lib/actions/sandbox/rebuild-recreate-journal.test.ts
  - src/lib/onboard/runtime-provider/snapshot.ts
  - src/lib/onboard/runtime-provider/snapshot.test.ts
  - tools/e2e/workflow-plan.mts
  - tools/e2e/release-qualification.mts
  - .github/workflows/e2e.yaml
  - test/e2e/support/workflow-plan.test.ts
  - test/e2e/support/release-qualification.test.ts
autonomous: true
requirements: [COMP-02, COMP-03, PKG-07, SEC-04, CTL-05]
must_haves:
  truths:
    - "D-05: A reviewed release-set change is blocked when any exact NemoClaw, OpenShell, package, image, controller-protocol, inference-provider, messaging-render, or suite identity is missing, stale, mismatched, or non-supported."
    - "D-06: A failed fetch, verification, qualification, update, or support-status check blocks merge or release and leaves the previously shipped release and installed receipts unchanged."
    - "D-08: Immutable qualification evidence references remain unchanged when a reviewed static status changes among `supported`, `revoked`, and `superseded`."
    - "D-09: Executor claims cannot satisfy independent evidence; unknown helper operations or mutation outside the declared scope block release and package-dependent mutation. Contract V1 has no read-only helper operation."
    - Only `supported` entries allow normal selection, onboarding, rebuild, restore, recovery, start, or reconciliation. Core-owned diagnosis and explicit cleanup remain available for non-supported entries.
  artifacts:
    - Static support-status schema and operation guards
    - Extended `tools/e2e/workflow-plan.mts` and `tools/e2e/release-qualification.mts`
    - Existing protected E2E workflow and deterministic workflow tests
  key_links:
    - Catalogue, selection, and production mutation paths consume the same status from the one checked-in release set.
    - The existing typed target planner derives required checks for an exact reviewed revision; package repositories cannot inject jobs.
    - Compatibility failure blocks normal review/release instead of mutating a runtime release head.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Gate static release-set changes on exact cross-project compatibility and enforce fail-closed support status.

Purpose: Manage independent NemoClaw, OpenShell, runtime, provider, and messaging-render updates without adding a second release or lifecycle system.
Output: Static support semantics, production mutation guards, existing typed workflow planning, and deterministic qualification checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-transition-cleanup-and-continuous-compatibility/10-CONTEXT.md
@release/agent-runtime-packages.json
@tools/e2e/workflow-plan.mts
@tools/e2e/release-qualification.mts
@tools/e2e/target-catalogue.mts
@.github/workflows/e2e.yaml
@test/e2e/support/release-qualification.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce static support status at selection and mutation edges</name>
  <files>release/agent-runtime-packages.json, release/agent-runtime-packages.schema.json, src/lib/agent-runtime-packages/release-set.ts, src/lib/agent-runtime-packages/release-set.test.ts, src/lib/agent-runtime-packages/catalogue.ts, src/lib/agent-runtime-packages/catalogue.test.ts, src/lib/agent-runtime-packages/selection.ts, src/lib/agent-runtime-packages/selection.test.ts, src/lib/agent-runtime-packages/references.ts, src/lib/onboard/sandbox-recreate-transaction.ts, src/lib/onboard/sandbox-recreate-transaction.test.ts, src/lib/actions/sandbox/rebuild-pipeline.ts, src/lib/actions/sandbox/rebuild-recreate-journal.ts, src/lib/actions/sandbox/rebuild-recreate-journal.test.ts, src/lib/onboard/runtime-provider/snapshot.ts, src/lib/onboard/runtime-provider/snapshot.test.ts</files>
  <read_first>
    - .planning/phases/10-transition-cleanup-and-continuous-compatibility/10-CONTEXT.md
    - release/agent-runtime-packages.json
    - release/agent-runtime-packages.schema.json
    - src/lib/agent-runtime-packages/release-set.ts
    - src/lib/agent-runtime-packages/catalogue.ts
    - src/lib/agent-runtime-packages/selection.ts
    - src/lib/agent-runtime-packages/references.ts
    - src/lib/onboard/sandbox-recreate-transaction.ts
    - src/lib/actions/sandbox/rebuild-pipeline.ts
    - src/lib/actions/sandbox/rebuild-recreate-journal.ts
    - src/lib/onboard/runtime-provider/snapshot.ts
  </read_first>
  <behavior>
    - The release-set schema accepts only `supported`, `revoked`, or `superseded` and keeps immutable qualification evidence references separate.
    - `supported` entries retain current selection and lifecycle behavior.
    - `revoked` and `superseded` entries remain visible with remediation but cannot be newly selected or used for package-dependent onboarding, start, reconcile, rebuild, restore, or recovery.
    - Non-supported status never silently chooses another package or automatically stops, migrates, or deletes a running sandbox; core-owned status/doctor and explicit destroy remain available.
  </behavior>
  <action>PKG-07 and D-08: keep support status as one closed field in the checked-in release set and add no parallel release-state or transition system. Preserve immutable qualification evidence references byte-for-byte when status changes in a reviewed pull request. Make the catalogue and list commands show the static status while selection accepts only `supported`. Revalidate `supported` at the production mutation edges used by sandbox recreate, the rebuild pipeline/journal, snapshot restore, and recovery; do not introduce a second lifecycle or journal. A non-supported entry stops before provider, policy, credential, port, state, or registry mutation and never falls back to another version or runtime. Permit credential-free core-owned diagnosis and explicit cleanup so revocation cannot strand user resources. SEC-04, CTL-05, and D-09: independently produced evidence of an unknown helper operation, mutation outside the declared scope, or a false executor claim can justify a reviewed status change, but package/helper claims cannot change status themselves. Do not add a read-only helper or before/after enforcement path.</action>
  <verify>
    <automated>npx vitest run --project cli src/lib/agent-runtime-packages/release-set.test.ts src/lib/agent-runtime-packages/catalogue.test.ts src/lib/agent-runtime-packages/selection.test.ts src/lib/onboard/sandbox-recreate-transaction.test.ts src/lib/actions/sandbox/rebuild-recreate-journal.test.ts src/lib/onboard/runtime-provider/snapshot.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Production types reject unknown statuses and any mutable-head or transition fields.
    - Qualification evidence references remain byte-identical across supported, revoked, and superseded fixtures.
    - Selection and every named package-dependent mutation fail closed for non-supported entries without fallback or side effects.
    - Existing workloads are not silently stopped, migrated, or deleted; core-owned diagnosis and explicit cleanup are tested.
    - Focused CLI tests exit 0.
  </acceptance_criteria>
  <done>One static support field governs product selection and mutations while the production lifecycle stays unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reuse the existing typed planner for compatibility review</name>
  <files>tools/e2e/workflow-plan.mts, tools/e2e/release-qualification.mts, .github/workflows/e2e.yaml, test/e2e/support/workflow-plan.test.ts, test/e2e/support/release-qualification.test.ts</files>
  <read_first>
    - .planning/phases/10-transition-cleanup-and-continuous-compatibility/10-CONTEXT.md
    - tools/e2e/workflow-plan.mts
    - tools/e2e/release-qualification.mts
    - tools/e2e/target-catalogue.mts
    - release/agent-runtime-packages.json
    - .github/workflows/e2e.yaml
    - test/e2e/support/workflow-plan.test.ts
    - test/e2e/support/release-qualification.test.ts
  </read_first>
  <behavior>
    - A proposed checked-in release-set revision expands through the existing planner into exact identities plus the full no-messaging Ubuntu lifecycle for all three packages, deterministic WSL, six native image cells, the real Apple silicon macOS OpenClaw journey on Docker Desktop or Colima, and the official staging Brev Launchable's baked default OpenClaw journey.
    - Protected Brev evidence is accepted only for the exact commit on a maintainer-controlled `NVIDIA/NemoClaw` candidate branch or for exact merged `main`; a contributor fork cannot receive Launchable secrets.
    - Missing or duplicate dimensions and package-selected target IDs are rejected.
    - Every required job result and evidence subject belongs to the same exact revision and release-set digest.
    - Any failed or mixed result blocks the pull request or release; it never changes product state.
  </behavior>
  <action>Per D-05 and D-06, extend `workflow-plan.mts` and the existing release-qualification evaluator instead of creating a compatibility planner or decision service. Derive jobs from the static release-set diff, core-owned capability declarations, existing typed targets, and accepted platform policy. Require exact package, image, OpenShell, provider, controller, mutation-capable helper, deterministic messaging-render, and suite identities; the Ubuntu full lifecycle for all three packages; deterministic WSL; six native image cells; the real Apple silicon macOS OpenClaw journey on Docker Desktop or Colima; and the official staging Brev Launchable's baked default OpenClaw journey. Preserve the protected trust gate: a pre-merge fork commit must be mirrored unchanged by a maintainer to an `NVIDIA/NemoClaw` candidate branch; otherwise qualify the exact merged `main` commit before release. Do not grant Launchable secrets to contributor forks. Do not add a generic Brev source-install lane or require Hermes or Deep Agents Code on Brev. CTL-05: require executor claims and independent observations to retain producer, method, subject, time, and limitations; claims cannot satisfy independent evidence obligations. SEC-04 and D-09: check only the two declared mutation-capable helper operations, unknown-operation rejection, and out-of-declared-scope mutation rejection. Contract V1 has no read-only helper operation or before/after enforcement. Update the existing E2E workflow only to expose planner-selected release checks for an exact reviewed revision. Do not auto-discover upstream versions or let an external repository select jobs: a maintainer opens an ordinary NemoClaw pull request for an exact update, CI and protected E2E evaluate it, and merge/release is the only support change. A failed check leaves the previously shipped revision authoritative and produces structured redacted evidence.</action>
  <verify>
    <automated>npx vitest run --project e2e-support test/e2e/support/workflow-plan.test.ts test/e2e/support/release-qualification.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every exact identity and required compatibility dimension appears once in the existing plan.
    - Protected Brev evidence is bound to the exact commit on a maintainer-controlled `NVIDIA/NemoClaw` candidate branch or exact merged `main`, and no contributor-fork workflow receives Launchable secrets.
    - Planner fixtures reject missing, duplicate, stale, package-injected, claim-only, unknown-producer, missing-limitation, and mixed-revision coverage.
    - Workflow YAML consumes the typed planner instead of maintaining a second matrix.
    - Failure blocks merge/release and leaves the static shipped release and installed receipts unchanged.
    - Output is deterministic and credential-free.
  </acceptance_criteria>
  <done>Independent updates enter NemoClaw through an ordinary exact release-set pull request and the existing qualification workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Cross-repository evidence to reviewed NemoClaw change | Multiple independently controlled sources inform one core-owned static release decision. |
| Static support status to production mutation | Edited release data can permit or deny security-sensitive package operations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-10-05 | Spoofing | Release and job evidence | mitigate | Bind every result to exact source, package, image, platform, protocol, release-set, and suite identities. |
| T-10-06 | Tampering | Release-set change | mitigate | Use one reviewed static file and block merge/release on any failed or mixed result. |
| T-10-12 | Repudiation | Support status | mitigate | Require ordinary code review and immutable qualification evidence references for every static status change. |
| T-10-13 | Spoofing | Helper or publisher evidence | mitigate | Preserve claims and require separately produced trusted observations with producer and limitations. |
</threat_model>

<verification>
- [ ] Static support-status and production mutation tests pass.
- [ ] Existing workflow-plan and release-qualification tests pass.
- [ ] E2E-support workflow tests prove one typed source of truth.
- [ ] A failed reviewed revision cannot affect the shipped release or installed receipts.
</verification>

<success_criteria>
- Relevant independent updates trigger complete exact compatibility evidence through the existing planner.
- No failed or incomplete reviewed revision can replace the shipped static release set.
- Revocation and supersession remain understandable static code-review changes rather than a parallel state system.
</success_criteria>

<output>
Create `.planning/phases/10-transition-cleanup-and-continuous-compatibility/10-03-SUMMARY.md` when done.
</output>
