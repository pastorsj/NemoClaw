---
phase: 01-scope-and-behavior-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/onboard/agent-selection.test.ts
  - src/lib/onboard/agent-resume-state.test.ts
  - src/lib/state/onboard-session-normalization.test.ts
  - src/lib/state/registry-normalization.test.ts
autonomous: true
requirements: [UX-01, UX-02, TEST-01]
user_setup: []
must_haves:
  truths:
    - "D-04: New tests fill the current menu and persisted-identity gaps; existing tests continue to protect flags, environment input, agents list, and compatibility launchers."
    - "D-08: Messaging coverage remains deterministic and requires no live service account."
    - Tests characterize legacy null OpenClaw session and registry records before Phase 2 changes their write form.
  artifacts:
    - path: src/lib/onboard/agent-selection.test.ts
      provides: Menu, default, explicit-input, and non-interactive selection baseline.
    - path: src/lib/onboard/agent-resume-state.test.ts
      provides: Legacy OpenClaw and explicit runtime resume-write baseline.
    - path: src/lib/state/onboard-session-normalization.test.ts
      provides: Persisted session normalization baseline.
  key_links:
    - from: src/lib/onboard/agent-selection.test.ts
      to: src/lib/onboard/agent-selection.ts
      via: Observable selection behavior at the dependency seam.
    - from: test/nemohermes-alias.test.ts
      to: package.json
      via: Existing published compatibility executable coverage that this plan reruns without modification.
    - from: src/lib/state/registry-normalization.test.ts
      to: src/lib/state/registry-normalization.ts
      via: Legacy and current runtime identity fixtures.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Capture the current user-visible and persisted-state behavior as executable characterization tests.

Purpose: Detect any migration change to selection, compatibility commands, or resumability before runtime code moves.
Output: Focused source and integration tests for all three standard agent runtimes, including the temporary legacy null OpenClaw representation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scope-and-behavior-baseline/01-CONTEXT.md
@src/lib/onboard/agent-selection.ts
@src/lib/agent/defs.ts
@src/lib/agent/onboard.ts
@src/lib/agent/runtime.ts
@src/lib/state/onboard-session.ts
@src/lib/state/registry-normalization.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Characterize the missing menu behavior</name>
  <files>src/lib/onboard/agent-selection.test.ts</files>
  <read_first>.planning/phases/01-scope-and-behavior-baseline/01-CONTEXT.md, src/lib/onboard/agent-selection.ts, src/lib/onboard/agent-selection.test.ts, src/lib/agent/defs.ts, src/lib/agent/defs.test.ts, src/lib/agent/list-command.test.ts, src/lib/onboard/command.test.ts, agents/openclaw/manifest.yaml, agents/hermes/manifest.yaml, agents/langchain-deepagents-code/manifest.yaml</read_first>
  <action>Per D-04, add only the missing behavior-oriented cases for the exact three standard menu entries, their current labels and descriptions, OpenClaw-first order, Choose [1] default, Deep Agents Code selection, non-interactive note, and the legacy null result for OpenClaw. Reuse the existing tests for flag and environment precedence, list rendering, command dispatch, and compatibility launchers instead of duplicating them. Assert outcomes through injected dependencies; do not bind tests to ANSI, spinner, timing, or incidental whitespace.</action>
  <verify><automated>npx vitest run --project cli src/lib/onboard/agent-selection.test.ts src/lib/agent/defs.test.ts src/lib/agent/list-command.test.ts src/lib/onboard/command.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - One test fails if OpenClaw is no longer first or default before Phase 2 intentionally updates it.
    - All three current labels and selectable names are covered.
    - Deep Agents Code, interactive, and non-interactive paths fill the current focused-test gaps.
    - Existing flag, environment, list, and command tests still pass without duplicate assertions.
    - Assertions avoid terminal rendering details that are not product contracts.
  </acceptance_criteria>
  <done>The focused source suite records the current menu journey without duplicating existing command coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Characterize persisted identity and resume state</name>
  <files>src/lib/onboard/agent-resume-state.test.ts, src/lib/state/onboard-session-normalization.test.ts, src/lib/state/registry-normalization.test.ts</files>
  <read_first>.planning/phases/01-scope-and-behavior-baseline/01-CONTEXT.md, src/lib/onboard/agent-resume-state.ts, src/lib/onboard/agent-resume-state.test.ts, src/lib/state/onboard-session.ts, src/lib/state/onboard-session-normalization.test.ts, src/lib/state/registry/types.ts, src/lib/state/registry-normalization.ts, src/lib/state/registry-normalization.test.ts</read_first>
  <action>Per D-04 and D-08, add table-driven fixtures for absent, null, and explicit OpenClaw identity; explicit Hermes and langchain-deepagents-code identity; resumed writes; unknown strings; malformed non-string input; completed and interrupted step state; and unrelated registry-field preservation. Record current serialized and normalized outcomes before Phase 2 changes them. Include deterministic messaging-plan preservation only where it is already part of session or registry normalization; do not add live channel enrollment or service calls.</action>
  <verify><automated>npx vitest run --project cli src/lib/onboard/agent-resume-state.test.ts src/lib/state/onboard-session-normalization.test.ts src/lib/state/registry-normalization.test.ts --coverage=false</automated></verify>
  <acceptance_criteria>
    - Legacy null and absent OpenClaw fixtures are explicit and named as migration inputs.
    - Hermes and Deep Agents Code fixtures keep their canonical identity through normalize and resume.
    - Unknown, malformed, and unrelated fields have characterized current outcomes.
    - No credential values or live messaging accounts appear in fixtures.
  </acceptance_criteria>
  <done>The suite gives Phase 2 a precise before-state for sessions, registries, and resume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User input to runtime selection | Flags, environment, prompts, and aliases select code and resource behavior. |
| User-writable state to trusted runtime lookup | Session and registry identities must not become unchecked filesystem or runtime selectors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-04 | Spoofing | Persisted runtime identity | mitigate | Characterize known, absent, null, malformed, and unknown identities before fail-closed migration. |
| T-01-05 | Tampering | Compatibility inputs | mitigate | Test precedence and exact canonical selection across flag, environment, resume, and launcher paths. |
| T-01-06 | Information Disclosure | Messaging fixtures | avoid | Keep characterization deterministic and credential-free; exclude live service accounts. |
</threat_model>

<verification>
- [ ] Both focused CLI commands pass in less than 60 seconds each.
- [ ] `npx vitest run --project integration test/nemohermes-alias.test.ts test/nemo-deepagents-alias.test.ts test/cli/dispatch-basics.test.ts --coverage=false` passes without modifying those existing tests.
- [ ] `npx vitest run --project package-contract test/package-contract/cli/command-registry.test.ts --coverage=false` confirms compiled command registration.
- [ ] Test names describe user-visible or durable behavior.
- [ ] Every UX-01 and UX-02 compatibility surface has an assertion.
</verification>

<success_criteria>
- Later refactors cannot silently change the current menu, default, aliases, command registration, or persisted-state behavior.
- The legacy null representation is documented by tests rather than treated as the desired end state.
</success_criteria>

<output>
Create .planning/phases/01-scope-and-behavior-baseline/01-02-SUMMARY.md when done.
</output>
