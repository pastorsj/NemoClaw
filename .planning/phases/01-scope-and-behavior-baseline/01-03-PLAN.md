---
phase: 01-scope-and-behavior-baseline
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - proposals/agent-runtime-packages/RUNTIME-INVENTORY.md
  - proposals/agent-runtime-packages/OPENCLAW-CANDIDATES.tsv
  - proposals/agent-runtime-packages/HERMES-CANDIDATES.tsv
  - proposals/agent-runtime-packages/TERMINAL-CANDIDATES.tsv
  - proposals/agent-runtime-packages/DISPOSITION-LEDGER.md
  - proposals/agent-runtime-packages/ARCHITECTURE-BASELINE.json
autonomous: true
requirements: [GOV-02, TEST-01]
user_setup: []
must_haves:
  truths:
    - "D-07: Every audited candidate path has one final move, split, keep, or delete disposition with owner, target, phase, and evidence."
    - "D-06: Every root-required action maps to retained core authority, a non-root package rewrite, or deletion."
    - "D-14: Current runtime-name branches, package dependencies, direct OpenShell call sites, and RuntimeProviderBundle consumers have an exact path-and-symbol baseline."
  artifacts:
    - path: proposals/agent-runtime-packages/OPENCLAW-CANDIDATES.tsv
      provides: Machine-checkable OpenClaw disposition rows.
    - path: proposals/agent-runtime-packages/HERMES-CANDIDATES.tsv
      provides: Machine-checkable managed, portable, and broker disposition rows.
    - path: proposals/agent-runtime-packages/TERMINAL-CANDIDATES.tsv
      provides: Machine-checkable terminal-runtime disposition rows.
    - path: proposals/agent-runtime-packages/DISPOSITION-LEDGER.md
      provides: Ledger schema, totals, split rules, privileged-action classification, and unresolved blockers.
    - path: proposals/agent-runtime-packages/ARCHITECTURE-BASELINE.json
      provides: Machine-readable path, symbol, ownership, disposition, and removal facts for architecture debt that later checks may only reduce.
  key_links:
    - from: proposals/agent-runtime-packages/DISPOSITION-LEDGER.md
      to: proposals/agent-runtime-packages/TECHNICAL-PLAN.md
      via: Core/package authority and fixed bootstrap primitive mapping.
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Complete a machine-checkable disposition and ownership ledger for the audited extraction inventory.

Purpose: Prevent whole-file moves, hidden duplicate ownership, and missed privileged actions from being discovered during runtime cutover.
Output: Three completed candidate TSVs plus a human-readable ledger contract and privileged-action table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scope-and-behavior-baseline/01-CONTEXT.md
@proposals/agent-runtime-packages/TECHNICAL-PLAN.md
@proposals/agent-runtime-packages/RUNTIME-INVENTORY.md
@proposals/agent-runtime-packages/OPENCLAW-CANDIDATES.tsv
@proposals/agent-runtime-packages/HERMES-CANDIDATES.tsv
@proposals/agent-runtime-packages/TERMINAL-CANDIDATES.tsv
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assign final dispositions to every candidate</name>
  <files>proposals/agent-runtime-packages/RUNTIME-INVENTORY.md, proposals/agent-runtime-packages/OPENCLAW-CANDIDATES.tsv, proposals/agent-runtime-packages/HERMES-CANDIDATES.tsv, proposals/agent-runtime-packages/TERMINAL-CANDIDATES.tsv, proposals/agent-runtime-packages/DISPOSITION-LEDGER.md</files>
  <read_first>.planning/phases/01-scope-and-behavior-baseline/01-CONTEXT.md, proposals/agent-runtime-packages/RUNTIME-INVENTORY.md, proposals/agent-runtime-packages/OPENCLAW-CANDIDATES.tsv, proposals/agent-runtime-packages/HERMES-CANDIDATES.tsv, proposals/agent-runtime-packages/TERMINAL-CANDIDATES.tsv, proposals/agent-runtime-packages/TECHNICAL-PLAN.md, src/lib/agent/definition-types.ts, src/lib/state/registry/types.ts, src/lib/onboard/managed-startup/profile.ts, src/lib/onboard/managed-startup/image-runtime.ts</read_first>
  <action>Apply D-06 and D-07. Regenerate the inventory against exact refreshed base `a5486894c45140259d822625e74d1ccdfce807ee`; do not reuse the old counts. Rerun the documented discovery, add every current candidate, and explicitly classify the new Hermes Portable uninstall, Hermes Discord provider-profile and binding, Bedrock provider lifecycle, provider-republication ordering, final OpenShell lifecycle release, OpenClaw doctor timeout, plugin subprocess environment, and tar 7.5.21 surfaces. Keep provider profiles, credential bindings, effective policy, provider lifecycle, transaction ordering, and lifecycle-release evidence in core. Preserve each old row whose path still exists. Record old paths removed upstream in RUNTIME-INVENTORY.md with the upstream range and reason; do not add nonexistent paths to a current-tree TSV. Replace provisional values such as move-or-rewrite and lock with exactly one final disposition: move for a whole package-owned file, split for mixed core and runtime-native symbols, keep for core authority or a central compatibility or qualification test, and delete for current behavior that becomes superseded only after a named parity gate. Add columns for current_owner, target_owner, target, phase, validation, and notes. For split rows, name the specific responsibilities on both sides. For delete rows, name the replacement and gate. Keep Portable Hermes and broker workstreams outside the managed Hermes package. Create DISPOSITION-LEDGER.md with the discovery method, current-tree exclusions, column contract, per-runtime and per-disposition totals, package/core boundary rules, unresolved blockers, and exact audit revision.</action>
  <verify><automated>node -e "const fs=require('node:fs'),cp=require('node:child_process');const base='a5486894c45140259d822625e74d1ccdfce807ee';const inventory=fs.readFileSync('proposals/agent-runtime-packages/RUNTIME-INVENTORY.md','utf8');if(!inventory.includes(base))throw new Error('inventory revision mismatch');for(const f of ['OPENCLAW-CANDIDATES.tsv','HERMES-CANDIDATES.tsv','TERMINAL-CANDIDATES.tsv']){const text=fs.readFileSync('proposals/agent-runtime-packages/'+f,'utf8');if(!text.includes(base))throw new Error(f+': revision mismatch');const rows=text.split(/\r?\n/).filter(x=>x.length>0).filter(x=>!x.startsWith('#'));const h=rows.shift().split('\t');for(const c of ['disposition','path','current_owner','target_owner','target','phase','validation','notes'])if(!h.includes(c))throw new Error(f+': missing '+c);const seen=new Set();for(const line of rows){const v=line.split('\t');const o=Object.fromEntries(h.map((k,i)=>[k,v[i]??'']));if(!['move','split','keep','delete'].includes(o.disposition))throw new Error(f+': bad disposition '+o.path);for(const c of ['path','current_owner','target_owner','target','phase','validation'])if(!o[c])throw new Error(f+': empty '+c+' for '+o.path);if(seen.has(o.path))throw new Error(f+': duplicate '+o.path);seen.add(o.path);cp.execFileSync('git',['cat-file','-e',base+':'+o.path])}}if(!fs.existsSync('proposals/agent-runtime-packages/DISPOSITION-LEDGER.md'))throw new Error('missing ledger')"</automated></verify>
  <acceptance_criteria>
    - No candidate row is removed or left with a provisional disposition.
    - Every split row names symbol-level ownership on both sides.
    - Every delete row names a replacement and parity gate.
    - Portable Hermes, the tool broker, Pi, and NemoCUA remain outside the three standard package extraction commitments.
    - The current-tree discovery and all three TSVs identify the refreshed base revision, and every row resolves at that revision.
    - Every material runtime-related path added between the initial and refreshed bases has an explicit row or documented current-tree exclusion, and every path removed in that range is recorded outside the current-tree TSVs.
    - Ledger totals reconcile exactly with the three TSVs.
  </acceptance_criteria>
  <done>Every audited path has one actionable, owned, test-bound migration disposition.</done>
</task>

<task type="auto">
  <name>Task 2: Record the architecture and OpenShell boundary baseline</name>
  <files>proposals/agent-runtime-packages/ARCHITECTURE-BASELINE.json, proposals/agent-runtime-packages/DISPOSITION-LEDGER.md</files>
  <read_first>.planning/phases/01-scope-and-behavior-baseline/01-CONTEXT.md, proposals/agent-runtime-packages/DISPOSITION-LEDGER.md, proposals/agent-runtime-packages/RFC-RECONCILIATION.md, src/lib/adapters/openshell/client.ts, src/lib/onboard/runtime-provider/contract.ts, src/lib/onboard/runtime-provider/registry.ts, src/lib/agent/defs.ts, src/lib/onboard/agent-selection.ts, scripts/checks/source-architecture.mts</read_first>
  <action>Apply D-06, D-07, and D-14. Create a machine-readable baseline bound to the audited commit with separate arrays for runtime-ID executable comparisons in runtime-neutral core, core imports of runtime-native implementation, runtime-package imports of core implementation, direct OpenShell CLI/API/process/transport calls outside `src/lib/adapters/openshell/client.ts`, and every `RuntimeProviderBundle` facet consumer. Keep each finding direct: category, exact path, containing symbol, current owner, disposition-ledger row, target owner, migration phase, and removal evidence. Include the current direct OpenShell adapters, including Portable Hermes uninstall, resolve, and restore-gateway-pairing. Record all 14 provider facets: plan, capabilities, preflightDoctor, gateway, workload, hostLocalInference, lifecycle, mutationAuthority, stateMutation, bootstrap, snapshot, recovery, cleanup, and containerEngine. For each facet include current consumers, retain/converge/remove decision, supported OpenShell parity, one-client-boundary target, exact E2E target, and removal condition. Treat the new Portable Hermes adapter as an explicit out-of-scope exception with a removal gate or converge it through the planned single client; do not hide it. Exclude test fixtures and declarative release data only through explicit category rules, not broad directory exemptions. Add totals to DISPOSITION-LEDGER.md and make them reconcile with the JSON arrays. Phase 3 may add syntax fingerprints when it implements the repository check; Phase 1 records reviewable ownership facts.</action>
  <verify><automated>node -e "const b=JSON.parse(require('node:fs').readFileSync('proposals/agent-runtime-packages/ARCHITECTURE-BASELINE.json','utf8'));for(const k of ['revision','runtimeIdBranches','runtimeNativeImports','packageCoreImports','directOpenShellCalls','runtimeProviderFacets'])if(!Object.hasOwn(b,k))throw new Error('missing '+k);const expected=['plan','capabilities','preflightDoctor','gateway','workload','hostLocalInference','lifecycle','mutationAuthority','stateMutation','bootstrap','snapshot','recovery','cleanup','containerEngine'];if(b.runtimeProviderFacets.length!==expected.length||expected.some(x=>!b.runtimeProviderFacets.some(y=>y.name===x)))throw new Error('facet inventory mismatch');for(const [k,v] of Object.entries(b))if(Array.isArray(v))for(const row of v){if(!row.path&amp;&amp;k!=='runtimeProviderFacets')throw new Error(k+' missing path');if(!row.disposition||!row.removalEvidence)throw new Error(k+' incomplete row')}"</automated></verify>
  <acceptance_criteria>
    - Every direct OpenShell call outside the proposed client boundary has an exact path, symbol, owner, disposition, and removal gate.
    - Every runtime-name execution branch and cross-boundary import has the same reviewable ownership facts.
    - All 14 RuntimeProviderBundle facets have consumers, disposition, parity, E2E, and removal fields.
    - Baseline totals reconcile with the disposition ledger and permit later counts only to decrease.
  </acceptance_criteria>
  <done>Phase 3 can enforce architecture direction from an accepted exact baseline instead of normalizing new violations.</done>
</task>

<task type="auto">
  <name>Task 3: Classify privileged and root-required runtime actions</name>
  <files>proposals/agent-runtime-packages/DISPOSITION-LEDGER.md</files>
  <read_first>.planning/phases/01-scope-and-behavior-baseline/01-CONTEXT.md, proposals/agent-runtime-packages/DISPOSITION-LEDGER.md, proposals/agent-runtime-packages/TECHNICAL-PLAN.md, src/lib/onboard/managed-startup/root-apply.ts, src/lib/onboard/managed-startup/image-runtime.ts, src/lib/onboard/managed-startup/shared-state-transaction.ts, src/lib/policy/index.ts, scripts/state-dir-guard.py</read_first>
  <action>Apply D-06 and D-07 by adding a privileged-action table for Deep Agents Code, NemoClaw-managed Hermes, and OpenClaw. Give each action a stable ID, current file and symbol, required identity, mutated resource, rollback owner, validation evidence, and exactly one classification: closed core/bootstrap primitive, non-root package rewrite, or delete. Map retained privileged effects only to the candidate allowlist in TECHNICAL-PLAN section 5; do not add an arbitrary shell, host path, UID, mount, capability, device, or package-defined primitive. Mark any action that cannot be classified as a Phase 3 blocker.</action>
  <verify><automated>node -e "const s=require('node:fs').readFileSync('proposals/agent-runtime-packages/DISPOSITION-LEDGER.md','utf8');for(const x of ['Deep Agents Code','NemoClaw-managed Hermes','OpenClaw','closed core/bootstrap primitive','non-root package rewrite','delete','rollback owner','Phase 3 blocker'])if(!s.includes(x))throw new Error('missing '+x)"</automated></verify>
  <acceptance_criteria>
    - All three standard runtimes have privileged-action coverage.
    - Every retained privileged effect has a closed core owner and rollback path.
    - Package-owned rewrites run non-root inside the sandbox boundary.
    - Unclassifiable actions block controller implementation instead of expanding the primitive set silently.
  </acceptance_criteria>
  <done>The ledger exposes every privileged migration risk before the controller allowlist is implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Mixed source file to new owner | A whole-file move can transfer credential, policy, state, or privileged authority unintentionally. |
| Core bootstrap to package process | Root-required operations must remain closed and core-selected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-07 | Elevation of Privilege | Split runtime files | mitigate | Require symbol-level ownership and classify every root-required effect. |
| T-01-08 | Tampering | Extraction inventory | mitigate | Preserve every audited row, enforce unique paths, and reconcile totals mechanically. |
| T-01-09 | Repudiation | Deletion and rollback | mitigate | Bind each delete to a replacement, validation gate, owner, and rollback responsibility. |
| T-01-14 | Repudiation | Architecture debt baseline | mitigate | Bind each exception to an exact path, symbol, owner, disposition, and removal gate. |
</threat_model>

<verification>
- [ ] The machine-check command passes for all three TSVs.
- [ ] Disposition totals in DISPOSITION-LEDGER.md match the TSV rows.
- [ ] Every privileged action has one allowed classification or an explicit blocker.
- [ ] Architecture totals and all 14 RuntimeProviderBundle facet rows reconcile mechanically.
</verification>

<success_criteria>
- Later package plans can select work by phase and owner without rediscovering scope.
- No runtime package receives implicit host or root authority through a file move.
</success_criteria>

<output>
Create .planning/phases/01-scope-and-behavior-baseline/01-03-SUMMARY.md when done.
</output>
