---
phase: NCC-04-runtime-contract
plan: 04
type: execute
wave: 4
depends_on:
  - "04-03"
autonomous: true
requirements:
  - PKG-06
  - PKG-07
  - AGENT-02
  - AGENT-03
  - AGENT-05
  - FABRIC-03
  - TEST-01
  - TEST-08
must_haves:
  truths:
    - "DCode and Pi use the same package and contract path without empty scaffolding."
    - "Neither package receives an operation that it does not need."
    - "Native behavior and Fabric evaluation remain distinct, package-owned test lanes."
    - "No standard package depends on a root-relative harness implementation."
---

<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

# Phase 4 Plan 4: DCode and Pi Closure

<objective>
Close the smaller DCode and Pi ownership gaps and prove that the contract does not require every
package to implement every optional operation.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Close DCode ownership</name>
  <action>Move remaining DCode-native assets, configuration, MCP, session, skill, compatibility, fixtures, and tests into `packages/nemoclaw-langchain-deepagents-code`. Preserve the native `dcode` path and keep the released Deep Agents Fabric evaluation explicitly separate. Delete matching core branches and temporary root mappings.</action>
  <verify>DCode package-only, native, Fabric evaluation, composed, image, archive, and negative capability tests pass.</verify>
</task>

<task type="auto">
  <name>Task 2: Close Pi ownership</name>
  <action>Move remaining Pi-native assets, configuration, session behavior, compatibility code, fixtures, and tests into `packages/nemoclaw-pi`. Keep unsupported MCP, messaging, gateway, dashboard, and pairing operations absent rather than adding no-op files.</action>
  <verify>Pi package-only, native, package-owned Fabric adapter, composed, image, archive, and negative capability tests pass.</verify>
</task>

<task type="auto">
  <name>Task 3: Remove transitional package mappings</name>
  <action>Make every standard package root the authoritative build and execution source. Remove bundled-source path translations and root-relative legacy asset fallbacks after exact installed-package tests cover installation, resume, rebuild, backup, restore, and upgrade.</action>
  <verify>Source architecture and hostile package tests reject sibling-root reads, implicit package data, and a missing package operation fallback.</verify>
</task>

</tasks>

<success_criteria>

- The four packages share responsibility names and contract semantics, not identical file counts.
- Pi demonstrates a small valid implementation; OpenClaw and Hermes demonstrate richer ones.
- A fifth package follows the same workflow without editing a core agent catalogue or switch.

</success_criteria>

<output>
Create `04-04-SUMMARY.md` with DCode and Pi move counts, absent capabilities, and deterministic
evidence.
</output>
