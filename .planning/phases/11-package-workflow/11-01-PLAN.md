---
phase: 11-package-workflow
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [PKG-01, DCO-02, HER-01, OCL-02, TEST-04]
files_modified:
  - packages/README.md
  - packages/nemoclaw-openclaw/**
  - packages/nemoclaw-hermes/**
  - packages/nemoclaw-langchain-deepagents-code/**
  - src/lib/onboard/managed-startup/image-runtime.ts
  - src/lib/agent/**
  - src/lib/actions/sandbox/**
  - test/package-contract/**
  - test/e2e/**
  - .planning/**
---
<!-- SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved. -->
<!-- SPDX-License-Identifier: Apache-2.0 -->

<objective>
Turn the three extracted agent runtime packages into clear examples of one NemoClaw package
workflow, remove the next proven agent-specific core dispatch, and verify behavior without changing
the user experience.
</objective>

<context>
@.planning/phases/11-package-workflow/11-CONTEXT.md
@packages/README.md
@src/lib/harness/package-registry.ts
@src/lib/agent/defs.ts
@src/lib/onboard/managed-startup/image-runtime.ts
@test/package-contract/harness-packages.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the package workflow and configuration command</name>
  <action>Document the common root files and responsibility directories. Add one package-contract assertion for the workflow. Add `runtime/generate-config.sh` to each managed package image and install it at `/usr/local/lib/nemoclaw/generate-config`. Keep Pi as a compatibility consumer only; it must not shape the package contract.</action>
  <verify><automated>npx vitest run --project package-contract test/package-contract/harness-packages.test.ts</automated></verify>
  <done>The contract is visible in the tree and every managed package image exposes the same configuration command.</done>
</task>

<task type="auto">
  <name>Task 2: Reorganize the three package implementations</name>
  <action>Move package support files into `config`, `runtime`, `host`, `compat`, `plugin`, and `checks` according to execution responsibility. Keep common contract files at the package root. Update Dockerfiles, manifests, build inventories, core consumers, and tests without changing behavior. Add one README per package that tells its workflow and names version-bound compatibility debt. Split a large file only when it already contains separable responsibilities and the move can retain its protocol.</action>
  <verify><automated>npm run test:package &amp;&amp; npm run typecheck:cli</automated></verify>
  <done>A reader can predict why each file exists from its path, and all three packages use the same directory vocabulary where responsibilities match.</done>
</task>

<task type="auto">
  <name>Task 3: Remove proven agent-specific core dispatch</name>
  <action>Replace the managed-startup generator switch with the fixed in-sandbox command. Audit remaining core package-path and agent-ID branches. Move a branch only when the package already owns the behavior and a bounded package contract can replace the named path without broad callback execution. Record remaining MCP and lifecycle coupling as explicit follow-up work rather than introducing a generic framework.</action>
  <verify><automated>npx vitest run --project cli src/lib/onboard/managed-startup-image-runtime.test.ts &amp;&amp; npm run checks:repository</automated></verify>
  <done>Core no longer selects configuration implementation by agent ID, and every remaining agent-specific core boundary has a named current reason.</done>
</task>

<task type="auto">
  <name>Task 4: Qualify deterministic and live behavior</name>
  <action>Run focused tests after each change set, then run package contracts, CLI type-checking, repository checks, and the non-live suite needed by the changed boundaries. Use the configured Brev environment and inference route for one credential-free install, onboard, inference, restart or reconciliation, status, and cleanup journey. Do not run live messaging-service tests. Record exact commands, results, and any remaining environmental limit.</action>
  <verify><automated>npm test</automated></verify>
  <done>Deterministic checks pass and one real no-messaging environment completes the changed package workflow.</done>
</task>

</tasks>

<verification>
- [x] Common configuration command is installed and selected without an agent-ID switch.
- [x] All three packages have responsibility-based trees and package workflow READMEs.
- [x] Package contracts, focused tests, type-checks, and repository checks pass.
- [ ] One credential-free live environment completes install through cleanup.
- [ ] No live messaging credential is required.
</verification>

<output>
Create `.planning/phases/11-package-workflow/11-01-SUMMARY.md` after every task is complete.
</output>
